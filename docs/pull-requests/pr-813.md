---
type: pull_request
state: closed (merged)
branch: release/0.4.0 → main
created: 2026-07-04T15:04:28Z
updated: 2026-07-06T14:02:24Z
author: vig-os-release-app[bot]
author_url: https://github.com/vig-os-release-app[bot]
url: https://github.com/vig-os/devkit/pull/813
comments: 0
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-07-06T14:02:21Z
synced: 2026-07-11T13:36:03.990Z
---

# [PR 813](https://github.com/vig-os/devkit/pull/813) chore: release 0.4.0

# [Release 0.4.0](https://github.com/vig-os/devcontainer/releases/tag/0.4.0) - 2026-07-06

This PR prepares release 0.4.0 for merge to main.

## [0.4.0](https://github.com/vig-os/devcontainer/releases/tag/0.4.0) - 2026-07-06

### Added

- **Shared `mkProjectServices` local-services helper (process-compose + services-flake)** ([#795](https://github.com/vig-os/devcontainer/issues/795))
  - New `lib.mkProjectServices` output implementing the local-services axis of [ADR-nix-devenv-strategy](docs/rfcs/ADR-nix-devenv-strategy.md): declared [`services-flake`](https://github.com/juspay/services-flake) modules become a daemonless [`process-compose`](https://github.com/F1bonacc1/process-compose) stack — `nix run .#services` boots native-process services with **no Docker/Podman daemon**, versions from the pinned `nixpkgs` lock (no out-of-lock image tags), and zero extra flake inputs downstream (both service flakes resolve from this flake's lock as dependency-free leaf entries)
  - This flake carries the validating PoC: `nix run .#services` boots **SeaweedFS (S3) + Postgres**, asserted end-to-end (boot, health probes, teardown, no container fixtures) by the new `tests/test_flake_services.py`. The issue named MinIO, but nixpkgs marks `minio` abandoned upstream with unfixed CVEs, so the PoC ships the maintained S3-compatible SeaweedFS instead (recorded in `docs/NIX.md` and on the issue)
  - The flake-parts question is resolved without adopting it: `mkProjectServices` uses `process-compose-flake`'s standalone `evalModules` (services-flake's documented no-flake-parts path), so the flake stays on `flake-utils`. Measured cost recorded in `docs/NIX.md` (~0.8 s eval / ~3 s boot-to-healthy vs devenv's ~165 s IFD)
  - The scaffold opts consumers in but never forces them: the flake stub documents a commented `packages.services` block and `justfile.project` ships a commented `services` recipe — both preserved-on-upgrade files, so existing consumers are untouched
- **Flake checks as CI Tier 0 (nix-fast-build)** ([#779](https://github.com/vig-os/devcontainer/issues/779))
  - The `project-checks` CI job now builds every `checks.<system>` derivation (treefmt formatting, deadnix/statix, dev-shell build, `devShellTools`, and the git-hooks.nix `pre-commit` gate) in parallel with an eval cache via **`nix-fast-build`**, replacing the serial `nix flake check` build step. `nix-fast-build` is exposed as `packages.<system>.nix-fast-build` (a pinned-nixpkgs passthrough) so CI runs it reproducibly via `nix run .#nix-fast-build` without baking a CI-only tool into the dev-shell or the image
  - A new `docs/rfcs/ADR-flake-checks-tier0.md` records the tiering: pure, source-only checks are Tier 0 (nix-driven, cacheable, portable), while effectful jobs (image testinfra, integration, vulnix CVE-DB, multi-arch/publish, the impure pre-commit hooks and repo/git-dependent pytest units) stay GitHub-Actions-orchestrated. It also records the driver decision (nix-fast-build on existing runners now; garnix as a documented future option; Tier-1 self-hosting deferred)
  - The flake output schema `nix-fast-build` does not build (the treefmt `formatter`, the `checks` names, the `install` app, the nixos/homeManager modules) is now validated in CI by `tests/test_flake_checks.py`, preserving what the removed `nix flake check` covered
- **`checks.pre-commit` flake gate via git-hooks.nix + prek** ([#778](https://github.com/vig-os/devcontainer/issues/778))
  - Added the [`cachix/git-hooks.nix`](https://github.com/cachix/git-hooks.nix) input and a `checks.pre-commit` output that runs the **sandbox-pure subset** of the pre-commit hooks under `nix flake check`, driven by the `prek` runner (`package = pkgs.prek`) — no network, no project venv. It reuses the `treefmt` wrapper for the single formatting hook (nixfmt + ruff-format + taplo), the nix-provided pure linters (`ruff`, `shellcheck`, `yamllint`, `typos`, `taplo lint`, `just --fmt --check`), the `pre-commit-hooks` meta hooks, and the `vig-utils`/`bandit` hooks wired to hermetic Nix binaries — so the flake is a Nix-verified guarantee that the committed config's pure hooks stay correct
  - Impure/generator/stage-gated hooks (`generate-docs`, `sync-manifest`, `pip-licenses`, `pymarkdown`, `no-commit-to-branch`, `destroyed-symlinks`, `check-agent-identity`, and the `commit-msg`/`prepare-commit-msg` hooks) stay runner-only in the committed `.pre-commit-config.yaml`; the two-artifact model is documented in `docs/NIX.md`
- **Docs: local Nix image build/iterate loop + downstream `agent-models.toml` customization** ([#717](https://github.com/vig-os/devcontainer/issues/717))
  - `docs/NIX.md` gained a "Building and iterating the image locally" section: when to build locally vs. pull the published image, the `just build` → `just test-image` iterate loop, and that `just build` tags `<repo>:dev` (the tag the default `test`/`test-image`/`test-integration` recipes use and auto-build)
  - `docs/SKILL_PIPELINE.md` gained a "Customizing models downstream" note under **Model Selection** explaining how a consuming project overrides the `[models]` tiers and `[skill-tiers]` assignments in its own committed `.claude/agent-models.toml` — no recipe edits needed
- **Flake polish: treefmt-nix, deadnix/statix gates, NixOS/home-manager modules, `nix run .#install`** ([#777](https://github.com/vig-os/devcontainer/issues/777))
  - `nix fmt` now runs [`treefmt`](https://github.com/numtide/treefmt-nix) across every supported language in one pass (`nixfmt-rfc-style` for `*.nix`, `ruff format` for `*.py`, `taplo` for `*.toml`), wrapping the same formatters the pre-commit hooks already run so the editor, hooks, and CI agree on one formatting
  - Added `checks.deadnix` and `checks.statix` (dead-Nix-code + anti-pattern linters), scoped to the authored `flake.nix`; `deadnix` and `statix` also join `devTools`
  - Added `nixosModules.default` and `homeManagerModules.default` that install the shared `devTools` toolchain into a NixOS / home-manager configuration via `programs.vigos-devtools.enable = true`
  - Added `apps.install` so `nix run github:vig-os/devcontainer#install` bootstraps a consumer project straight from the flake (wrapping `install.sh`, which stays the behavior SSoT)
- **Secrets-management pattern ADR (sops-nix/age + OIDC)** ([#780](https://github.com/vig-os/devcontainer/issues/780))
  - Added `docs/security/ADR-secrets-management.md` recording the storage/delivery pattern for two secret classes — sops-nix + age for runtime/downstream-consumer secrets (each consumer decrypts with their own key, no per-repo GitHub-secret dance) and GitHub OIDC for cloud/registry auth — plus the honest caveat that on hosted runners SOPS relocates the root of trust to one bootstrap key rather than eliminating the GitHub secret, with OIDC as the only true no-stored-secret lever. Classifies each current stored secret (`CACHIX_AUTH_TOKEN`, `RELEASE_APP_*`, `COMMIT_APP_*`) as OIDC / sops / keep, notes GHCR-via-`GITHUB_TOKEN` and keyless cosign are already correct, and ships an inert reference example under `docs/security/examples/sops-nix/`. Complements the agent-behaviour standard in [#786](https://github.com/vig-os/devcontainer/issues/786) (design only; no workflow changed)
- **Nix dev-environment strategy ADR (activation / shell definition / local services)** ([#794](https://github.com/vig-os/devcontainer/issues/794))
  - Added `docs/rfcs/ADR-nix-devenv-strategy.md` recording why the org uses `pkgs.mkShell` (via `mkProjectShell`) + `nix-direnv` and rejects `devenv`/`numtide/devshell` as the shared builder. Reframes the recurring "direnv vs devenv vs devshell vs mkshell" question as three separable axes — activation (`nix-direnv` vs `nix develop`), shell definition (`mkShell` vs `devshell` vs `devenv`), and local services (`devenv up`-style orchestration) — noting devenv couples the latter two, which is the source of the confusion. Ratifies axes 1–2 on the dev-shell↔image parity-SSoT constraint plus devenv's ~165s IFD cold-eval (measured, [exo-pet/exo-fleet#76](https://github.com/exo-pet/exo-fleet/issues/76)), and adopts `process-compose` + `services-flake` for local services, with the shared `mkProjectServices` helper tracked in [#795](https://github.com/vig-os/devcontainer/issues/795). Authoritative for `vig-os`; a recommendation to `exo-pet`/`exoma` siblings. Cross-linked from `docs/NIX.md` (decision record; no code or workflow changed)
- **In-container `.#devShellTools` parity test** ([#754](https://github.com/vig-os/devcontainer/issues/754))
  - `tests/test_image.py` now reads the `devTools` toolchain SSoT straight from the flake (`nix eval --json .#devShellTools.<system>`, never a hardcoded list) and asserts every entry resolves on PATH inside the running image via `command -v`, parametrized per tool. Previously the SSoT was exercised only on the dev-shell side (`tests/test_flake_devshell.py`, skipped where the host lacks nix) while the image had a hand-curated check covering ~10 of the 27 tools — so adding a tool to `devTools` but not shipping it in the image went uncaught. This turns the SSoT into an actual image-side gate
- **Bake `/etc/nix/nix.conf` enabling `nix-command`/`flakes` and on-demand local builds** ([#739](https://github.com/vig-os/devcontainer/issues/739), [#749](https://github.com/vig-os/devcontainer/issues/749))
  - The Nix-built image bundles CppNix but shipped no `nix.conf`, leaving the modern CLI's `nix-command`/`flakes` features disabled by default so ad-hoc on-demand tooling (`nix shell nixpkgs#<x>`, `nix run`, `nix eval`) failed without an explicit `--extra-experimental-features` flag. The `buildLayeredImage` bootstrap layer now writes `/etc/nix/nix.conf` with `experimental-features = nix-command flakes` and an empty `build-users-group =` so the in-image root/single-user/daemonless nix (which has no `nixbld` group) can do on-demand local builds — not just cache substitutions, e.g. a `rust-overlay` toolchain — instead of aborting with "the group 'nixbld' … does not exist" (the explicit `substituters`/`trusted-public-keys` are covered under Security, [#773](https://github.com/vig-os/devcontainer/issues/773))
- **Consolidated `docs/NIX.md` Nix reference** ([#255](https://github.com/vig-os/devcontainer/issues/255))
  - Added a single onboarding/architecture doc for the flake: the `devTools` toolchain SSoT and the dev-shell ↔ image parity guard, the stable/unstable channel split + fast-mover overlay, the Nix-built (`buildLayeredImage`) reproducible multi-arch image, the CppNix-vs-Lix and `pre-commit`-vs-`prek` decisions, the `vig-os` Cachix `direnv allow` flow, how `nixpkgs` bumps flow (Renovate `nix` manager + `vulnix` before/after), and the #639 publish-cutover — cross-linking `CONTRIBUTE.md`, `docs/NIX2CONTAINER.md`, and `docs/CONTAINER_SECURITY.md`
- **In-container Nix runtime smoke test** ([#675](https://github.com/vig-os/devcontainer/issues/675))
  - The `Nix Image (discovery)` workflow now runs a self-contained, network-free smoke script (`scripts/nix_runtime_smoke.sh`) inside the built image to prove the baked Nix toolchain actually *functions* (not merely that it is present, which is all the portable testinfra suite checked): `nix --version`, `direnv version`, a real `nix eval` exercising the evaluator with `nix-command`/`flakes`, and a `direnv allow`/`exec` round-trip — gating the build/test job so a broken in-container `nix`/`direnv` fails CI
- **Nix flake quality gates** ([#674](https://github.com/vig-os/devcontainer/issues/674))
  - Added a `formatter` output (`nixfmt-rfc-style`) so `nix fmt` formats nix files idempotently, a `nixfmt --check` pre-commit hook (nixfmt sourced from the flake dev-shell), lightweight flake `checks` (format check, dev-shell build, `devShellTools` eval), and a `nix flake check --accept-flake-config` step in the CI project-checks job
- **Install/init delivery-mode picker (`--mode devcontainer|direnv|both`)** ([#641](https://github.com/vig-os/devcontainer/issues/641))
  - `install.sh` gained a `--mode devcontainer|direnv|both` flag (accepts both `--mode X` and `--mode=X`), validated up front and passed through to `init-workspace.sh`. Empty means "let init-workspace decide": the one-line install runs non-interactively and defaults to `both` (unchanged behaviour)
  - `init-workspace.sh` gained the same `--mode` flag plus an interactive prompt when the mode is unset and prompts are enabled (default selection `both`); under `--no-prompts`/`--smoke-test` with no `--mode` it defaults to `both`. After the rsync scaffold it prunes to the chosen mode: `devcontainer` removes the `flake.nix` + `.envrc` stub, `direnv` removes the `.devcontainer/` scaffold, and `both` keeps everything (prune is idempotent and scoped to the new workspace)
- **Downstream minimal flake stub (non-overwriting) + `nix2container` production builder** ([#640](https://github.com/vig-os/devcontainer/issues/640))
  - Scaffold `assets/workspace/flake.nix` (a minimal stub consuming the shared toolchain as a flake input — `vigos.url = github:vig-os/devcontainer`, `nixpkgs.follows = vigos/nixpkgs`, `vigos.lib.mkProjectShell` + a placeholder `extraPackages`) and `assets/workspace/.envrc` (`use flake` via nix-direnv). Updating the dev environment is `nix flake update vigos`; it never overwrites user files
  - Added both to the `PRESERVE_FILES` never-overwrite class in `init-workspace.sh` (same guarantee as `justfile.project`) and committed the template `.envrc` (un-ignored in the template `.gitignore`, with `.direnv/`/`.envrc.local` still ignored)
  - Documented the `nix2container` production-image pattern (`docs/NIX2CONTAINER.md`) with a buildable example (`examples/nix2container-production/`) that derives a minimal runtime image from the same pinned `nixpkgs`, plus a note on the future opt-in modular language shells
  - CI now gates the stub: a `nix flake check ./assets/workspace --override-input vigos path:.` step (and a `tests/test_downstream_flake.py` parity test) validates the scaffold against the working-tree toolchain, so an `lib.mkProjectShell`/`overlays.default` API change can't silently break a downstream `direnv allow`
- **`vulnix` + SBOM CVE scanning for the Nix image; re-authored security policy** ([#637](https://github.com/vig-os/devcontainer/issues/637))
  - Added a nightly `scan-nix-image` job that builds the image's package closure (new flake `packages.devcontainerImageEnv`) and runs `vulnix` (the nixpkgs-native CVE scanner) as the primary signal, since a Nix image has no apt/dpkg database for Trivy's OS scanner; Trivy stays on to emit a CycloneDX SBOM and an SBOM-mode vuln view (defence in depth), and both scanners' output is archived as `vulnix`-vs-Trivy overlap evidence
  - Added the `vulnix-gate` utility (`packages/vig-utils`) and the `.vulnixignore` exception register: a HIGH/CRITICAL finding (CVSS v3 ≥ 7.0) blocks only when it is not covered by a non-expired exception. `.vulnixignore` reuses the `.trivyignore` `Expiration:` format and the `check-expirations` validator (pre-commit + CI), and exposes a pinned `packages.vulnix` for reproducible scans. The gate is non-blocking during discovery and becomes the #639 go/no-go gate at cutover
  - Re-authored `docs/CONTAINER_SECURITY.md` for the Nix posture: dropped the `apt --only-upgrade` escape hatch and the "why not `apt-get upgrade`" section, made "advance the pinned `nixpkgs` rev" the primary CVE lever, and documented the dual `.vulnixignore`/`.trivyignore` registers and the residual Debian `:latest` scan until decommission (#642)
- **Multi-arch Nix image (amd64 + arm64) discovery build** ([#636](https://github.com/vig-os/devcontainer/issues/636))
  - The `Nix Image (discovery)` workflow now builds `packages.devcontainerImage` natively on an amd64 (`ubuntu-24.04`) + arm64 (`ubuntu-24.04-arm`) matrix — no QEMU or cross-compilation — pushes per-arch discovery tags (`nix-dev-amd64`, `nix-dev-arm64`), and assembles a top-level multi-arch index (`nix-dev`) with `docker buildx imagetools create`, verifying both platforms via `imagetools inspect`
  - `cachix-action` runs with an auth token on every leg so the arm64 closure is pushed to the `vig-os` Cachix cache; the workflow stays `continue-on-error` and only touches the disposable `nix-dev*` tags — the versioned/`:latest` publish-cutover remains #639
- **Renovate `nix` manager for `flake.lock` maintenance** ([#638](https://github.com/vig-os/devcontainer/issues/638))
  - Enabled the Renovate `nix` manager and weekly `lockFileMaintenance` in `renovate.json` so flake inputs (notably `nixpkgs`) are bumped through the normal PR/CI gate; the existing `pep621`, `npm`, `github-actions`, and `dockerfile` managers are retained
  - Documented the compensating control in `docs/CONTAINER_SECURITY.md`: every `flake.lock`/nixpkgs-bump PR must include a `vulnix` before/after diff, since the `nix` manager reports only the input revision change and not which CVE a bump fixes
- **De-duplicate the flake into the toolchain SSoT** ([#631](https://github.com/vig-os/devcontainer/issues/631))
  - Factored a single `devTools` list in `flake.nix` as the source of truth shared by the dev-shell now and the image later, absorbing the agent-CLI toolkit (`rg`, `fd`, `bat`, `eza`, `delta`, `lazygit`, `zoxide`, `starship`, `freeze`, `expect`, `nvim`) plus `claude` ([#545](https://github.com/vig-os/devcontainer/issues/545))
  - Pinned `nixpkgs` to `nixos-25.05` and added a `nixpkgs-unstable` input overlaid only for fast-movers (`uv`, `gh`, `claude-code`); refreshed `flake.lock`
  - Added reusable flake outputs `lib.mkProjectShell`, `overlays.default`, and a `packages.devcontainerImage` stub for the later image build
  - Added a non-blocking `Nix Cachix` workflow (with `workflow_dispatch`) that builds the dev-shell and pushes its closure to the `vig-os` Cachix cache
  - Added a per-tool `nix develop -c <tool> --version` parity test driven from the flake SSoT to guard against future dev-shell/image drift
- **nix-direnv onboarding fast path** ([#633](https://github.com/vig-os/devcontainer/issues/633))
  - Switched `.envrc` from bare `use flake` to nix-direnv: the dev-shell evaluation is now GC-rooted and cached under `.direnv/`, so re-entering the directory is instant and the closure is never garbage-collected; nix-direnv self-bootstraps on first `direnv allow` and falls back to bare `use flake` when unavailable
  - Documented the clone → `direnv allow` onboarding flow, the `vig-os` Cachix substituter (binary fetch instead of from-source build on first allow), and enabling the `nix-command`/`flakes` experimental features in `CONTRIBUTE.md` ([#255](https://github.com/vig-os/devcontainer/issues/255))
- **Build the devcontainer image with Nix (`buildLayeredImage`, non-publishing)** ([#634](https://github.com/vig-os/devcontainer/issues/634))
  - Fleshed out `packages.devcontainerImage` from a stub into a real, bit-reproducible image assembled by `dockerTools.buildLayeredImage` (not a Dockerfile `FROM`); a `--rebuild` verifies the closure hash is identical
  - Baked the in-container Nix evaluator (upstream CppNix, `pkgs.nix`) plus `direnv`/`nix-direnv` into the closure so `nix`/`direnv` are live inside the container; documented the CppNix-vs-Lix and `pre-commit`-vs-`prek` decisions in the flake
  - Reproduced the Debian bootstrap layers in Nix: locale via `glibcLocales` + `LOCALE_ARCHIVE` (no `locale-gen`), `/root/assets`, pre-commit cache dir, template `.venv` scaffold (`UV_PYTHON_DOWNLOADS=never`, `UV_PYTHON=<nix python3.14>`), the `precommit`/`cc`/`cld` aliases, and `IS_SANDBOX=1`
  - Added `fakeNss` (root uid-0 user database) and a sticky `/tmp` to close the first FHS gaps surfaced by the portable testinfra (fixing `ssh`, `whoami`, and `tmux`)
  - Added a non-publishing `Nix Image (discovery)` workflow (with `workflow_dispatch`) that builds the image and runs the portable testinfra under `continue-on-error: true`

### Changed

- **BREAKING for consumers — this release is the Nix publish-cutover** ([#639](https://github.com/vig-os/devcontainer/issues/639), [#625](https://github.com/vig-os/devcontainer/issues/625))
  - From this release on, the published image (`:latest` and every versioned tag) is the Nix-built image: pure-Nix userland with **no `apt`/`dpkg`**, a `docker → podman` shim (no Docker engine), and uv-managed CPython 3.14 (pin `requires-python` as a range, never an exact patch). See `docs/MIGRATION.md` for the full consumer contract
  - The final Debian-built release is **0.3.9**; it stays pullable indefinitely but frozen (no CVE fixes). Rollback/stay-behind: pin `DEVCONTAINER_VERSION=0.3.9` in the repo-root `.vig-os`
  - Heads-up: the next release cycle renames the project `devcontainer` → `devkit`, moving the image to a new GHCR package `ghcr.io/vig-os/devkit` ([#781](https://github.com/vig-os/devcontainer/issues/781))
- **Scaffolded devcontainer verbs renamed `up`/`down`/… → `devc-up`/`devc-down`/…** ([#795](https://github.com/vig-os/devcontainer/issues/795), completed by [#806](https://github.com/vig-os/devcontainer/issues/806))
  - The managed `.devcontainer/justfile.devc` namespaces its compose-stack verbs — `devc-up`, `devc-down`, `devc-status`, `devc-logs`, `devc-shell`, `devc-restart`, `devc-open` — so generic verb names stay free for project use (the new opt-in `services` recipe was the trigger: `up` was squatted by the devcontainer stack). The file is managed (replaced on upgrade), so consumers pick the rename up automatically on their next upgrade; muscle memory is the only breakage
  - The audit follow-up (#806) completes the namespacing: `check` → `devc-check` and `devcontainer-upgrade` → `devc-upgrade` (recipes, their hint strings, and the `version-check.sh` notification text), freeing the generic `check` name for project use
- **Git-hook runner migrated from `pre-commit` to `prek`** ([#778](https://github.com/vig-os/devcontainer/issues/778), closes [#40](https://github.com/vig-os/devcontainer/issues/40))
  - The Rust [`prek`](https://github.com/j178/prek) (a faster, drop-in `pre-commit` replacement) is now the hook runner and joins the shared `devTools` SSoT, so it ships in both the dev-shell and the image; the standalone Python `pre-commit` is dropped from both — one fewer manylinux/FHS consumer in the image closure
  - The `.githooks` shims, `scripts/init.sh` (`prek prepare-hooks`), `just precommit` (`prek run --all-files`), the worktree setup (`prek install`), and the downstream scaffold now invoke `prek`; the baked hook cache is renamed `PREK_HOME=/opt/prek-cache` and the `precommit` shell alias runs `prek run`. The committed `.pre-commit-config.yaml` (root + scaffold) is unchanged and prek runs it as-is
  - The CI lint gate (`.github/actions/test-project`) now runs the whole committed hook suite via `prek run --all-files` (was `uv run pre-commit run --all-files`), so `prek` — not the Python `pre-commit` — is what CI validates against the impure hooks too; `pre-commit==4.6.0` is removed from `pyproject.toml` + `uv.lock` (and the now-vestigial `pre-commit-` dev-shell PATH exclusion in `setup-env` is dropped), completing the "dropped from both" migration
  - Migration-completeness follow-ups: the committed `check-yaml` hook now passes `--allow-multiple-documents` in both the runner and `checks.pre-commit` so the Nix gate is no longer more lenient than the runner on multi-document YAML; the worktree `prek install` wires all three hook stages (`-t pre-commit -t commit-msg -t prepare-commit-msg`) so commit-msg / prepare-commit-msg hooks run in worktrees; and the downstream scaffold's remaining `pre-commit`/`PRE_COMMIT_HOME` references (CI `env`, `container-ci-quirks.md`, `init-precommit.sh`) are repointed at `prek`/`PREK_HOME`
- **Nix image bakes the build-time placeholder manifest so workspace init takes the fast path** ([#718](https://github.com/vig-os/devcontainer/issues/718))
  - The flake bootstrap layer now generates `/root/assets/.placeholder-manifest.txt` (the file `init-workspace.sh` reads next to itself) by `grep`-listing every workspace asset that carries a `{{SHORT_NAME}}`/`{{ORG_NAME}}`/`{{GITHUB_REPOSITORY}}` token, at its in-image runtime path and sorted for bit-reproducibility. Previously the Nix image shipped without the manifest, so `init-workspace.sh` always fell back to a slow runtime `find`+`grep` over the whole scaffold; the fast substitution path now fires. Output is unchanged (the fallback already produced correct results) — this is a startup-time optimization only
- **CI provisions every job from the Nix flake — the ad-hoc `setup-env` install path (and its hardcoded `uv` pin) is gone** ([#720](https://github.com/vig-os/devcontainer/issues/720))
  - The `setup-env` composite action is now flake-only: it always installs Nix + Cachix and enters the flake dev-shell, so CI and local `nix develop` run the exact same toolchain (uv, Python, just, taplo, BATS, linters). The `provision-via-flake` toggle and the ad-hoc install steps (`astral-sh/setup-uv`, `actions/setup-python`, `taiki-e/install-action` for just, the taplo curl, and `bats-action`) — with their now-removed `install-python`/`python-version`/`install-just`/`install-taplo`/`install-bats` inputs and the unused `uv-version` output — are deleted
  - Resolves the version drift #720 was filed for: the second, hardcoded `uv` pin (`0.11.23`) in `setup-env` is removed, so the provisioned `uv` version now flows from a single source — the flake's overlaid `pkgs.uv.version` in `flake.lock`. The lightweight security and release-orchestration jobs (which previously used the ad-hoc path) now pull the warm `vig-os` Cachix closure instead; `security-scan.yml`'s Nix-image job drops its duplicate direct Nix/Cachix setup in favour of the shared action. Host-integration tools (podman, Node.js, the devcontainer CLI) keep their dedicated steps
- **`nix fmt` and the flake format gate now run treefmt (superseding the nixfmt-only formatter)** ([#777](https://github.com/vig-os/devcontainer/issues/777))
  - The `formatter` output is the treefmt wrapper (was `nixfmt-rfc-style` directly, #674) and the flake `checks.format` gate is replaced by `checks.formatting` (a `treefmt --fail-on-change` check covering nix, python, and toml — superseding the former `nixfmt --check`-over-`*.nix` gate from #774)
- **Image-closure Cachix push is now first-class and blocking on the trusted paths** ([#776](https://github.com/vig-os/devcontainer/issues/776))
  - Published images are now guaranteed **cache-backed**: on the trusted paths (push to `dev` and releases) the built image closure is pushed to the `vig-os` Cachix cache as a **blocking** step (`nix path-info --recursive ./result | cachix push`), so consumers substitute the exact pinned closure instead of rebuilding it from source. Previously the image closure only reached the cache incidentally (a non-blocking, discovery-only side effect of `cachix-action`)
  - `.github/actions/build-image` gained an opt-in `push-image-closure` input (default `false`) that performs the blocking push, guarded on a non-empty auth token — so per-PR CI stays **pull-only** and fork PRs (which lack `CACHIX_AUTH_TOKEN`) never fail. The release `build-and-test` job opts in; the `Nix Image (discovery)` workflow pushes each per-arch image closure on `dev` as the same blocking step (distinct from the still-non-blocking GHCR discovery *tag* push)
  - The release CVE gate (`vulnix-gate`) now also pushes the `devcontainerImageEnv` scan-target closure, so the vulnix scan surface is cache-backed too. Documented the guarantee in `docs/NIX.md` and `docs/CONTAINER_SECURITY.md`
- **Flake cheap-fix batch: darwin-guarded image outputs, single nixpkgs-unstable eval, uv downloads URL derived from version, Renovate wheel-hash rule, nixfmt over all `*.nix`** ([#774](https://github.com/vig-os/devcontainer/issues/774))
  - The Linux-only `packages` (`devcontainerImage` plus its `devcontainerImageEnv`/`vulnix` scan targets) are now exposed only on `*-linux` systems, so `nix flake check --all-systems` no longer aborts during the darwin `dockerTools` evaluation; the dev-shell stays cross-platform
  - `nixpkgs-unstable` is imported once per system and threaded into the fast-mover overlay, instead of being re-imported inside the overlay fixpoint on each application
  - `UV_PYTHON_DOWNLOADS_JSON_URL` is derived from `pkgs.uv.version` rather than a literal version pin, so it can no longer drift from the overlaid (floating) `uv`
  - Added a Renovate custom regex manager that tracks the `pip-licenses` wheel version pinned in `flake.nix` (the sha256/URL hash path is refreshed by hand when the bump PR lands)
  - The flake `checks.format` gate now runs `nixfmt --check` over every `*.nix` in the repo (source filtered to `.nix` files), not just `flake.nix`
- **Resync security-gate docs to reflect the blocking vulnix gate** ([#758](https://github.com/vig-os/devcontainer/issues/758))
  - `docs/NIX.md`, `docs/CONTAINER_SECURITY.md`, and the `security-scan.yml` summary string still described the nightly `vulnix` CVE gate as non-blocking / in a discovery phase, with a stale `builder: debian|nix` selector reference; the gate is now **blocking** (#639) and the build pipeline is Nix-only post-Debian-decommission (#642), so the wording is corrected to match
- **Offline skip-guard for network-dependent image tests** ([#761](https://github.com/vig-os/devcontainer/issues/761))
  - The image tests that fetch over the network — `test_uv_venv_workflow` (`uv add`/`uv sync` from PyPI) and `test_npm_global_install_resolves_on_path` (`npm install -g tsx` from the npm registry) — now `pytest.skip(...)` when the host cannot reach the relevant registry instead of failing on an offline/air-gapped runner. A reusable `_host_can_reach(host, hostname)` probe (generalizing the existing `_pypi_reachable` PyPI check) backs the guards; online runs still execute the tests unchanged
- **Applied the still-relevant Renovate dependency updates** ([#625](https://github.com/vig-os/devcontainer/issues/625))
  - Folded the open Renovate PRs that still apply post-migration onto the epic: `actions/cache` → v6.1.0 (the v6 service, superseding the v5.x bumps), `actions/attest`/`actions/attest-build-provenance` → v4.1.1, `actions/setup-python` and `taiki-e/install-action` (just) digest refreshes, and `pandas` 3.0.3 → 3.0.4 in the workspace template. Stale ones were dropped: the `python:3.14-slim-bookworm` Docker digest (the `Containerfile` is gone) and the `ruff` pip pin (sourced from the flake now, #697); the `uv` 0.11.23 → 0.11.25 bump is deferred to the flake↔setup-env version sync (#720)
- **`SECURITY.md` describes the Nix image security posture** ([#642](https://github.com/vig-os/devcontainer/issues/642))
  - Replaced the stale Debian/`Containerfile` base-image pin, the "Trivy in CI/release" line, the 78 Debian LOW-CVE `.trivyignore` note, and the nine legacy BATS-npm GHSA exceptions (the framework now ships from the flake) with the current `vulnix` + CycloneDX SBOM scanning model and the `.vulnixignore`/`.trivyignore` dual register, mirroring `docs/CONTAINER_SECURITY.md`
- **README now describes the Nix-built image** ([#673](https://github.com/vig-os/devcontainer/issues/673))
  - Replaced the stale `python:3.12-slim-trixie` Debian base-image claim with the actual build: a Nix flake assembled via `dockerTools.buildLayeredImage` (no Debian/Docker base), with CPython 3.14 and the toolchain from a pinned `nixpkgs`, bit-reproducible
- **Make `just init` Nix-first** ([#671](https://github.com/vig-os/devcontainer/issues/671))
  - Rewrote `scripts/init.sh` from a multi-OS package installer into a Nix-first gate + bootstrapper: it requires Nix (and direnv, unless `--no-direnv`) and the dev-shell toolchain, then performs one-time, idempotent project bootstrap (`uv sync --frozen --all-extras`, git hooks path, commit-message template, `pre-commit install-hooks`) with advisory `podman info` / `gh auth status` checks. It no longer installs any tool — the toolchain is the flake's `devTools` — and short-circuits inside the built image (`IN_CONTAINER=true`)
  - Repointed `docs/generate.py` and the `CONTRIBUTE.md.j2` template: the per-OS "Requirements" table is now a "Prerequisites: Nix + direnv + a working host container runtime" section, with the toolchain sourced from `flake.nix`
- **Nix image passes the full testinfra suite (toolchain parity)** ([#666](https://github.com/vig-os/devcontainer/issues/666))
  - Packaged `vig-utils` (and `pip-licenses` from its PyPI wheel, as it is not in nixpkgs) as Nix python packages exposed through a `python314.withPackages` env, and added `ruff`, `bandit`, `cargo-binstall`, `just-lsp`, and `typstyle` from nixpkgs — the Nix image now carries the project Python toolchain hermetically, replacing the Debian image's build-time `uv pip install`
  - Relaxed `requires-python` from `==3.14.6` to `>=3.14,<3.15` across the root, `vig-utils`, and workspace-template pyprojects: `flake.lock` is the reproducibility anchor now, so the exact pin was redundant and unsatisfiable against nixpkgs (3.14.4)
  - Adapted `tests/test_image.py` to the Nix toolchain (version prefixes are nixpkgs-pinned, so fast-movers/mismatched tools are checked for presence/run only; the pre-commit cache dir is asserted present rather than pre-populated, since a hermetic build cannot fetch hook repos), taking the suite to 63/63 — and made the `nix-image.yml` `build-and-test` job gate on it (discovery phase closed)
- **Stage the Nix publish-cutover; advance the nixpkgs baseline to 26.05** ([#639](https://github.com/vig-os/devcontainer/issues/639))
  - Bumped the pinned channel `nixos-25.05` → `nixos-26.05` (the "advance the rev" CVE lever), cutting the vulnix HIGH/CRITICAL surface 83 → 27 and Trivy HIGH 244 → 14 on the image; triaged the residual 27 into `.vulnixignore` (4 CPE-mismatch false positives — VS Code/Jenkins, not the binaries; 23 recent CVEs accepted as low-risk in an interactive dev container with a 3-month re-review)
  - Made the nightly `vulnix-gate` **blocking** (the #639 go/no-go gate) now that it is legitimately green, and archived the `vulnix`-vs-Trivy scan overlap in `docs/security/nix-cutover-scan-overlap.md` (zero overlap — disjoint surfaces, no finding class lost in the Debian→Nix switch)
  - Staged the publish-cutover so the versioned/`:latest` publish stays paused pending a deliberate Nix release: the nightly `vulnix-gate` is the go/no-go signal. The build pipeline became Nix-only once the Debian path was decommissioned (#642), so no `builder` toggle remains — the interim `builder: debian|nix` selector this issue introduced was superseded by that decommission
- **Make `.claude/` the single source of truth for agent rules and skills** ([#626](https://github.com/vig-os/devcontainer/issues/626))
  - Moved the 30 agent skills from `.cursor/skills/` to `.claude/skills/` and rewrote the 29 `.claude/commands/*.md` wrappers to point at the new paths
  - Split the seven `.cursor/rules/*.mdc`: static principles (coding principles, commit messages, changelog, single source of truth) are now consolidated in `CLAUDE.md`; workflow rules (`branch-naming`, `tdd`, `subagent-delegation`) became on-demand `.claude/skills/`
  - Ported `agent-models.toml` and `worktrees.json` to `.claude/`, updated the docs generator, pre-commit hooks, shell entrypoints, and the workspace sync manifest, and deleted the root `.cursor/` directory
- **Drive autonomous worktree pipelines with the `claude` CLI** ([#627](https://github.com/vig-os/devcontainer/issues/627))
  - `just worktree-start`/`worktree-attach` now launch `claude --dangerously-skip-permissions` in the tmux session instead of `cursor-agent` (`agent chat --yolo --approve-mcps`); the cursor-specific directory-trust step and the `tmux send-keys "a"` approval trigger are no longer needed and have been removed
  - Prerequisite, authentication (`claude auth status`/`claude auth login`, `ANTHROPIC_API_KEY`), and `scripts/requirements.yaml` now reference the `claude` CLI rather than the Cursor Agent CLI
- **Migrate the workspace template and editor glue off Cursor (VS Code only)** ([#629](https://github.com/vig-os/devcontainer/issues/629))
  - New workspaces now scaffold `.claude/` (skills, `agent-models.toml`, `worktrees.json`) instead of the removed `.cursor/` template tree; the sync manifest carries the `.claude/` payload accordingly
  - `just open` launches VS Code only (dropped the `command -v cursor` fallback), and `verify-auth.sh` no longer scans the `cursor-remote-ssh` SSH-agent socket
  - `COMMIT_MESSAGE_STANDARD.md` now refers to VS Code rather than "VS Code / Cursor"
- **Make the image testinfra suite portable across Debian and Nix images** ([#635](https://github.com/vig-os/devcontainer/issues/635))
  - Replace dpkg `host.package(...).is_installed` checks (git, curl, openssh-client, nano, tmux, rsync) with path-agnostic `--version`/`-V` runs
  - Resolve `gh`, `just`, `hadolint`, `taplo` and cargo-installed tools via PATH (`command -v`) instead of hardcoded `/usr/local/bin` / `/root/.cargo/bin` / `/root/.local/bin` locations
  - Drop the `DEBIAN_FRONTEND` environment assertion and the apt-sourced version-prefix checks (git, curl, tmux, rsync) from `EXPECTED_VERSIONS`
- **Provision CI build/test tooling from the flake dev-shell** ([#632](https://github.com/vig-os/devcontainer/issues/632))
  - The `setup-env` action gained a `provision-via-flake` mode that installs Nix (SHA-pinned `install-nix-action`) and the `vig-os` Cachix substituter, builds the flake dev-shell, and prepends its tools to `PATH`, replacing the ad-hoc installs of `uv`/Python, `just`, `hadolint`, and `taplo`
  - Enabled the mode in the CI build/test path (`build-image`, `test-image`, `test-integration`, `project-checks`) so jobs run inside the flake shell (the toolchain SSoT); `podman`, Node.js, BATS, and the devcontainer CLI keep their dedicated install paths
  - Set `UV_PYTHON_DOWNLOADS_JSON_URL` in the flake dev-shell so the nixpkgs `uv` (whose embedded Python-download list is stripped) can fetch the project's pinned CPython `3.14.6`, which nixpkgs does not package, letting `uv sync --frozen` succeed under flake provisioning
  - Keep `podman` off the flake-provisioned `PATH` so the runner's rootless-configured host `podman` is used (the nix-store `podman` cannot reach the host's setuid `newuidmap`/`newgidmap`, so `podman info` failed)

### Removed

- **Removed the sidecar / multi-container capability** ([#799](https://github.com/vig-os/devcontainer/issues/799))
  - Dropped the `just sidecar`/`just sidecars` recipes, the `docker-compose.project.yaml` sidecar examples, the sidecar test fixtures (`sidecar.Containerfile`, `test-build.sh`, the fixtures `justfile`, the `test-sidecar` compose service) and `TestSidecarConnectivity`, and `hadolint` from `devTools` (unused since the Containerfile lint hook was dropped in #642; the deleted sidecar Containerfile was its last Dockerfile-like artifact)
  - Migration: use Nix devShells for toolchains and `process-compose`/`services-flake` ([#795](https://github.com/vig-os/devcontainer/issues/795)) for local services; the Docker-out-of-Docker socket for building containers is retained
- **Retire `scripts/requirements.yaml`** ([#671](https://github.com/vig-os/devcontainer/issues/671))
  - Deleted the per-OS dependency manifest and its consumers (the `load_requirements`/`format_requirements_table`/`format_install_commands` helpers in `docs/generate.py` and their tests). `flake.nix` `devTools` is now the single source of truth for the toolchain, ending the dual-SSoT drift
- **Decommission the Debian build path** ([#642](https://github.com/vig-os/devcontainer/issues/642))
  - Deleted the root `Containerfile`, `scripts/prepare-build.sh`, `scripts/build.sh`, and the `.hadolint.yaml` config (plus its synced workspace copy); the image now builds Nix-only
  - Removed the `hadolint` pre-commit hook and its `setup-env`/`test-project` install wiring, the `hadolint` and Containerfile entries from `scripts/requirements.yaml` and `scripts/manifest.toml`, and the `Containerfile`/build-script `CODEOWNERS` entries
  - `build-image`, `release.yml`, and `ci.yml` are now Nix-only
  - Dropped the Debian `scan-latest` nightly Trivy job, the ~78 Debian OS-package CVE entries from `.trivyignore`, and the Renovate `dockerfile` manager; `docs/CONTAINER_SECURITY.md` now reads as Nix-only
- **Remove the `cursor-agent` CLI install from the image** ([#628](https://github.com/vig-os/devcontainer/issues/628))
  - Dropped the unpinned `curl … cursor.com/install` build step and its `/root/.local/bin` PATH entry, leaving an all-nixpkgs toolchain ahead of the Nix migration
  - Removed the coupled `test_cursor_agent_installed` image test

### Fixed

- **vulnix CVE scans survive `nvd.nist.gov` outages via a self-hosted NVD feed mirror** ([#870](https://github.com/vig-os/devcontainer/issues/870))
  - `nvd.nist.gov` is chronically throttled/unstable (upstream `nix-community/vulnix#171`): cold scan runs crawled the full NVD year-feed set for tens of minutes and failed with `ReadTimeoutError`/`IncompleteRead`, blocking both the nightly gate and the release publish gate. A dedicated public mirror, [`vig-os/nvd-mirror`](https://github.com/vig-os/nvd-mirror), now downloads the NVD 2.0 feed files (`nvdcve-2.0-<6 years>.json.gz` + `modified`) with resumable retry, validates them, and serves them on GitHub Pages — the only job that talks to NVD
  - `security-scan.yml` and the `release.yml` `vulnix-gate` now run `vulnix --mirror https://vig-os.github.io/nvd-mirror/`, fetching NVD data from a CDN-backed mirror instead of `nvd.nist.gov`, so an NVD outage can no longer fail a scan or block a release. The mirror also localizes the NVD-format dependency to one place should the feeds ever change
- **Renovate changelog automation no longer self-triggers an empty-commit loop and keeps the workspace mirror in sync** ([#863](https://github.com/vig-os/devcontainer/issues/863))
  - The changelog commit is pushed with a GitHub App token (which re-triggers workflows), but the build gated only on the PR author (permanently `renovate[bot]`) and never the pusher, and `commit-action` has no empty-diff guard — so the bot's own no-op commits fired fresh `synchronize` events without end (#862 accrued 150+ identical empty commits before the build was disabled by hand). The build now skips `synchronize` events raised by the changelog commit bot (`github.event.sender.login`), severing the loop
  - The automation committed only root `CHANGELOG.md`, leaving the `scripts/manifest.toml` mirror `assets/workspace/.devcontainer/CHANGELOG.md` stale so the `sync-manifest` gate failed on every Renovate PR; the build now mirrors the verbatim copy and commits both files
- **rc4 field-validation fix batch: direnv-mode commits, pipe-safe scripts, prune/typos/installer hardening** ([#859](https://github.com/vig-os/devcontainer/issues/859))
  - Scaffolded `.githooks/*` now use `#!/usr/bin/env bash` (hosts without `/bin/bash`, e.g. NixOS, could not run them) and accept the nix dev-shell (`IN_NIX_SHELL`) as a sanctioned commit environment — previously direnv-mode consumers could not commit at all (the guard demanded the container)
  - Restored the `${BASH_SOURCE[0]:-$0}` pipe-safety fallback in the scaffolded lifecycle scripts (`initialize`/`post-create`/`post-attach`/`version-check`) — a regression from the 0.3.x scaffold caught by a consumer's own regression tests
  - `--mode devcontainer` no longer deletes a consumer's **pre-existing** `flake.nix`/`.envrc` (two real repos lost their own nix-direnv setup); the prune now only removes stub files the scaffold itself created, mirroring the #738 guard
  - The installer's final `just sync` is non-fatal when the preserved old-generation `justfile.project` predates the `sync` recipe (warns and points to MIGRATION.md instead of failing an otherwise complete scaffold)
  - The typos hook (repo + scaffold) passes `--force-exclude` so `[files] extend-exclude` applies to explicitly-passed staged files — three consumer repos hit garbage findings on committed binary artifacts (PDFs, SVGs, `.bin` fixtures)
  - `docs/MIGRATION.md` gained the field-validated "Upgrading an existing 0.3.x consumer" checklist (prek recipe migration, recipe renames, typos config precedence/shadowing, artifact excludes, name re-derivation)
- **Shipped consumer `prepare-release.yml` caps its PR body under GitHub's 65,536-char limit** ([#857](https://github.com/vig-os/devcontainer/issues/857))
  - #812 capped the PR body only in this repo's own workflow; the scaffolded consumer copy still interpolated the full frozen changelog section uncapped, so a consumer with a large release (the 0.4.0-rc3 smoke test seeds this repo's ~67k-char section) failed `Create draft PR to main` with `GraphQL: Body is too long`. The shipped workflow now applies the same line-boundary truncation with a pointer to the release branch's full `CHANGELOG.md`
- **Scaffold ships `.typos.toml` so the shipped typos hook passes out of the box** ([#855](https://github.com/vig-os/devcontainer/issues/855))
  - The scaffolded `.pre-commit-config.yaml` runs the `typos` hook, but the exception config stayed repo-local — consumers linted scaffold-shipped content (`version-check.sh`'s `Nd` duration syntax, the synced changelog's "unexcepted" CVE-policy term) with zero exceptions and failed immediately, as the 0.4.0-rc2 smoke test showed. `.typos.toml` now syncs into the workspace template via `scripts/manifest.toml`, same as `.yamllint`/`.pymarkdown`
- **`install.sh --version` now pins the scaffolded `.vig-os` to the requested version** ([#852](https://github.com/vig-os/devcontainer/issues/852))
  - The Nix image bakes the release it was built from into the scaffolded `.vig-os` (correct for finals, where the repo pin is bumped at finalize — but stale for release candidates), and `install.sh` never wrote its `--version` into the scaffold. An RC install therefore pinned the previous release: the 0.4.0-rc1 smoke test scaffolded the new prek-era workspace pinned to the Debian 0.3.9 image and its CI lint failed with `prek: command not found`. `install.sh` now forwards an explicit `--version` as `VIG_OS_VERSION` and `init-workspace.sh` pins it in `.vig-os` post-scaffold; plain `latest` installs keep the baked pin
- **Release-lane Trivy scan aligned with the ratified awareness-only posture** ([#849](https://github.com/vig-os/devcontainer/issues/849))
  - `release.yml`'s build-and-test Trivy step was the last Debian-era blocking scanner (`exit-code: 1` with only `.trivyignore`), failing the candidate on embedded language-ecosystem HIGHs already triaged into `.vulnixignore`. Per the #637 decision the Nix image's blocking CVE control is the vulnix-gate; the step is now non-blocking (`exit-code: 0`, `continue-on-error`) with its table kept in the log as awareness signal, mirroring `security-scan.yml` and `ci.yml`
- **`setup-env` no longer flakes on a SIGPIPE race when listing the provisioned dev-shell PATH** ([#847](https://github.com/vig-os/devcontainer/issues/847))
  - The provisioning step's `grep '^/nix/store' | head -50` summary runs under the runner's `pipefail` shell; once the dev-shell PATH topped 50 nix-store entries, `head` exiting early could SIGPIPE `grep` (exit 2) and fail the step — a scheduling race that killed a release build-and-test leg. `sed -n '1,50p'` consumes the whole stream and prints the same summary
- **Release lane fixed for the Nix stack: current runners + tolerated vulnix findings exit** ([#842](https://github.com/vig-os/devcontainer/issues/842))
  - `release.yml`'s build-and-test matrix still pinned Debian-era `ubuntu-22.04`/`ubuntu-22.04-arm` runners, whose podman 3.4.4 breaks rootless volume UID mapping — `init-workspace.sh`'s in-container rsync failed to chmod the `/workspace` scaffold (`Operation not permitted`), killing the integration tests that pass everywhere else on `ubuntu-24.04` (podman 4.9.3). The matrix now uses the same `ubuntu-24.04`/`ubuntu-24.04-arm` expression as `nix-image.yml`
  - The release `Run vulnix` step missed `security-scan.yml`'s `|| rc=$?` capture, so under the runner's `bash -e` a normal exit 2 (findings present, to be judged against `.vulnixignore`) aborted the step before the vulnix-gate ran. The step now uses the nightly's capture/retry pattern (crashes with exit > 2 or invalid JSON still fail loudly)
- **Release pipeline no longer fails when the release changelog exceeds GitHub's PR-body limit** ([#810](https://github.com/vig-os/devcontainer/issues/810))
  - The `Create draft PR to main` step passed the entire frozen changelog section as the PR body; GitHub caps PR bodies at 65,536 characters, so a large release (e.g. 0.4.0 at ~64k chars) overflowed with `GraphQL: Body is too long` and the release rolled back. The step now caps the body, truncating the changelog at a line boundary and pointing to the full `CHANGELOG.md` on the release branch when it would overflow; small releases still inline the whole changelog unchanged
  - The same uncapped rebuild existed in `release.yml`: the finalize-path `Refresh release PR body from finalized changelog` step (which would have crashed the final release run mid-finalize and triggered rollback) and the rollback-path `Restore release PR body` step. Both now apply the same cap
- **Scaffold justfile audit: worktree recipes reachable, compose docs de-stale'd** ([#806](https://github.com/vig-os/devcontainer/issues/806))
  - The scaffold root `justfile` now imports `.devcontainer/justfile.worktree` (`import?`, so direnv-mode workspaces still parse): the shipped `.claude` worktree skills invoke `just worktree-start`, but nothing imported the recipes, so they were unreachable in consumer repos
  - The shipped `.devcontainer/README.md` no longer documents the long-removed `docker-compose.override.yml(.example)` workflow; it now describes the real layering (`docker-compose.project.yaml` team-shared / `docker-compose.local.yaml` personal) that #799 deliberately kept
  - Dropped a vacuous image-test assertion on a `docker-compose.local.yml` (`.yml`) spelling nothing ships, and aligned the dev-env ADR's illustrative services verb with the shipped `just services` recipe. The audit found no other compose-era leftovers: the repo-root justfiles are clean and the project/local compose layering is intentional
- **Review-nit batch: dry-run/scaffold-guard, summary cancel handling, help/message fixes** ([#759](https://github.com/vig-os/devcontainer/issues/759))
  - `install.sh` `--dry-run` now derives the shown command from the real `CMD` array via `printf '%q'` (no hand-maintained duplicate string), and the automatic "initial project scaffold" commit is guarded so it only runs for a freshly scaffolded (empty) target instead of sweeping a pre-populated directory
  - `release.yml` now treats a `cancelled` required job as not-green (it triggers rollback like a failure), so a cancellation can no longer leave a partial release un-rolled-back
  - `version-check.sh` help text corrected (`And (days)` → `Nd (days)`), and `init.sh` now reports "Pre-commit hook environments installed" (the `install-hooks` step only fetches hook environments)
- **Restore arm64 image-testing coverage post-merge** ([#760](https://github.com/vig-os/devcontainer/issues/760))
  - `nix-image.yml` (the only lane that builds + runs the portable testinfra suite natively on arm64) was push-filtered to the migration epic branch only, so once it merges the arm64 image would no longer be exercised on the integration branch — PR CI (`ci.yml`) builds and tests amd64 only. Added `dev` to the workflow's `push:` branch filter (keeping the existing `flake.nix`/`flake.lock`/workflow `paths:` guard) so the native amd64 + arm64 build/test matrix keeps running on `dev` post-merge
- **release.yml publish retags the loaded Nix image tag** ([#752](https://github.com/vig-os/devcontainer/issues/752))
- **install.sh `--skip-pull` works under docker; ci.yml runs safety via `uv run`** ([#757](https://github.com/vig-os/devcontainer/issues/757))
  - `install.sh` checked for a present local image with the podman-only `$RUNTIME image exists`, which docker lacks, so `--skip-pull` always failed under docker; it now uses `$RUNTIME image inspect`, which works on both runtimes
  - The CI `safety` dependency scan was invoked bare instead of via `uv run`, so it ran outside the uv env (unlike the adjacent `uv run bandit`); it now runs via `uv run safety`
- **Dev-shell exposes `python3` + `pre-commit` (image parity), CI-safely** ([#729](https://github.com/vig-os/devcontainer/issues/729))
  - `mkProjectShell` now ships a bare `python3`/`pre-commit` on PATH so the downstream flake-input/direnv dev-shell matches the image. `setup-env` filters the Nix `python3-<ver>` (and `pre-commit`) out of the forwarded CI runner PATH so `uv` keeps building the project venv from the downloaded managed CPython. No new `LD_LIBRARY_PATH`, so the #703 FHS leak-guard is intact. A `nix develop --ignore-environment` parity test (and the FHS leak-guard) now run in the Project Checks job
- **Nix image runs pre-compiled PyPI (manylinux) wheels at runtime (arch-aware FHS loader + baked `LD_LIBRARY_PATH`)** ([#736](https://github.com/vig-os/devcontainer/issues/736))
  - The bare `dockerTools.buildLayeredImage` shipped neither the FHS dynamic loader (the `PT_INTERP` every manylinux wheel hardcodes) nor the Nix C++/compression runtime on the loader path, so runtime-installed PyPI binaries broke: standalone tools (consumer pre-commit configs pinning PyPI `ruff`/`typos`) aborted with `cannot execute: required file not found`, and C extensions dlopened by the baked CPython (`numpy`/`scipy`, pre-commit `pymarkdown`'s `pyjson5`) failed with `ImportError: libstdc++.so.6`
  - The image now ships an architecture-aware FHS loader (`/lib64/ld-linux-x86-64.so.2` on x86_64, `/lib/ld-linux-aarch64.so.1` on aarch64 — the loader name and FHS dir are derived from the build platform, so the arm64 build passes `test_fhs_loader_exists`) symlinked to the Nix glibc loader (newer glibc runs the old-glibc wheels via backward compatibility), and bakes `LD_LIBRARY_PATH` with `${stdenv.cc.cc.lib}/lib` (libstdc++/libgcc_s) + `${zlib}/lib` (libz) so both standalone wheel executables and C-extension `.so` files dlopened by the store CPython resolve their runtime libs. This is the image-scope analogue of the dev-shell [#698](https://github.com/vig-os/devcontainer/issues/698) fix, but ungated: an all-Nix container has no foreign FHS host binaries to pollute, so the `/etc/NIXOS` ABI gate never applies
- **Nix image bakes the template `/root/assets/workspace/.venv` (renameable prompt)** ([#735](https://github.com/vig-os/devcontainer/issues/735))
  - The image advertised `UV_PROJECT_ENVIRONMENT`/`VIRTUAL_ENV` at `/root/assets/workspace/.venv` but never created it (the Debian image did). The published 0.3.x consumer `post-create.sh` runs `sed -i .../.venv/bin/activate` as its first venv step and aborted under `set -euo pipefail` (`exit 2`) when the file was missing — so git setup, gh auth, pre-commit install, and `just sync` never ran. The flake bootstrap layer now pre-creates the venv from the baked CPython (matching the advertised env vars, hermetic and network-free with no packages — `just sync` populates it), and normalizes the activate `VIRTUAL_ENV_PROMPT` to the quoted `"template-project"` form the consumer `post-create.sh` rename targets (so the prompt rename no longer no-ops)
- **`docker` now resolves in the Nix-built image (podman compatibility shim)** ([#740](https://github.com/vig-os/devcontainer/issues/740))
  - The image shipped `podman` but no `docker` binary, while the consumer `.devcontainer/docker-compose.yml` mounts the socket at `/var/run/docker.sock` and sets `DOCKER_HOST`/`CONTAINER_HOST`. Docker-out-of-Docker worked because podman honors `DOCKER_HOST`, but any recipe/script invoking `docker` literally failed with `command not found`. The bootstrap layer now bakes a tiny `docker -> podman` wrapper at `/usr/local/bin/docker` (already on the baked `PATH`) that execs the baked podman, so `docker`-literal callers get a working binary without pulling in the Docker engine
- **`install.sh --mode direnv --force` no longer clobbers a populated consumer repo** ([#738](https://github.com/vig-os/devcontainer/issues/738))
  - Re-initializing an existing project in `direnv` mode deployed the full workspace template over it: the scaffold `rsync` overwrote a real `pyproject.toml` with the generic template one, and copied the template `.devcontainer/` before the mode prune deleted the directory wholesale — destroying tracked files (`devcontainer.json`, project compose files, …). `pyproject.toml` is now in the never-overwrite `PRESERVE_FILES` class, and `direnv` mode excludes `.devcontainer/` from the copy and skips the prune when a populated `.devcontainer/` predates the (re)scaffold, so real project files survive untouched. `--force` still deploys the Nix/direnv stub (`flake.nix`, `.envrc`, `.vig-os`) onto repos that lack it
  - `install.sh` no longer prints a misleading `User configuration script not found … copy-host-user-conf.sh` warning in `direnv` mode, which scaffolds no `.devcontainer/` and therefore has no host-user-conf step to run
- **`/usr/bin/env` now exists in the Nix-built image** ([#727](https://github.com/vig-os/devcontainer/issues/727))
  - The bare `dockerTools.buildLayeredImage` had no `/usr/bin` at all, so the ubiquitous `#!/usr/bin/env <interp>` shebang failed with `/usr/bin/env: bad interpreter: No such file or directory` — breaking essentially every Node/Python/Ruby CLI (e.g. `node_modules/.bin/tsc`) for image-mode consumers. Added `dockerTools.usrBinEnv` (the FHS shim symlinking `/usr/bin/env` to coreutils `env`) to the image package set, alongside the existing `fakeNss` shim
- **`npm install -g` now lands CLIs on PATH in the Nix image** ([#728](https://github.com/vig-os/devcontainer/issues/728))
  - npm's default global prefix was the read-only `nodejs` nix-store path, whose `bin/` is not on `PATH` — so `npm install -g <tool>` reported success but the binary was unresolvable (`command -v <tool>` failed). The image now bakes `NPM_CONFIG_PREFIX=/usr/local` (already on the baked `PATH`) and creates a writable `/usr/local/bin` in the bootstrap layer, so globally installed CLIs resolve on `PATH`
- **`init-workspace --mode direnv` now produces a loadable `justfile`** ([#641](https://github.com/vig-os/devcontainer/issues/641))
  - The scaffolded root `justfile` hard-imported `.devcontainer/justfile.devc` and `.devcontainer/justfile.gh`, but `direnv` mode prunes `.devcontainer/` — so every `just` command (including init-workspace's own final `just sync`) failed to parse in a direnv-mode workspace. Made the two `.devcontainer/` imports optional (`import?`, matching `justfile.project`/`justfile.local`); the `sync` recipe lives in the preserved `justfile.project`, so `just sync` still works in all modes
- **Worktree recipes read agent config from the `.claude/` SSoT** ([#627](https://github.com/vig-os/devcontainer/issues/627))
  - The Cursor→Claude migration swapped the launch command but left `justfile.worktree` reading the removed `.cursor/` tree: a dead `_read_model` helper pointed at `.cursor/agent-models.toml`, and the branch-naming rule passed to `derive-branch-summary` was `.cursor/rules/branch-naming.mdc`. Removed the unused helper and repointed the rule at `.claude/skills/branch-naming/SKILL.md`; trimmed the stale `.cursor/` wording from the sync-manifest comment. Added `worktree-claude-cli.bats` / `test_claude_ssot.py` guards against entrypoints reading removed `.cursor/` config paths
  - `derive-branch-summary` (invoked by `just worktree-start` to name an unlinked branch) still shelled out to the removed `cursor-agent` binary (`agent --print --yolo --trust`); with no test exercising the non-`BRANCH_SUMMARY_CMD` path it slipped through CI. It now drives `claude --print --dangerously-skip-permissions`, and `agent-models.toml` tiers map to claude model aliases (`haiku`/`sonnet`/`opus`) instead of Cursor names (`composer-1.5`/`sonnet-4.5`/`opus-4.6`) so `--model` resolves
- **Integration tests now exercise the freshly-built image, not the published `DEVCONTAINER_VERSION`** ([#701](https://github.com/vig-os/devcontainer/issues/701))
  - The integration suite scaffolded a workspace from the image under test (`TEST_CONTAINER_TAG`) but then brought the devcontainer up from whatever `DEVCONTAINER_VERSION` resolved to (the published `0.3.9`), so it validated fresh scaffolding running inside a stale image. The `devcontainer_up`/`devcontainer_with_sidecar` fixtures now export `DEVCONTAINER_VERSION=TEST_CONTAINER_TAG`; compose reads the shell environment over `.env`, so the scaffolded `docker-compose.yml` resolves to the build under test (and every `devcontainer exec`, which inherits the environment, agrees). Added `test_devcontainer_runs_image_under_test` asserting the running container's image
- **Dev-shell resolves `pymarkdown`'s `pyjson5` `libstdc++` on NixOS without breaking `just` on FHS hosts** ([#698](https://github.com/vig-os/devcontainer/issues/698), [#703](https://github.com/vig-os/devcontainer/issues/703))
  - The `pymarkdown` hook runs from pre-commit's own manylinux-wheel Python env, whose dependency `pyjson5` is a C extension linked against `libstdc++.so.6`; on a NixOS host that library is off the loader path outside an FHS environment, so the hook aborted with `ImportError: libstdc++.so.6` and forced `--no-verify`. Unlike the standalone binaries in [#697](https://github.com/vig-os/devcontainer/issues/697), `pymarkdown` is not in nixpkgs, so the "add to `devTools` + `language: system`" recipe does not apply
  - `mkProjectShell` now appends the Nix C++ runtime (`${stdenv.cc.cc.lib}/lib`, libstdc++) to `LD_LIBRARY_PATH` **only on NixOS** (`[ -e /etc/NIXOS ]`), where it is both required (libstdc++ is off the default loader path) and ABI-safe (the system glibc *is* the Nix glibc). On an FHS host the system loader resolves `libstdc++` and nothing is exported — avoiding the `GLIBC_ABI_DT_X86_64_PLT not found` breakage an unconditional export caused in host `just`/bash recipes (whose `#!/usr/bin/env bash`, plus anything `/etc/ld.so.preload` forces `libstdc++` into, were dragged onto the Nix C++ runtime built against a newer glibc). The dev-shell parity tests are gated to NixOS and an FHS leak-guard (`test_devshell_no_nix_cxx_runtime_leak_on_fhs_host`) was added
- **pre-commit ruff/ruff-format/typos hooks now run on NixOS hosts (sourced from the flake)** ([#697](https://github.com/vig-os/devcontainer/issues/697))
  - The `ruff`, `ruff-format`, and `typos` hooks pulled compiled tools as generic-linux (manylinux) wheels from `astral-sh/ruff-pre-commit` and `crate-ci/typos`; a NixOS host cannot execute those binaries out of the box (no FHS `ld-linux`), forcing `--no-verify` on every local commit
  - Added `ruff` and `typos` to the flake `devTools` SSoT and converted the three hooks to `repo: local` / `language: system` (`ruff check --fix`, `ruff format`, `typos`), so they resolve their tool from the Nix dev-shell like the other local hooks — no host setup needed inside the dev-shell. Hook versions now track `nixpkgs`/`flake.lock` (Renovate `nix` manager) instead of upstream `rev:` pins, consistent with the #625 toolchain consolidation. The scaffolded `assets/workspace/.pre-commit-config.yaml` is `language: system` as well (no upstream-hook decoupling — downstream workspaces resolve the tools from the toolchain baked into the image)
  - Removed `ruff` from the project's uv dependency groups (`pyproject.toml`/`uv.lock`) and repointed `just lint`/`just format` to the flake `ruff` (dropping `uv run`). Otherwise the venv's `ruff` (a manylinux wheel) shadowed the flake `ruff` under `uv run` — which is how the `.githooks/pre-commit` hook and the `just` recipes invoke it — so `ruff` stayed broken on NixOS; the flake is now the single `ruff` source (its `[tool.ruff]` config is unchanged)
  - Declared `PATH` in the devcontainer image's OCI `config.Env`. `buildLayeredImage` symlinks the toolchain into `/bin` but set no PATH; `podman run` injects a default (so it worked), but `docker-compose` / `devcontainer exec` inherit `config.Env` verbatim, leaving the baked toolchain off PATH so `language: system` hooks could not resolve. Added an image test asserting the OCI config declares a PATH containing `/bin`
- **BATS suite no longer fails locally on the Nix toolchain (helper libraries unresolved)** ([#695](https://github.com/vig-os/devcontainer/issues/695))

_Changelog truncated to fit GitHub's 65000-char PR-body limit; see `CHANGELOG.md` on `release/0.4.0` for the full entry._


---
---

## Commits

### Commit 1: [655af38](https://github.com/vig-os/devkit/commit/655af38ed2531be1d66570db0a50a7cbb4b9c0e6) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 03:07 PM
test(setup): align install.sh bats shebang assertion with portable shebang, 2 files modified (tests/bats/install.bats)

### Commit 2: [d99765c](https://github.com/vig-os/devkit/commit/d99765c6d2d07aa589e9b765d91fe048e4a316e7) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 03:33 PM
fix(ci): make install_workspace a staticmethod fixture, 7 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_install_script.py)

### Commit 3: [8cd1d55](https://github.com/vig-os/devkit/commit/8cd1d55b9b1af4baedf30540cdbf5b583887d0ea) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 03:33 PM
fix(setup): use portable env-bash shebang in git hooks, 6 files modified (.githooks/commit-msg, .githooks/pre-commit, .githooks/prepare-commit-msg)

### Commit 4: [95ab585](https://github.com/vig-os/devkit/commit/95ab585c8a912848bc71ca185a686e83931f068a) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 03:35 PM
test(setup): raise init dep-sync timeout and make it configurable, 11 files modified (tests/conftest.py)

### Commit 5: [388182f](https://github.com/vig-os/devkit/commit/388182fa67f2d70e802fab3057401c2161dc49d0) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 03:43 PM
fix(ci): make install_workspace a staticmethod fixture (#691) (#693), 7 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_install_script.py)

### Commit 6: [2b09e08](https://github.com/vig-os/devkit/commit/2b09e0815ad7f4585bca52544de3fd224cec05d7) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 03:44 PM
Merge branch 'feature/625-nix-claude-migration' into bugfix/692-init-fixture-sync-timeout, 7 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_install_script.py)

### Commit 7: [23aae6d](https://github.com/vig-os/devkit/commit/23aae6daeedb9120bcc69cc8b115892382135d19) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 03:45 PM
fix(setup): unblock testinfra init fixture (dep-sync timeout + NixOS git-hook shebangs) (#694), 17 files modified (.githooks/commit-msg, .githooks/pre-commit, .githooks/prepare-commit-msg, tests/conftest.py)

### Commit 8: [4c30fa8](https://github.com/vig-os/devkit/commit/4c30fa8b93432dab9dde05b561c38ce58436b7fa) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 03:52 PM
docs(setup): document direnv shell hook prerequisite in CONTRIBUTE, 24 files modified (CHANGELOG.md, CONTRIBUTE.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/templates/CONTRIBUTE.md.j2)

### Commit 9: [9975662](https://github.com/vig-os/devkit/commit/99756625dbea572c50732709bd6ef0d1c2812482) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:02 PM
test(nix): assert flake provides bats with resolvable helper libs, 32 files modified (tests/test_flake_devshell.py)

### Commit 10: [36ee161](https://github.com/vig-os/devkit/commit/36ee161814aaa0838686494d20ac22b4ff7a22a3) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:06 PM
build(nix): provide bats + helper libraries from the flake, 25 files modified (flake.nix)

### Commit 11: [c0642cd](https://github.com/vig-os/devkit/commit/c0642cd2c2c0b4af897497c915f155d75b3ba258) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:11 PM
build(nix): resolve bats via flake BATS_LIB_PATH; drop npm bats deps, 104 files modified (.github/actions/setup-env/action.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, justfile, package-lock.json, package.json, tests/bats/test_helper.bash)

### Commit 12: [addb632](https://github.com/vig-os/devkit/commit/addb632a1352d537031e737507a6da2513868fd2) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:13 PM
build(nix): resolve BATS helper libraries from the flake (#696), 161 files modified

### Commit 13: [501d988](https://github.com/vig-os/devkit/commit/501d988b79fc9a3485b55e8b1f58bab975972722) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:37 PM
test(nix): assert ruff and typos are in the devTools SSoT, 19 files modified (tests/test_flake_devshell.py)

### Commit 14: [943a834](https://github.com/vig-os/devkit/commit/943a83418bb90e3523184c7eafb51e81a7af0e26) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:40 PM
test(nix): assert dev-shell LD_LIBRARY_PATH carries libstdc++ for pymarkdown, 63 files modified (tests/test_flake_devshell.py)

### Commit 15: [e588e4f](https://github.com/vig-os/devkit/commit/e588e4f3eeea2139934be18850890d1e2e2a1a28) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:42 PM
build(nix): source ruff/ruff-format/typos pre-commit hooks from the flake, 53 files modified (.pre-commit-config.yaml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.pre-commit-config.yaml, flake.nix)

### Commit 16: [c59f932](https://github.com/vig-os/devkit/commit/c59f9329bd332221455604250cd0a4f28e5c61fc) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:43 PM
build(nix): expose libstdc++ in dev-shell so pymarkdown runs on NixOS, 51 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, flake.nix)

### Commit 17: [134119f](https://github.com/vig-os/devkit/commit/134119f8d1e7a80677f1033a03c124f4073ac439) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:54 PM
build(nix): source ruff from the flake, not the project venv, 39 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, justfile, pyproject.toml, uv.lock)

### Commit 18: [f30f4f9](https://github.com/vig-os/devkit/commit/f30f4f925c106d7baef72c6522a85f270cee879d) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:57 PM
build(nix): source ruff/ruff-format/typos pre-commit hooks from the flake (#699), 111 files modified

### Commit 19: [2a03092](https://github.com/vig-os/devkit/commit/2a030921dadda1e37dd321a2955612f6a3f5ca00) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 04:59 PM
Merge remote-tracking branch 'origin/feature/625-nix-claude-migration' into bugfix/698-pymarkdown-nixos, 111 files modified

### Commit 20: [77a8f4a](https://github.com/vig-os/devkit/commit/77a8f4a30b63b98f06b06ee29a98cc1305353794) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 05:00 PM
build(nix): expose libstdc++ in the dev-shell so pymarkdown runs on NixOS (#700), 114 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, flake.nix, tests/test_flake_devshell.py)

### Commit 21: [1236458](https://github.com/vig-os/devkit/commit/12364587778fff3edaac2c2c09c382e44dc073a5) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 05:07 PM
style: taplo-format pyproject lint group after ruff removal, 6 files modified (pyproject.toml)

### Commit 22: [6ec3a51](https://github.com/vig-os/devkit/commit/6ec3a512a1b2997493e452723afa79b542d647ba) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 05:38 PM
fix(image): declare PATH in the devcontainer OCI config, 47 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 23: [fabf6d1](https://github.com/vig-os/devkit/commit/fabf6d14abe4501fe27461ff1e7e8b6d4be96413) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 06:18 PM
fix(setup): keep scaffolded ruff/typos as self-contained pre-commit hooks, 167 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.pre-commit-config.yaml, scripts/manifest.toml, scripts/sync_manifest.py, scripts/transforms.py, tests/test_transforms.py)

### Commit 24: [58f198e](https://github.com/vig-os/devkit/commit/58f198ede2ce6fcea0785a07e83c371b2d0035d8) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 06:42 PM
test(integration): assert devcontainer runs the image under test, 42 files modified (tests/test_integration.py)

### Commit 25: [baa4637](https://github.com/vig-os/devkit/commit/baa4637ae66b05c4a2c5e1a4d231456481a4fbfc) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 08:36 AM
fix(testing): run the integration devcontainer from the image under test, 17 files modified (tests/conftest.py)

### Commit 26: [e4d4e5e](https://github.com/vig-os/devkit/commit/e4d4e5ec3adde492f8add15074c2b5786f9ef07a) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 08:36 AM
fix(setup): bring the Nix image up cleanly under devcontainer up, 15 files modified (assets/workspace/.devcontainer/scripts/post-create.sh)

### Commit 27: [5824bdf](https://github.com/vig-os/devkit/commit/5824bdf6efef5f4809e40a2542eb3feeb745f820) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 08:37 AM
fix(setup): scaffold ruff/ruff-format/typos as language:system hooks, 168 files modified (assets/workspace/.pre-commit-config.yaml, scripts/manifest.toml, scripts/sync_manifest.py, scripts/transforms.py, tests/test_transforms.py)

### Commit 28: [f4fff13](https://github.com/vig-os/devkit/commit/f4fff136bc36dcaec4e98a3cbecc451848359918) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 08:37 AM
docs(testing): document the integration image-selection behaviour, 27 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/README.md)

### Commit 29: [3b1b1e0](https://github.com/vig-os/devkit/commit/3b1b1e0ec4c05dec0f5302c6a2b6c70a5c0a8797) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 09:09 AM
fix(testing): run integration tests against the freshly-built image (#702), 269 files modified

### Commit 30: [a818fb7](https://github.com/vig-os/devkit/commit/a818fb78494e1513175b70f6bb8c67d94a17f0a8) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 10:13 AM
test(setup): gate dev-shell libstdc++ tests to NixOS, add FHS leak guard, 66 files modified (tests/test_flake_devshell.py)

### Commit 31: [e27962a](https://github.com/vig-os/devkit/commit/e27962a2c6f81df68a0bbc3e56e31f234123916a) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 10:27 AM
fix(setup): gate dev-shell C++ runtime LD_LIBRARY_PATH injection to NixOS, 29 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix)

### Commit 32: [25554b4](https://github.com/vig-os/devkit/commit/25554b4efe0f5d6b8506c05c95260de6ad74a204) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 10:44 AM
fix(setup): gate dev-shell C++ runtime LD_LIBRARY_PATH injection to NixOS (#704), 95 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_flake_devshell.py)

### Commit 33: [2172bc7](https://github.com/vig-os/devkit/commit/2172bc7e4acc61db258c9fb465979742b873ec0f) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:24 PM
build(image): tag the discovery image nix-dev to match CI, 5 files modified (flake.nix)

### Commit 34: [c3c17bb](https://github.com/vig-os/devkit/commit/c3c17bb38e4ab15b447b8209fbaf0db03506d148) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:24 PM
style: fix the duplicated word in the just clean recipe, 2 files modified (justfile)

### Commit 35: [fdd1a0c](https://github.com/vig-os/devkit/commit/fdd1a0cfe3775fb9df17d65dfb1939de1f718f88) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:24 PM
docs: correct the Python version to 3.14 in the TESTING template, 4 files modified (TESTING.md, docs/templates/TESTING.md.j2)

### Commit 36: [3652cda](https://github.com/vig-os/devkit/commit/3652cda3aaf2e66a28495a66b514d58005e4586c) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:24 PM
fix(setup): drop the duplicate sync-manifest pre-commit hook, 10 files modified (.pre-commit-config.yaml)

### Commit 37: [37ce798](https://github.com/vig-os/devkit/commit/37ce7989d4ed16926ade76adc54906e029f8fb4c) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:24 PM
test(setup): assert workspace interpreter uses the workspace venv, 34 files modified (tests/test_transforms.py)

### Commit 38: [fcd99e8](https://github.com/vig-os/devkit/commit/fcd99e88cfe1854d01a271983ac36c9e3d5e34fd) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:25 PM
fix(setup): point the workspace python interpreter at the workspace venv, 5 files modified (assets/workspace/.vscode/settings.json, scripts/manifest.toml)

### Commit 39: [c0b5f5d](https://github.com/vig-os/devkit/commit/c0b5f5dd627baf235e8545fd4237b86e051d1205) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:26 PM
docs(setup): note the workspace interpreter path fix in the changelog, 6 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 40: [606d114](https://github.com/vig-os/devkit/commit/606d114c17a1054aabed3d3d436f1869b9b2266a) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:40 PM
style(setup): ruff-format the workspace interpreter test, 4 files modified (tests/test_transforms.py)

### Commit 41: [ad1a5d5](https://github.com/vig-os/devkit/commit/ad1a5d5d9f07aed87c28e655134fec110cca0b01) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:23 PM
chore(ci): set ruff target-version to py314, 10 files modified (packages/vig-utils/tests/test_claude_ssot.py, pyproject.toml, tests/conftest.py)

### Commit 42: [25b8620](https://github.com/vig-os/devkit/commit/25b86200772378bad7e6744ca70d5969eac0f04d) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:53 PM
build(image): tag the discovery image nix-dev to match CI (#713), 5 files modified (flake.nix)

### Commit 43: [78a973d](https://github.com/vig-os/devkit/commit/78a973d3bfd280fac9f88eeabd430b398876563e) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:53 PM
style: fix the duplicated word in the just clean recipe (#714), 2 files modified (justfile)

### Commit 44: [0423186](https://github.com/vig-os/devkit/commit/0423186d337a257aaa5cad06b0df35b0bcb38450) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:53 PM
docs: correct the Python version to 3.14 in the TESTING template (#715), 4 files modified (TESTING.md, docs/templates/TESTING.md.j2)

### Commit 45: [30f02ed](https://github.com/vig-os/devkit/commit/30f02ed49c7c5c0f8dc7478718f425c53ba57e29) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:53 PM
fix(setup): drop the duplicate sync-manifest pre-commit hook (#712), 10 files modified (.pre-commit-config.yaml)

### Commit 46: [8975eca](https://github.com/vig-os/devkit/commit/8975ecaa3e85ff70a0f5ac94c0d684c49f7f9bd6) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:53 PM
chore(ci): set ruff target-version to py314 (#711), 10 files modified (packages/vig-utils/tests/test_claude_ssot.py, pyproject.toml, tests/conftest.py)

### Commit 47: [5eb64b1](https://github.com/vig-os/devkit/commit/5eb64b176dbc1d7958309731794fc47e172fd960) by [c-vigo](https://github.com/c-vigo) on June 25, 2026 at 12:53 PM
fix(setup): point the workspace python interpreter at the workspace venv (#716), 43 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.vscode/settings.json, scripts/manifest.toml, tests/test_transforms.py)

### Commit 48: [f0d0fd8](https://github.com/vig-os/devkit/commit/f0d0fd8b008def28224f64a7b0163840a0c28910) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on June 26, 2026 at 06:18 AM
chore: sync issues and PRs, 3662 files modified

### Commit 49: [f0b0192](https://github.com/vig-os/devkit/commit/f0b019254e714cb35fbcab22df50d2a13f644a32) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 07:45 AM
test(worktree): guard agent entrypoints against stale .cursor paths, 47 files modified (packages/vig-utils/tests/test_claude_ssot.py, tests/bats/worktree-claude-cli.bats)

### Commit 50: [2b54457](https://github.com/vig-os/devkit/commit/2b54457beaafa34fa569704ed747dfcd003d9602) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 07:48 AM
fix(worktree): read agent config from the .claude SSoT, not .cursor, 36 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.devcontainer/justfile.worktree, justfile.worktree, scripts/manifest.toml)

### Commit 51: [86ff1e3](https://github.com/vig-os/devkit/commit/86ff1e3fef34c8b8a701280028d67d5f18193926) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 07:50 AM
fix(worktree): derive branch summaries through the claude CLI, 37 files modified (.claude/agent-models.toml, CHANGELOG.md, assets/workspace/.claude/agent-models.toml, assets/workspace/.devcontainer/CHANGELOG.md, packages/vig-utils/src/vig_utils/shell/derive-branch-summary.sh)

### Commit 52: [39ee444](https://github.com/vig-os/devkit/commit/39ee44495f0e356a4fb48ef43aba6f5ee81f7a91) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 07:53 AM
test(workspace): gate the downstream flake stub in CI, 90 files modified (.github/workflows/ci.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_downstream_flake.py)

### Commit 53: [5b6d208](https://github.com/vig-os/devkit/commit/5b6d2082e7b16d2684f848d92a501fae96ecd4b4) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 08:01 AM
fix(init): make scaffolded .devcontainer justfile imports optional, 12 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/justfile)

### Commit 54: [00e7353](https://github.com/vig-os/devkit/commit/00e7353a685d020ce847a9261893f53b271c1b7b) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 08:03 AM
refactor(init): allow TEMPLATE_DIR/WORKSPACE_DIR override, 6 files modified (assets/init-workspace.sh)

### Commit 55: [2786c1c](https://github.com/vig-os/devkit/commit/2786c1c74e96c2381b3d60b3f016a015928eca6c) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 08:03 AM
test(init): scaffold the --mode picker end-to-end, 81 files modified (tests/bats/init-workspace.bats)

### Commit 56: [85a0a46](https://github.com/vig-os/devkit/commit/85a0a46e192318f4c34e5f9b840d699db9629c46) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 08:06 AM
docs(security): describe the Nix image posture in SECURITY.md, 71 files modified (CHANGELOG.md, SECURITY.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 57: [d6eb42e](https://github.com/vig-os/devkit/commit/d6eb42efcaa8a0b04dd0873ed78f82e88197e3cb) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 08:06 AM
chore: drop CODEOWNERS entry for the retired requirements.yaml, 1 file modified (.github/CODEOWNERS)

### Commit 58: [4c8785c](https://github.com/vig-os/devkit/commit/4c8785c335094f92392a37d2daa82c132084a060) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 08:08 AM
refactor(worktree): drop stale Cursor wording from comments, 14 files modified (assets/workspace/.devcontainer/justfile.worktree, assets/workspace/.devcontainer/scripts/verify-auth.sh, justfile.worktree)

### Commit 59: [f1ebd86](https://github.com/vig-os/devkit/commit/f1ebd86d861bb91381bb8b2e39ba3a89fd727723) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 08:26 AM
style(claude): taplo-format agent-models.toml comment spacing, 12 files modified (.claude/agent-models.toml, assets/workspace/.claude/agent-models.toml)

### Commit 60: [0e09dcb](https://github.com/vig-os/devkit/commit/0e09dcb5fbb5d9cd90ec9a963ca83b0e2530d7ef) by [c-vigo](https://github.com/c-vigo) on June 26, 2026 at 08:27 AM
test(init): expect optional .devcontainer justfile import, 8 files modified (tests/test_integration.py)

### Commit 61: [cb9e813](https://github.com/vig-os/devkit/commit/cb9e813c4222b2f259a293d0b578c03ae0c89152) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on June 27, 2026 at 05:59 AM
chore: sync issues and PRs, 157 files modified (docs/issues/issue-529.md, docs/issues/issue-717.md, docs/issues/issue-718.md, docs/issues/issue-719.md, docs/issues/issue-720.md)

### Commit 62: [f566fff](https://github.com/vig-os/devkit/commit/f566fff6b20c6aa85274dd47838151d1d2a75ba0) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 07:17 AM
fix(devshell): isolate neovim from host ~/.config/nvim via NVIM_APPNAME, 15 files modified (flake.nix)

### Commit 63: [05a7e46](https://github.com/vig-os/devkit/commit/05a7e46c20b5f9e8e15d5983972ae0b4393790ac) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 07:21 AM
fix(devshell): isolate neovim from host ~/.config/nvim via NVIM_APPNAME (#724), 15 files modified (flake.nix)

### Commit 64: [e365c2b](https://github.com/vig-os/devkit/commit/e365c2bde50a1d582187ea3570f666dc70b857a0) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:15 AM
test(image): assert npm global install lands on PATH, 49 files modified (tests/test_image.py)

### Commit 65: [cf6fadb](https://github.com/vig-os/devkit/commit/cf6fadb0b6b5ecf239d507b0fec0896256e3eca2) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:15 AM
test(image): assert /usr/bin/env exists and shebang runs, 40 files modified (tests/test_image.py)

### Commit 66: [9c2d77a](https://github.com/vig-os/devkit/commit/9c2d77acddaf5032dc654a92a8f690dddaa91777) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:17 AM
test(devshell): assert python3 and pre-commit on dev-shell PATH, 45 files modified (tests/test_flake_devshell.py)

### Commit 67: [f5679cd](https://github.com/vig-os/devkit/commit/f5679cd40832a7c66b56260c9092cea0ab069355) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:18 AM
fix(image): add /usr/bin/env to the Nix-built image, 10 files modified (CHANGELOG.md, flake.nix)

### Commit 68: [507cfdc](https://github.com/vig-os/devkit/commit/507cfdc5ff48ac76818d2eb8176f559b72e66e43) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:18 AM
fix(devshell): expose python3 and pre-commit on dev-shell PATH, 24 files modified (flake.nix)

### Commit 69: [af86316](https://github.com/vig-os/devkit/commit/af863169a64bbc154f84bfddef8d22b6a6c5b484) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:19 AM
docs(changelog): note dev-shell python3 + pre-commit parity fix, 2 files modified (CHANGELOG.md)

### Commit 70: [c984c29](https://github.com/vig-os/devkit/commit/c984c29414f466af011d8a2bebaa1bf0628e746e) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:21 AM
fix(image): bake writable on-PATH npm global prefix, 27 files modified (CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 71: [ef1309d](https://github.com/vig-os/devkit/commit/ef1309ded64257da07ed1d5044aeb6a0033616df) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:35 AM
fix(image): add /usr/bin/env to the Nix-built image (#730), 50 files modified (CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 72: [4a596cb](https://github.com/vig-os/devkit/commit/4a596cb7640d74d8df6d1746e83cbb518882924d) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:41 AM
chore(changelog): merge feature branch, resolve CHANGELOG conflict, 50 files modified (CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 73: [31c77a5](https://github.com/vig-os/devkit/commit/31c77a5d95e5e48fbfa142ee641b8e496f786595) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:42 AM
fix(image): bake writable on-PATH npm global prefix (#734), 68 files modified (CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 74: [1e3d2fe](https://github.com/vig-os/devkit/commit/1e3d2fe44dfe62980c9db88c51285e712e855e9c) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:43 AM
chore(changelog): merge feature branch, resolve CHANGELOG conflict, 118 files modified (CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 75: [a7bda4e](https://github.com/vig-os/devkit/commit/a7bda4eece741aa23c265d2397945b59c8112410) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 08:43 AM
fix(devshell): expose python3 and pre-commit on dev-shell PATH (#729) (#733), 71 files modified (CHANGELOG.md, flake.nix, tests/test_flake_devshell.py)

### Commit 76: [63d6b33](https://github.com/vig-os/devkit/commit/63d6b33fb79934d512e71cbf9b1aecf6e7f8c661) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 09:10 AM
chore(changelog): re-sync workspace CHANGELOG mirror, 6 files modified (assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 77: [8f778f5](https://github.com/vig-os/devkit/commit/8f778f5c3ab39d34ab2c879389531198f23413ea) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 09:11 AM
revert(devshell): drop python3/pre-commit from dev-shell PATH, 73 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_flake_devshell.py)

### Commit 78: [c443ae4](https://github.com/vig-os/devkit/commit/c443ae47e3707c230c3a551dc51533235a3b4c85) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:35 AM
test(init): assert direnv --force preserves populated consumer files, 34 files modified (tests/bats/init-workspace.bats)

### Commit 79: [2c4e2cd](https://github.com/vig-os/devkit/commit/2c4e2cda92f6e2b570da2a7bacbc1ed4536fc664) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:38 AM
fix(workspace): stop direnv --force from clobbering populated repos, 39 files modified (assets/init-workspace.sh, install.sh)

### Commit 80: [e69e757](https://github.com/vig-os/devkit/commit/e69e75756aae810b1c75a82fc05d24ba01ec0412) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:38 AM
docs(changelog): record direnv --force populated-repo guard, 6 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 81: [0aa5125](https://github.com/vig-os/devkit/commit/0aa5125794883122d0db7cdf28dbe186890b88fd) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:39 AM
feat(image): bake /etc/nix/nix.conf enabling nix-command and flakes, 67 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 82: [936e44a](https://github.com/vig-os/devkit/commit/936e44af3df3a997ccad9444d8113188b53e65bc) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:43 AM
test(image): assert docker shim is on PATH and runs podman, 30 files modified (tests/test_image.py)

### Commit 83: [fcbe02f](https://github.com/vig-os/devkit/commit/fcbe02f661ede7c9a302f2c1a52a1d301f71a082) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:43 AM
fix(image): add docker -> podman compatibility shim, 20 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix)

### Commit 84: [792c6fe](https://github.com/vig-os/devkit/commit/792c6fed7ddf89b66832c707d9ff864e9a537cda) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:43 AM
test(image): cover FHS loader and manylinux wheel import, 79 files modified (tests/test_image.py)

### Commit 85: [b882d35](https://github.com/vig-os/devkit/commit/b882d35fb183bf1df71a75570f11fe7b3bfdee12) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:44 AM
fix(workspace): stop direnv --force from clobbering populated consumer repos (#741), 79 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, install.sh, tests/bats/init-workspace.bats)

### Commit 86: [586b50e](https://github.com/vig-os/devkit/commit/586b50e89f658e75528a5a845ee2c694d3a37f53) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:47 AM
fix(image): provide FHS loader and lib path for manylinux wheels, 42 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix)

### Commit 87: [c2c8329](https://github.com/vig-os/devkit/commit/c2c8329c1f8d46e630f4cab3a1182c68cff67679) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:51 AM
fix(image): bake template .venv into Nix image, 38 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 88: [fbdeb47](https://github.com/vig-os/devkit/commit/fbdeb47eb57698d8879797625f9768e25a0f08d3) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 10:51 AM
chore(changelog): merge epic into #740 branch, resolve CHANGELOG conflict, 79 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, install.sh, tests/bats/init-workspace.bats)

### Commit 89: [dc7e282](https://github.com/vig-os/devkit/commit/dc7e2823a77c4805c5e691c90bf298422f3614e4) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 11:00 AM
fix(image): add docker -> podman compatibility shim (#743), 50 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 90: [354a888](https://github.com/vig-os/devkit/commit/354a888d4e1a8606168dce98785d33505e1d0873) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 11:00 AM
chore(changelog): merge epic into #739 branch, 129 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, install.sh, tests/bats/init-workspace.bats, tests/test_image.py)

### Commit 91: [d3e487e](https://github.com/vig-os/devkit/commit/d3e487e0f9b526bcc6eb5869fe09f43f784f0ef4) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 11:09 AM
feat(image): bake /etc/nix/nix.conf enabling nix-command and flakes (#742), 67 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 92: [6365c89](https://github.com/vig-os/devkit/commit/6365c89a8e0e6680ef32d6bb02dd62e506bfdae1) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 11:10 AM
chore(changelog): merge epic into #736 branch, resolve flake/CHANGELOG conflict, 195 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, install.sh, tests/bats/init-workspace.bats, tests/test_image.py)

### Commit 93: [e934e0d](https://github.com/vig-os/devkit/commit/e934e0d48c056e98f85ae9667c9aa1d10d12d144) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 11:21 AM
fix(image): provide FHS loader + lib path for manylinux wheels (#744), 120 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 94: [c1c5819](https://github.com/vig-os/devkit/commit/c1c58193925eb5cb4d0af45eb3f06184205ba220) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 11:21 AM
chore(changelog): merge epic into #735 branch, resolve CHANGELOG conflict, 237 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 95: [cff9e69](https://github.com/vig-os/devkit/commit/cff9e691ebea7ac8ee08e8e7b23f92aada61d071) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 11:35 AM
fix(image): bake template .venv into Nix image (#745), 38 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 96: [3c3c93d](https://github.com/vig-os/devkit/commit/3c3c93d1d36b88e41c90e4c32988d3d3d2534d6b) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 11:59 AM
fix(image): make FHS loader symlink architecture-aware, 63 files modified (flake.nix, tests/test_image.py)

### Commit 97: [5ff7b0c](https://github.com/vig-os/devkit/commit/5ff7b0c9b17f9c4193863a5fe8dcfe5e15943e65) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 11:59 AM
fix(image): normalize baked .venv prompt so post-create can rename it, 8 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 98: [9605589](https://github.com/vig-os/devkit/commit/9605589c1d8c48b2ede300155094febd0bde7d0c) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 12:16 PM
fix(image): arm64-aware FHS loader + renameable baked .venv prompt (#746), 71 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 99: [793c2fb](https://github.com/vig-os/devkit/commit/793c2fbd5977f43057e774d9b5b63bba0982f082) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 12:19 PM
style(flake): nixfmt flake.nix after merge/fixup edits, 5 files modified (flake.nix)

### Commit 100: [a2f0e54](https://github.com/vig-os/devkit/commit/a2f0e547970d1284352f5f34b9b186880e4faed2) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 01:06 PM
fix(devshell): expose python3 + pre-commit on dev-shell PATH (CI-safe), 94 files modified (.github/actions/setup-env/action.yml, .github/workflows/ci.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_flake_devshell.py)

### Commit 101: [7d9aadd](https://github.com/vig-os/devkit/commit/7d9aadd0c2b27b6bba2bd1bd4762192bb54ff66e) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 01:10 PM
fix(devshell): expose python3 + pre-commit on dev-shell PATH (CI-safe) (#747), 94 files modified (.github/actions/setup-env/action.yml, .github/workflows/ci.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_flake_devshell.py)

### Commit 102: [797e1a2](https://github.com/vig-os/devkit/commit/797e1a22179d77ddf5045368bb54291e18242232) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 01:17 PM
chore(deps): apply applicable Renovate action/dep updates, 30 files modified

### Commit 103: [baf2520](https://github.com/vig-os/devkit/commit/baf2520e87a0b57c87acfda6ace92f2e8424622b) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 01:39 PM
chore(deps): apply applicable Renovate action/dep updates (#748), 30 files modified

### Commit 104: [046ba4f](https://github.com/vig-os/devkit/commit/046ba4fbdac5559421f00abedf17baeff5af23ae) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 02:02 PM
fix(image): set empty build-users-group in baked nix.conf, 38 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 105: [30a20d2](https://github.com/vig-os/devkit/commit/30a20d278545437a0a49ffd5ba532dd01a396a72) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 02:14 PM
fix(image): set empty build-users-group in baked nix.conf (#750), 38 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 106: [872efae](https://github.com/vig-os/devkit/commit/872efae86148a46d54617fb25f29a621433ab0d4) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 02:38 PM
docs(migration): add MIGRATION.md and ship it in the image, 124 files modified (docs/MIGRATION.md, flake.nix, tests/test_image.py)

### Commit 107: [923c8ae](https://github.com/vig-os/devkit/commit/923c8aefa44960b0ecb4dc321a58b4e204c49307) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 02:41 PM
docs(changelog): consolidate Unreleased Nix-migration entries to the end state, 86 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 108: [7eddbf1](https://github.com/vig-os/devkit/commit/7eddbf1c24a2f5b8ccfa8d5def749ea895efb501) by [c-vigo](https://github.com/c-vigo) on June 29, 2026 at 02:54 PM
docs: consolidate the migration CHANGELOG + add/ship MIGRATION.md (#751), 210 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md, flake.nix, tests/test_image.py)

### Commit 109: [110e113](https://github.com/vig-os/devkit/commit/110e11366d4aa72d5abe27730577327ad64ac082) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:19 AM
test(vig-utils): assert unscored CVEs block the vulnix gate, 31 files modified (packages/vig-utils/tests/test_vulnix_gate.py)

### Commit 110: [862fd9d](https://github.com/vig-os/devkit/commit/862fd9d285b735556ced21a9825c7e8aaf85e273) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:20 AM
fix(vig-utils): fail loud on unscored CVEs in vulnix gate, 38 files modified (packages/vig-utils/src/vig_utils/vulnix_gate.py)

### Commit 111: [2c55390](https://github.com/vig-os/devkit/commit/2c5539018bab85ef78fc791df9e6a41d75f4dc2a) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:22 AM
ci(security-scan): fail the job on vulnix scanner crashes, 23 files modified (.github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 112: [8c76ec4](https://github.com/vig-os/devkit/commit/8c76ec4d2c2c9aa653bd6c536eb60e01d574cee5) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:17 AM
fix(release): retag the loaded Nix image tag in publish, 11 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 113: [e39d76f](https://github.com/vig-os/devkit/commit/e39d76f416821d67db2b56a196cad76afdd82687) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:32 AM
test(image): add parametrized in-container devShellTools parity gate, 93 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_image.py)

### Commit 114: [7087347](https://github.com/vig-os/devkit/commit/708734734d58d42758b2e8277794a7e67218a42f) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:39 AM
fix(vig-utils): fail loud on unscored CVEs in vulnix gate (#765), 92 files modified (.github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, packages/vig-utils/src/vig_utils/vulnix_gate.py, packages/vig-utils/tests/test_vulnix_gate.py)

### Commit 115: [f3f22e3](https://github.com/vig-os/devkit/commit/f3f22e30777224d4177c15e8c04c414be5b377ca) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:39 AM
fix(release): retag the loaded Nix image tag in publish (#763), 11 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 116: [847a604](https://github.com/vig-os/devkit/commit/847a604e9cf9823bd9a59deaeeceb7e04fb573be) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:19 AM
test: assert install.sh uses docker-compatible image inspect for --skip-pull, 12 files modified (tests/bats/install.bats)

### Commit 117: [65d183c](https://github.com/vig-os/devkit/commit/65d183c9d81a5aa668533d48811121ac6309e415) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:20 AM
fix(install): use docker-compatible image inspect for --skip-pull, 2 files modified (install.sh)

### Commit 118: [f878a05](https://github.com/vig-os/devkit/commit/f878a05d9419789b9be7a42773879996b7a45d0a) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:20 AM
fix(ci): run safety dependency scan via uv run, 8 files modified (.github/workflows/ci.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 119: [685c7b8](https://github.com/vig-os/devkit/commit/685c7b86427cc8f68efe7127c5c57f22d99fab42) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:46 AM
test(image): parametrized in-container .#devShellTools parity gate (#766), 93 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_image.py)

### Commit 120: [b43883b](https://github.com/vig-os/devkit/commit/b43883bbb50a4cb334ebd800c3a01cf3a8e8b247) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 09:46 AM
fix: skip-pull image check under docker and run safety via uv (#764), 22 files modified (.github/workflows/ci.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, install.sh, tests/bats/install.bats)

### Commit 121: [e0b49f2](https://github.com/vig-os/devkit/commit/e0b49f290064cd8e128d485f04abd6c72958fc4e) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 11:15 AM
ci: restore arm64 image-testing coverage post-merge, 10 files modified (.github/workflows/nix-image.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 122: [330e868](https://github.com/vig-os/devkit/commit/330e868a6620fa646b65dfe8b22bd6c1068a8899) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 11:15 AM
test(image): offline skip-guard for network-dependent tests, 31 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_image.py)

### Commit 123: [4ed1cde](https://github.com/vig-os/devkit/commit/4ed1cde1f253c65f3b4442e6b6e359705e110667) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 11:17 AM
ci(release): gate publish on a hard vulnix CVE check, 60 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 124: [69ec69e](https://github.com/vig-os/devkit/commit/69ec69e1a594f7343758de05d9c07b6785834d4d) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 11:19 AM
ci: restore arm64 image-testing coverage post-merge (#767), 10 files modified (.github/workflows/nix-image.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 125: [a9ccbc9](https://github.com/vig-os/devkit/commit/a9ccbc96d53c1dde310a3eebdf1f69773794322a) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 11:21 AM
test(image): offline skip-guard for network-dependent tests (#768), 31 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_image.py)

### Commit 126: [2eea6ce](https://github.com/vig-os/devkit/commit/2eea6ce438a52b4c93d09d1166bb2c880b831aab) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 11:22 AM
ci(release): gate publish on a hard vulnix CVE check (#769), 60 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 127: [9fa7574](https://github.com/vig-os/devkit/commit/9fa75746f1bdce145660712cfe16cf5290516648) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 11:50 AM
docs: resync security-gate docs to the blocking vulnix gate, 21 files modified (.github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/CONTAINER_SECURITY.md, docs/NIX.md)

### Commit 128: [81ece65](https://github.com/vig-os/devkit/commit/81ece659b972ac1b1dab8ed9a1565b7c105a49d8) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 11:54 AM
docs: resync security-gate docs to the blocking vulnix gate (#770), 21 files modified (.github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/CONTAINER_SECURITY.md, docs/NIX.md)

### Commit 129: [28e403b](https://github.com/vig-os/devkit/commit/28e403b5d47b6dd329388682e6207fc2e5e9589a) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:02 PM
chore(security): per-CVE rationale and shorter expiry for .vulnixignore, 100 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 130: [846cdf2](https://github.com/vig-os/devkit/commit/846cdf2754fd5184ec7a11f6b83f9e4fdb4ab545) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:07 PM
chore(security): per-CVE rationale and shorter expiry for .vulnixignore (#771), 100 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 131: [9fd6367](https://github.com/vig-os/devkit/commit/9fd636754c59835c880ba729d4ddb5d261987957) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:16 PM
test: expect %q-derived dry-run and scaffold-commit guard in install.sh, 37 files modified (tests/bats/install.bats)

### Commit 132: [1e7639a](https://github.com/vig-os/devkit/commit/1e7639ae49dcc3422718181f6bb1d2269dd3557e) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:17 PM
fix(install): derive dry-run command from CMD array and guard scaffold commit, 46 files modified (install.sh)

### Commit 133: [926f876](https://github.com/vig-os/devkit/commit/926f876ddb952c6be3d7f82c9bbbe3fb58317660) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:17 PM
test: sync install dry-run name-sanitization assertion to %q output, 4 files modified (tests/test_install_script.py)

### Commit 134: [1a0a2a1](https://github.com/vig-os/devkit/commit/1a0a2a196ab9fca4fb4573c817bf7c3ac6ba944a) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:17 PM
fix: correct version-check help typo, init message, and release cancel handling, 13 files modified (.github/workflows/release.yml, assets/workspace/.devcontainer/scripts/version-check.sh, scripts/init.sh)

### Commit 135: [29c2209](https://github.com/vig-os/devkit/commit/29c22099f2a43c07c4ce44a2d4b3653182eede10) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:17 PM
chore(typos): allowlist 'Nd' duration placeholder, 2 files modified (.typos.toml)

### Commit 136: [a1f2a65](https://github.com/vig-os/devkit/commit/a1f2a653ffa3e077bcbb6952abf1c296a37eaade) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:17 PM
docs(changelog): record the PR #670 review-nit batch, 12 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 137: [089fb43](https://github.com/vig-os/devkit/commit/089fb4390c0182cb82b343aacb122eee50ad7226) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:22 PM
fix: address PR #670 review nit batch (#772), 114 files modified

### Commit 138: [beaf78d](https://github.com/vig-os/devkit/commit/beaf78d705507d776d0c05cae13e4714cc5f3965) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 12:55 PM
test(image): assert nix.conf drops accept-flake-config for explicit caches, 64 files modified (tests/test_image.py)

### Commit 139: [6b91cdb](https://github.com/vig-os/devkit/commit/6b91cdbe8326b795dd7fe1088224735852785ea4) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 01:00 PM
fix(image): bake explicit substituters/keys instead of accept-flake-config, 35 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix)

### Commit 140: [ee9cfb7](https://github.com/vig-os/devkit/commit/ee9cfb7b0dd93cd6c4d36d6af39991a289f6242f) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 01:15 PM
fix(image): bake explicit substituters/keys instead of accept-flake-config (#782), 99 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 141: [1254652](https://github.com/vig-os/devkit/commit/125465242b1112fb899e5fbaacba86943fea4d2a) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 01:28 PM
chore(flake): batch cheap hardening and hygiene fixes, 112 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, renovate.json)

### Commit 142: [3401dcd](https://github.com/vig-os/devkit/commit/3401dcdd856f12bd8baaba79453b08a611ccf5d1) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 01:33 PM
chore(flake): cheap hardening/hygiene batch (darwin guard, single eval, uv URL, renovate, nixfmt glob) (#783), 112 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, renovate.json)

### Commit 143: [e664099](https://github.com/vig-os/devkit/commit/e664099c24f9396c03d0b3ee0f82f0f1c0a9b46e) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 01:43 PM
test(image): assert nix.conf tokens by exact membership, 11 files modified (tests/test_image.py)

### Commit 144: [bd0491a](https://github.com/vig-os/devkit/commit/bd0491a3bc297d6177df83e6a4dda8cfb74f5e85) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 01:59 PM
test(image): assert nix.conf tokens by exact membership (clear CodeQL url-substring) (#785), 11 files modified (tests/test_image.py)

### Commit 145: [676e799](https://github.com/vig-os/devkit/commit/676e799337077d5365696f5686155a788a539e50) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 03:34 PM
Merge branch 'dev' into feature/625-nix-claude-migration, 3811 files modified

### Commit 146: [ae50df2](https://github.com/vig-os/devkit/commit/ae50df284afe5067ad11409e19658cdcacabba27) by [c-vigo](https://github.com/c-vigo) on June 30, 2026 at 04:51 PM
feat(nix): migrate the devcontainer to a Nix toolchain + Claude-native setup (#625) (#670), 10324 files modified

### Commit 147: [5c0393c](https://github.com/vig-os/devkit/commit/5c0393cc2a2f2d892ae4d39fbf3cf33460553628) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:17 AM
test(cachix): assert image-closure push is blocking on trusted paths, 103 files modified (tests/test_workflow_cachix.py)

### Commit 148: [301448e](https://github.com/vig-os/devkit/commit/301448e383d3ca9db1744e9aacbfba6bb6c89036) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:18 AM
ci(cachix): push image closure to cache as a blocking step, 78 files modified (.github/actions/build-image/action.yml, .github/workflows/nix-image.yml, .github/workflows/release.yml)

### Commit 149: [62dfbaf](https://github.com/vig-os/devkit/commit/62dfbaf93a6d7a55c435a2cb70e9a22e849265a6) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:19 AM
test(flake): assert treefmt, deadnix/statix, install app, modules, 79 files modified (tests/test_flake_checks.py)

### Commit 150: [025363d](https://github.com/vig-os/devkit/commit/025363d7ebf7ac87f27a13845b10040b9cab50e0) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:19 AM
docs(security): record sops-nix/age + OIDC secrets-management pattern, 297 files modified

### Commit 151: [f7d60f3](https://github.com/vig-os/devkit/commit/f7d60f31e892cc7092e48bb1d956bfc421a0e6bc) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:20 AM
docs(rfc): record uv2nix/pyproject-nix evaluation and decision, 278 files modified (docs/rfcs/ADR-uv2nix-pyproject-nix.md)

### Commit 152: [c15464a](https://github.com/vig-os/devkit/commit/c15464a6336a12f7423dfad1cef6a0da42d31a48) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:20 AM
docs(cachix): document cache-backed published-image guarantee, 34 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/CONTAINER_SECURITY.md, docs/NIX.md)

### Commit 153: [cb28a18](https://github.com/vig-os/devkit/commit/cb28a1801721658df16aa9edcd09c96bcb5fe26f) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:27 AM
feat(flake): add treefmt, deadnix/statix, modules, and install app, 203 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, flake.lock, flake.nix)

### Commit 154: [a6717c9](https://github.com/vig-os/devkit/commit/a6717c9161a37e5c7850e4f496ef88f3481c769d) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:34 AM
ci(cachix): make image-closure Cachix push first-class and blocking (#789), 215 files modified

### Commit 155: [08b9342](https://github.com/vig-os/devkit/commit/08b93422209729d69b74b9519b95e6ab4fe9fa2e) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:36 AM
docs(rfc): record uv2nix/pyproject-nix evaluation and decision (#788), 278 files modified (docs/rfcs/ADR-uv2nix-pyproject-nix.md)

### Commit 156: [c87af1b](https://github.com/vig-os/devkit/commit/c87af1befef5bbbb9d14bf8928bbc8adec22fa9e) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:37 AM
docs(security): record sops-nix/age + OIDC secrets-management pattern (#787), 297 files modified

### Commit 157: [9a2b516](https://github.com/vig-os/devkit/commit/9a2b516e2485ffa5a1c19131ebde55e21087e583) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:38 AM
Merge branch 'dev' into feature/777-flake-polish, 790 files modified

### Commit 158: [c98a918](https://github.com/vig-os/devkit/commit/c98a9183c1bc3f8f254181fc4b68b3b50e1737c9) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 06:49 AM
feat(flake): treefmt-nix, deadnix/statix, modules, and `nix run .#install` (#777) (#790), 282 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, flake.lock, flake.nix, tests/test_flake_checks.py)

### Commit 159: [1e7d801](https://github.com/vig-os/devkit/commit/1e7d801f87874078260b4bf2594c30fb8c45aa47) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 07:19 AM
test: assert checks.pre-commit gate and prek hook runner, 51 files modified (tests/test_flake_checks.py, tests/test_flake_devshell.py)

### Commit 160: [d231a79](https://github.com/vig-os/devkit/commit/d231a7927f09575d88d908d762fad5c4d96d1358) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 07:27 AM
build: adopt git-hooks.nix + prek runner and expose checks.pre-commit, 245 files modified (flake.lock, flake.nix)

### Commit 161: [1bee6d1](https://github.com/vig-os/devkit/commit/1bee6d19bd27b15c7e4b50ca2c83746e0436170b) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 07:37 AM
refactor: invoke the prek runner across hooks, scripts, and scaffold, 60 files modified

### Commit 162: [37d6f16](https://github.com/vig-os/devkit/commit/37d6f16c48abadad54a5190dfad9e19aec93cc48) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 07:37 AM
test: assert prek runner and PREK_HOME in the image tests, 42 files modified (tests/test_image.py)

### Commit 163: [27e9e85](https://github.com/vig-os/devkit/commit/27e9e8513f920e8ab558ddc58a250be32cb526ef) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 07:37 AM
docs: document the prek + git-hooks.nix pre-commit model, 62 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md)

### Commit 164: [9d7299f](https://github.com/vig-os/devkit/commit/9d7299f64b50501e1e913a9d310e4cf5c852cd94) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 07:42 AM
ci: gate the prek dev-shell runner parity assertion, 13 files modified (.github/workflows/ci.yml)

### Commit 165: [f53029e](https://github.com/vig-os/devkit/commit/f53029e53697db3c6bcc4e609f9cceb406dc8c69) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 07:51 AM
test: use --help for the statix dev-shell parity check, 4 files modified (tests/test_flake_devshell.py)

### Commit 166: [d163d2d](https://github.com/vig-os/devkit/commit/d163d2ddcf948079f23b3295bd3478e615d85cac) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 08:04 AM
test: fix statix dev-shell parity check (--help, no top-level --version) (#792) (#793), 4 files modified (tests/test_flake_devshell.py)

### Commit 167: [071756a](https://github.com/vig-os/devkit/commit/071756a398cc6097f7ce2203d152b7084a7aed37) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 08:26 AM
test: run prek (not pre-commit) in in-container integration tests, 28 files modified (tests/test_integration.py)

### Commit 168: [34212ae](https://github.com/vig-os/devkit/commit/34212aeb2df077451b858d710f3462033a028489) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 08:27 AM
Merge branch 'dev' into feature/778-git-hooks-prek, 4 files modified (tests/test_flake_devshell.py)

### Commit 169: [bc57ab5](https://github.com/vig-os/devkit/commit/bc57ab5328e363d758c9142340d4ed40509aff69) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 08:30 AM
docs: record org nix dev-environment strategy ADR, 349 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, docs/rfcs/ADR-nix-devenv-strategy.md)

### Commit 170: [dc88780](https://github.com/vig-os/devkit/commit/dc887800756aca394e05cbde3699496e5b1f9614) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 08:43 AM
docs: record org nix dev-environment strategy ADR (#796), 349 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, docs/rfcs/ADR-nix-devenv-strategy.md)

### Commit 171: [b1af891](https://github.com/vig-os/devkit/commit/b1af891ea2a3b5720fb7170efeb4be12137ec7a2) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 08:50 AM
ci: gate flake checks as CI Tier 0 via nix-fast-build, 184 files modified (.github/workflows/ci.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/rfcs/ADR-flake-checks-tier0.md, flake.nix, tests/test_flake_checks.py)

### Commit 172: [bc61cc0](https://github.com/vig-os/devkit/commit/bc61cc0ad6f956c03969407358d42c07f50d5237) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 08:55 AM
Merge branch 'dev' into feature/778-git-hooks-prek, 349 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, docs/rfcs/ADR-nix-devenv-strategy.md)

### Commit 173: [d30f9a2](https://github.com/vig-os/devkit/commit/d30f9a2a85be1aaab0e5a3241d9677e6688c0ef1) by [c-vigo](https://github.com/c-vigo) on July 1, 2026 at 04:00 PM
docs: document local image build loop and agent-models customization, 60 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, docs/SKILL_PIPELINE.md, docs/templates/SKILL_PIPELINE.md.j2)

### Commit 174: [17f88d7](https://github.com/vig-os/devkit/commit/17f88d7cc7ee6442545c5f7a85aca1dc5ac03c0f) by [c-vigo](https://github.com/c-vigo) on July 2, 2026 at 07:38 AM
docs: document local image build loop and agent-models customization (#798), 60 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, docs/SKILL_PIPELINE.md, docs/templates/SKILL_PIPELINE.md.j2)

### Commit 175: [bc91ea2](https://github.com/vig-os/devkit/commit/bc91ea20c3ec5ae549c7d185afc5766eaaf72b3e) by [c-vigo](https://github.com/c-vigo) on July 2, 2026 at 07:39 AM
Merge branch 'dev' into feature/778-git-hooks-prek, 60 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md, docs/SKILL_PIPELINE.md, docs/templates/SKILL_PIPELINE.md.j2)

### Commit 176: [9e876b0](https://github.com/vig-os/devkit/commit/9e876b0853428dc7e5ecd6fb5286588b8801688a) by [c-vigo](https://github.com/c-vigo) on July 2, 2026 at 10:24 AM
build: finish the pre-commit to prek migration, 209 files modified

### Commit 177: [4908b9b](https://github.com/vig-os/devkit/commit/4908b9b9c5df4520c4edf5948be9038c3d3fa682) by [c-vigo](https://github.com/c-vigo) on July 2, 2026 at 11:25 AM
fix: close prek migration-completeness gaps from #791 review, 52 files modified

### Commit 178: [47ab473](https://github.com/vig-os/devkit/commit/47ab4735bc0c16724a0f6814c446906d16fd1f7c) by [c-vigo](https://github.com/c-vigo) on July 2, 2026 at 01:11 PM
test: remove the sidecar fixtures and integration tests, 583 files modified (tests/README.md, tests/conftest.py, tests/docker-compose.test.yml, tests/fixtures/justfile, tests/fixtures/sidecar.Containerfile, tests/fixtures/test-build.sh, tests/test_integration.py)

### Commit 179: [1b30038](https://github.com/vig-os/devkit/commit/1b300382236c93fe6f3861753a596d5e9a2b6fe4) by [c-vigo](https://github.com/c-vigo) on July 2, 2026 at 01:11 PM
refactor: drop the sidecar recipes, compose examples, and CI cleanup, 134 files modified

### Commit 180: [1173335](https://github.com/vig-os/devkit/commit/11733358daea2ce79b7aaeee160c1a6289a2b695) by [c-vigo](https://github.com/c-vigo) on July 2, 2026 at 01:11 PM
build: drop hadolint from the flake devTools, 17 files modified (docs/NIX.md, flake.nix, tests/test_image.py)

### Commit 181: [fedb9ff](https://github.com/vig-os/devkit/commit/fedb9ff8f2126a7da7ec7736f5e336af2b2402f1) by [c-vigo](https://github.com/c-vigo) on July 2, 2026 at 01:11 PM
docs: record the sidecar-removal decision boundary and changelog, 50 files modified (CHANGELOG.md, CONTRIBUTE.md, TESTING.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/rfcs/ADR-nix-devenv-strategy.md, docs/templates/CONTRIBUTE.md.j2, docs/templates/TESTING.md.j2)

### Commit 182: [fd1f1ef](https://github.com/vig-os/devkit/commit/fd1f1efc4800c05dcd0b21502874f9cda256f09a) by [c-vigo](https://github.com/c-vigo) on July 2, 2026 at 02:15 PM
ci(setup): unify CI on flake provisioning, drop the ad-hoc uv pin, 298 files modified

### Commit 183: [d79113a](https://github.com/vig-os/devkit/commit/d79113a8f3f0ab25da041626677970ca7386f4b2) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 06:31 AM
ci(setup): unify CI on flake provisioning, drop the ad-hoc uv pin (#801), 298 files modified

### Commit 184: [b1d6ee2](https://github.com/vig-os/devkit/commit/b1d6ee21cdaa8a502d9f73fa873913683d817c53) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 06:32 AM
chore: drop the sidecar / multi-container capability entirely (#800), 784 files modified

### Commit 185: [027ab65](https://github.com/vig-os/devkit/commit/027ab6574232dcec8f4b5af13f02d565497a04b7) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 06:43 AM
test(image): assert baked placeholder manifest present, 28 files modified (tests/test_image.py)

### Commit 186: [b91d782](https://github.com/vig-os/devkit/commit/b91d78231610878aca4cbfacade6abece27eba44) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 06:43 AM
perf(image): bake placeholder manifest into the Nix image, 20 files modified (CHANGELOG.md, flake.nix)

### Commit 187: [41d480c](https://github.com/vig-os/devkit/commit/41d480cdc08baf51869acbb682164a2819cc795d) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 06:55 AM
chore: ruff-format the #718 image test and sync the changelog mirror, 14 files modified (assets/workspace/.devcontainer/CHANGELOG.md, tests/test_image.py)

### Commit 188: [802aa64](https://github.com/vig-os/devkit/commit/802aa648dfbd241cc66f43d29cb1d3ea6441e728) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 07:21 AM
ci: pin pre-commit python to 3.14 for PEP 758 bare-except tuples, 10 files modified (.pre-commit-config.yaml, assets/workspace/.pre-commit-config.yaml)

### Commit 189: [2956b43](https://github.com/vig-os/devkit/commit/2956b43b67aec0a2f49e7bf04c9fefd5f4a9bd8d) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 07:44 AM
perf(image): bake the build-time placeholder manifest into the Nix image (#802), 48 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/test_image.py)

### Commit 190: [5bfaa28](https://github.com/vig-os/devkit/commit/5bfaa28b8e431b31acd5d0c4a04c57bcef285501) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 07:44 AM
ci: pin pre-commit python to 3.14 for PEP 758 bare-except tuples (#804), 10 files modified (.pre-commit-config.yaml, assets/workspace/.pre-commit-config.yaml)

### Commit 191: [51b6a6a](https://github.com/vig-os/devkit/commit/51b6a6a0675b23d415adc7d6b93c1ea08ab0c2ce) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 11:07 AM
Merge remote-tracking branch 'origin/dev' into feature/778-git-hooks-prek, 1138 files modified

### Commit 192: [e67ac1b](https://github.com/vig-os/devkit/commit/e67ac1b29f3b45154881d455063a277adbcdbc8d) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 11:20 AM
build: adopt git-hooks.nix + prek runner, expose checks.pre-commit, and gate flake checks as CI Tier 0 (#791), 922 files modified

### Commit 193: [6896b4a](https://github.com/vig-os/devkit/commit/6896b4a096c6c485f78e151855315fc3aa4ac62f) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 11:31 AM
test(flake): add failing gates for mkProjectServices export and services PoC, 267 files modified (tests/test_flake_services.py)

### Commit 194: [ec7ebfa](https://github.com/vig-os/devkit/commit/ec7ebfad7e025c38d4cad71ff7a21ba9af6dd867) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 11:40 AM
feat(flake): add mkProjectServices helper and SeaweedFS+Postgres PoC, 167 files modified (.gitignore, flake.lock, flake.nix, tests/test_flake_services.py)

### Commit 195: [23bd7fb](https://github.com/vig-os/devkit/commit/23bd7fb5b470362a1a81f8b31a2a64bce2e2297c) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 11:41 AM
test(scaffold): assert opt-in services wiring and devc-* verb rename, 29 files modified (tests/bats/init-workspace.bats)

### Commit 196: [1f8ff3f](https://github.com/vig-os/devkit/commit/1f8ff3f638213ca372651fdaaf0a8caf6933daee) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 11:44 AM
feat(scaffold): opt-in services wiring and devc-* devcontainer verb rename, 49 files modified (assets/workspace/.devcontainer/justfile.devc, assets/workspace/.gitignore, assets/workspace/flake.nix, assets/workspace/justfile.project)

### Commit 197: [1553a79](https://github.com/vig-os/devkit/commit/1553a79c9a0b41c2f1b4a713c99122d536db9039) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 11:46 AM
docs(nix): document mkProjectServices, flake-parts decision, and timing note, 96 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/NIX.md)

### Commit 198: [d1c2919](https://github.com/vig-os/devkit/commit/d1c2919efad251f6a5aebbbf4305a5230d6b6832) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:02 PM
feat(flake): mkProjectServices — daemonless local dev services (process-compose + services-flake) (#807), 580 files modified

### Commit 199: [6ef4a07](https://github.com/vig-os/devkit/commit/6ef4a07ed3def0c2a4e82955fec8070dfcb83888) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:07 PM
ci(security): add workflow_dispatch trigger to the scheduled security scan, 3 files modified (.github/workflows/security-scan.yml)

### Commit 200: [492dc5f](https://github.com/vig-os/devkit/commit/492dc5f348f008f08d705491f7908f1b5d924ec9) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:07 PM
chore(scaffold): fix stale Containerfile reference in scaffold compose comment, 2 files modified (assets/workspace/.devcontainer/docker-compose.yml)

### Commit 201: [5bcd7d8](https://github.com/vig-os/devkit/commit/5bcd7d89faf9d8c94c91bd6524d476c579d08ba5) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:07 PM
docs(release): add Debian rollback pin, devkit rename heads-up, and cutover framing, 32 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md)

### Commit 202: [d7603d7](https://github.com/vig-os/devkit/commit/d7603d721c06f5f6d10677a67095e08b40a6516c) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:17 PM
test(scaffold): assert devc-check/devc-upgrade, worktree import, live compose docs, 29 files modified (tests/bats/init-workspace.bats)

### Commit 203: [7f88b01](https://github.com/vig-os/devkit/commit/7f88b0193f2c3f04750914186cd16da6e81e486b) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:22 PM
refactor(scaffold): rename check/devcontainer-upgrade to devc-check/devc-upgrade, 78 files modified (assets/workspace/.devcontainer/justfile.devc, assets/workspace/.devcontainer/scripts/version-check.sh, tests/test_integration.py)

### Commit 204: [b1a0106](https://github.com/vig-os/devkit/commit/b1a01066fece4bedfe473340d266fdadd9c9b6aa) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:23 PM
fix(scaffold): import justfile.worktree and document live compose layering, 24 files modified (assets/workspace/.devcontainer/README.md, assets/workspace/justfile)

### Commit 205: [33b50cf](https://github.com/vig-os/devkit/commit/33b50cf515b53e765f4deec032d70084fb69ad10) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:23 PM
chore(test): drop stale compose .yml assertion; align ADR services-verb prose, 9 files modified (docs/rfcs/ADR-nix-devenv-strategy.md, tests/test_image.py)

### Commit 206: [ca7e26f](https://github.com/vig-os/devkit/commit/ca7e26f2bc788d421a09a580d0ef302e0fac927d) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:24 PM
docs(changelog): record the #806 audit outcomes; restore prek entry header, 20 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 207: [7c028b5](https://github.com/vig-os/devkit/commit/7c028b574b70b0ed0518433f9acc22a5ce89ea65) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:25 PM
style: apply hook autofixes to touched files, 10 files modified (assets/workspace/.devcontainer/README.md, tests/test_integration.py)

### Commit 208: [636b25f](https://github.com/vig-os/devkit/commit/636b25ffa3301b87ee44c51a162d34e54ff4f8cd) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:35 PM
fix(test): scan for devc-check recipe line in justfile.devc structure tests, 4 files modified (tests/test_integration.py)

### Commit 209: [e7c3196](https://github.com/vig-os/devkit/commit/e7c31964a468a0f44a608df139fef8c1fb7e6d5c) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:37 PM
ci(security): make the vulnix step errexit-safe and always upload findings, 13 files modified (.github/workflows/security-scan.yml)

### Commit 210: [746b88a](https://github.com/vig-os/devkit/commit/746b88a52c99b5520972ceaa48137d807a281e2f) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:51 PM
chore(scaffold): justfile audit — complete devc-* namespacing, reachable worktree recipes, de-stale'd compose docs (#809), 170 files modified

### Commit 211: [3bd0d24](https://github.com/vig-os/devkit/commit/3bd0d2493cd099ae3e27a716d56fbac636c8bbe0) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 12:55 PM
Merge remote-tracking branch 'origin/dev' into feature/639-cutover-readiness, 170 files modified

### Commit 212: [da6be2b](https://github.com/vig-os/devkit/commit/da6be2b4d8b9e203c5e117dab711804132c4ea03) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 01:06 PM
ci(security): cache the vulnix NVD database weekly and retry throttled fetches, 41 files modified (.github/workflows/security-scan.yml)

### Commit 213: [b676c92](https://github.com/vig-os/devkit/commit/b676c92de90d819748ed2799ab09443fd7e64917) by [c-vigo](https://github.com/c-vigo) on July 3, 2026 at 01:29 PM
build(flake): bump vulnix's hardcoded NVD fetch timeout to 60s, 17 files modified (.github/workflows/security-scan.yml, flake.nix)

### Commit 214: [c60beca](https://github.com/vig-os/devkit/commit/c60beca4521ccd6f891c8ecf8f676d739887db54) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 07:02 AM
chore(security): triage the 2026-07 CVE batch into the vulnix exception register, 63 files modified (.vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 215: [41761cf](https://github.com/vig-os/devkit/commit/41761cfcc7ed460f1af7976475f2607901dad7b7) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 07:33 AM
chore(release): 0.4.0 cutover readiness — security-scan dispatch, rollback docs, changelog framing (#808), 165 files modified (.github/workflows/security-scan.yml, .vulnixignore, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.devcontainer/docker-compose.yml, docs/MIGRATION.md, flake.nix)

### Commit 216: [cbf2625](https://github.com/vig-os/devkit/commit/cbf26257ef7793b2708411f35d7f7f405118e854) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on July 4, 2026 at 07:36 AM
chore: freeze changelog for release 0.4.0, 32 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 217: [a8f3115](https://github.com/vig-os/devkit/commit/a8f311590025e5d7669423952d420f2f5a9ab7cc) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on July 4, 2026 at 07:36 AM
chore: rollback failed prepare-release 0.4.0, 32 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 218: [7142c8c](https://github.com/vig-os/devkit/commit/7142c8cca7f95480824be9ca56f055ea9242662f) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 01:44 PM
fix(security): ignore fixed jq CVEs pending :latest rebuild, 12 files modified (.trivyignore)

### Commit 219: [e1d3985](https://github.com/vig-os/devkit/commit/e1d39859e7399041beab61ec7407057b14143e21) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 01:51 PM
fix(ci): cap prepare-release draft-PR body under GitHub's 65536-char limit, 20 files modified (.github/workflows/prepare-release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 220: [9d9bf66](https://github.com/vig-os/devkit/commit/9d9bf66921d709736ff5f92be2cb6d8ad2f7a489) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 02:43 PM
fix(ci): cap prepare-release draft-PR body under GitHub's 65536-char limit (#812), 20 files modified (.github/workflows/prepare-release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 221: [1724a97](https://github.com/vig-os/devkit/commit/1724a9758ac1aa66d0399abaf9e6ad1131487e1f) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 02:44 PM
fix(security): ignore fixed jq CVEs pending :latest rebuild (#811), 12 files modified (.trivyignore)

### Commit 222: [61bc206](https://github.com/vig-os/devkit/commit/61bc206dfd817f8a7ea0f46cda0f190b591e4731) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on July 4, 2026 at 03:04 PM
chore: freeze changelog for release 0.4.0, 32 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 223: [8341e87](https://github.com/vig-os/devkit/commit/8341e8710baa8877c8f015113668547f98f2b6ad) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 03:56 PM
fix(ci): cap release.yml PR-body writes under GitHub's 65536-char limit, 56 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 224: [c140fd6](https://github.com/vig-os/devkit/commit/c140fd6f3325a33a603a3fc273a778e2067e8509) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 04:10 PM
fix(ci): cap release.yml PR-body writes under GitHub's 65536-char limit (#830), 56 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 225: [9e8a43a](https://github.com/vig-os/devkit/commit/9e8a43a3d15fc4c81e47607c8ef4106dbac37ecf) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 06:09 PM
fix(ci): run release build-and-test on ubuntu-24.04 and capture vulnix exit, 26 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 226: [ebe1b10](https://github.com/vig-os/devkit/commit/ebe1b106dffdd70877cb2befc6f70036049e5c5f) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 06:25 PM
fix(ci): run release build-and-test on ubuntu-24.04 and capture vulnix exit (#843), 26 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 227: [7070938](https://github.com/vig-os/devkit/commit/7070938609ec34af516c293efe64a67dd1537e84) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 06:49 PM
fix(ci): avoid SIGPIPE race in setup-env dev-shell PATH summary, 8 files modified (.github/actions/setup-env/action.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 228: [f20162d](https://github.com/vig-os/devkit/commit/f20162dbc5bcbd61d119736d59fd5515e5c6991b) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 07:03 PM
fix(ci): avoid SIGPIPE race in setup-env dev-shell PATH summary (#848), 8 files modified (.github/actions/setup-env/action.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 229: [9deb09b](https://github.com/vig-os/devkit/commit/9deb09b230db4bd35d7373bec1ddfebed93e3c80) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 07:29 PM
fix(ci): make the release Trivy image scan awareness-only, 14 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 230: [80de2c5](https://github.com/vig-os/devkit/commit/80de2c521e20dd640d67408bee09ccb2c904469a) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 07:45 PM
fix(ci): make the release Trivy image scan awareness-only (#850), 14 files modified (.github/workflows/release.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 231: [5b482a0](https://github.com/vig-os/devkit/commit/5b482a05848af7140ac767e4cc21edb59f929959) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 08:29 PM
test(setup): cover the .vig-os version-pin override in the scaffold flow, 60 files modified (tests/bats/init-workspace.bats, tests/bats/install.bats, tests/conftest.py, tests/test_integration.py)

### Commit 232: [a2fd359](https://github.com/vig-os/devkit/commit/a2fd35953794506e20ce4fe966f6413859a74f77) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 08:32 PM
fix(setup): pin the scaffolded .vig-os to the requested install version, 32 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, install.sh)

### Commit 233: [cbd2b71](https://github.com/vig-os/devkit/commit/cbd2b712559f742e146c243d6f493733ad0e8ccf) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 08:47 PM
fix(setup): pin the scaffolded .vig-os to the requested install version (#853), 92 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, install.sh, tests/bats/init-workspace.bats, tests/bats/install.bats, tests/conftest.py, tests/test_integration.py)

### Commit 234: [288dd9b](https://github.com/vig-os/devkit/commit/288dd9b8ce55fae013c1b1a388953275e986a125) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 09:23 PM
test(setup): require .typos.toml in the workspace template, 9 files modified (tests/bats/init-workspace.bats)

### Commit 235: [bf9d7e4](https://github.com/vig-os/devkit/commit/bf9d7e46e8945f015200824a2dbca37f2e849f59) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 09:24 PM
fix(setup): ship .typos.toml with the workspace scaffold, 17 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.typos.toml, scripts/manifest.toml)

### Commit 236: [3b188e4](https://github.com/vig-os/devkit/commit/3b188e47bbcad0870648bb08d1da94316aa6f8c1) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 09:38 PM
fix(setup): ship .typos.toml with the workspace scaffold (#856), 26 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.typos.toml, scripts/manifest.toml, tests/bats/init-workspace.bats)

### Commit 237: [d397e54](https://github.com/vig-os/devkit/commit/d397e5408348396e6750d3b73344c7af4400a202) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 10:18 PM
fix(ci): cap the shipped prepare-release PR body under GitHub's limit, 22 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/prepare-release.yml)

### Commit 238: [4116e17](https://github.com/vig-os/devkit/commit/4116e17f40a17110c6515056265a6869be764b0d) by [c-vigo](https://github.com/c-vigo) on July 4, 2026 at 10:32 PM
fix(ci): cap the shipped prepare-release PR body under GitHub's limit (#858), 22 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.github/workflows/prepare-release.yml)

### Commit 239: [ff2be4b](https://github.com/vig-os/devkit/commit/ff2be4b928e88613194ed6e02afd6bb0fe6b7160) by [c-vigo](https://github.com/c-vigo) on July 5, 2026 at 01:12 PM
test(setup): cover the rc4 field-validation scaffold defects, 69 files modified (tests/bats/init-workspace.bats)

### Commit 240: [b8d353f](https://github.com/vig-os/devkit/commit/b8d353fdd93a06c2c1dbff739660b14748848e87) by [c-vigo](https://github.com/c-vigo) on July 5, 2026 at 01:18 PM
fix(setup): rc4 field-validation batch — direnv commits, prune, scripts, 135 files modified

### Commit 241: [45ac556](https://github.com/vig-os/devkit/commit/45ac556ff583c0269f0ac9bd405a39077081329f) by [c-vigo](https://github.com/c-vigo) on July 5, 2026 at 01:33 PM
fix(setup): rc4 field-validation batch — direnv commits, prune, scripts (#860), 204 files modified

### Commit 242: [eda2771](https://github.com/vig-os/devkit/commit/eda2771f4b3a0def0d2c0382d96c9f8673df8864) by [c-vigo](https://github.com/c-vigo) on July 6, 2026 at 07:18 AM
fix(ci): break renovate changelog self-commit loop and sync workspace mirror, 36 files modified

### Commit 243: [defa77c](https://github.com/vig-os/devkit/commit/defa77ccd6e44a1f0909102f2b5041e888482ec8) by [c-vigo](https://github.com/c-vigo) on July 6, 2026 at 08:19 AM
fix(ci): break renovate changelog self-commit loop and sync workspace mirror (#864), 36 files modified

### Commit 244: [5576c70](https://github.com/vig-os/devkit/commit/5576c70a154a32541e15ad1d75b5727fac9639f9) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on July 6, 2026 at 08:37 AM
chore: finalize release 0.4.0, 8 files modified (.vig-os, CHANGELOG.md, README.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 245: [40fb0a0](https://github.com/vig-os/devkit/commit/40fb0a091ae12e486292e9e9273bf8a56489b412) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on July 6, 2026 at 09:11 AM
revert: undo finalize release 0.4.0 (workflow rollback), 8 files modified (.vig-os, CHANGELOG.md, README.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 246: [4f0beec](https://github.com/vig-os/devkit/commit/4f0beecdea6de8ef5c14ba3cdd1906e8a390fa4d) by [c-vigo](https://github.com/c-vigo) on July 6, 2026 at 11:09 AM
fix(ci): self-host an NVD feed mirror for vulnix scans, 113 files modified (.github/workflows/nvd-mirror-refresh.yml, .github/workflows/release.yml, .github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/CONTAINER_SECURITY.md)

### Commit 247: [31dcba9](https://github.com/vig-os/devkit/commit/31dcba90d0c11c574b11f4767ca672f11599f1e6) by [c-vigo](https://github.com/c-vigo) on July 6, 2026 at 11:29 AM
fix(ci): self-host an NVD feed mirror for vulnix scans (#871), 113 files modified (.github/workflows/nvd-mirror-refresh.yml, .github/workflows/release.yml, .github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/CONTAINER_SECURITY.md)

### Commit 248: [de92a1d](https://github.com/vig-os/devkit/commit/de92a1dc1f3dfe76cebdb5f15d99dc41cf0f2012) by [c-vigo](https://github.com/c-vigo) on July 6, 2026 at 12:44 PM
fix(ci): host the NVD mirror in the dedicated vig-os/nvd-mirror repo, 123 files modified (.github/workflows/nvd-mirror-refresh.yml, .github/workflows/release.yml, .github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/CONTAINER_SECURITY.md)

### Commit 249: [5c16d58](https://github.com/vig-os/devkit/commit/5c16d581cdbcae5eb352c5ee227d5ca46f9e9597) by [c-vigo](https://github.com/c-vigo) on July 6, 2026 at 01:01 PM
fix(ci): move NVD mirror to dedicated vig-os/nvd-mirror Pages host (#872), 123 files modified (.github/workflows/nvd-mirror-refresh.yml, .github/workflows/release.yml, .github/workflows/security-scan.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/CONTAINER_SECURITY.md)

### Commit 250: [bef7644](https://github.com/vig-os/devkit/commit/bef76449c8758926b66abb7dfff25f05ab2ea801) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on July 6, 2026 at 01:14 PM
chore: finalize release 0.4.0, 8 files modified (.vig-os, CHANGELOG.md, README.md, assets/workspace/.devcontainer/CHANGELOG.md)
