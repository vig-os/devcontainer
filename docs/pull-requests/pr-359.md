---
type: pull_request
state: closed (merged)
branch: bugfix/357-bounded-retry-network-calls → release/0.3.1
created: 2026-03-18T14:01:11Z
updated: 2026-03-18T18:09:56Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devcontainer/pull/359
comments: 27
labels: none
assignees: c-vigo
milestone: none
projects: none
merged: 2026-03-18T18:09:53Z
synced: 2026-03-19T04:27:50.314Z
---

# [PR 359](https://github.com/vig-os/devcontainer/pull/359) fix: harden network retries in CI and release workflows

## Description

This PR hardens network-sensitive release and CI paths by replacing shell retries with the `vig-utils` Python `retry` CLI and making retry-safe workflow operations idempotent. It also syncs the same behavior into workspace templates and smoke-test dispatch logic to keep upstream and downstream workflows aligned.

## Type of Change

- [ ] `feat` -- New feature
- [x] `fix` -- Bug fix
- [ ] `docs` -- Documentation only
- [ ] `chore` -- Maintenance task (deps, config, etc.)
- [ ] `refactor` -- Code restructuring (no behavior change)
- [ ] `test` -- Adding or updating tests
- [ ] `ci` -- CI/CD pipeline changes
- [ ] `build` -- Build system or dependency changes
- [ ] `revert` -- Reverts a previous commit
- [ ] `style` -- Code style (formatting, whitespace)

### Modifiers

- [ ] Breaking change (`!`) -- This change breaks backward compatibility

## Changes Made

- `packages/vig-utils/src/vig_utils/retry.py`
  - Added a bounded retry CLI implementation for network-dependent command execution.
- `packages/vig-utils/tests/test_retry.py`
  - Added pytest coverage for retry behavior and failure handling.
- `.github/actions/setup-env/action.yml`, `.github/actions/test-image/action.yml`
  - Updated setup/image test paths to use the retry CLI where network calls are sensitive.
- `.github/workflows/release.yml`, `.github/workflows/prepare-release.yml`, `.github/workflows/ci.yml`, `.github/workflows/sync-issues.yml`, `.github/workflows/sync-main-to-dev.yml`
  - Hardened release/sync/CI workflow steps with retry-safe execution and idempotency guards.
- `assets/smoke-test/.github/workflows/repository-dispatch.yml`
  - Improved dispatch flow resiliency and safe rerun behavior.
- `assets/workspace/.github/workflows/release-core.yml`, `assets/workspace/.github/workflows/release-publish.yml`, `assets/workspace/.github/workflows/release.yml`, `assets/workspace/.github/workflows/prepare-release.yml`, `assets/workspace/.github/workflows/sync-issues.yml`, `assets/workspace/.github/workflows/sync-main-to-dev.yml`, `assets/workspace/.github/actions/resolve-image/action.yml`
  - Synced downstream workflow templates to the retry CLI and image tag resolution updates.
- `CHANGELOG.md`, `assets/workspace/.devcontainer/CHANGELOG.md`, `packages/vig-utils/pyproject.toml`
  - Added the issue #357 changelog entry and package wiring for the new CLI.

## Changelog Entry

### Fixed
- **Bounded retry added for network-dependent setup and prepare-release calls** ([#357](https://github.com/vig-os/devcontainer/issues/357))
  - Replace shell-based retry helper with pure Python `retry` CLI in `vig-utils` (`packages/vig-utils/src/vig_utils/retry.py`)
  - Update this repository CI workflows to call `uv run retry` after `setup-env` dependency sync
  - Update downstream workflow templates to call `retry` directly in devcontainer jobs and remove `source` lines
  - Ensure downstream containerized jobs resolve image tags from `.vig-os` instead of hardcoded `latest`
  - Bundle idempotency guards for branch/PR/tag/release creation paths to keep retried network calls safe on reruns
  - Remove synced `retry.sh` artifacts and BATS retry tests in favor of `vig-utils` pytest coverage

## Testing

- [x] Tests pass locally (`just test`)
- [ ] Manual testing performed (describe below)

### Manual Testing Details

N/A

## Checklist

- [x] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [ ] I have commented my code, particularly in hard-to-understand areas
- [ ] I have updated the documentation accordingly (edit `docs/templates/`, then run `just docs`)
- [x] I have updated `CHANGELOG.md` in the `[Unreleased]` section (and pasted the entry above)
- [ ] My changes generate no new warnings or errors
- [x] I have added tests that prove my fix is effective or that my feature works
- [ ] New and existing unit tests pass locally with my changes
- [ ] Any dependent changes have been merged and published

## Additional Notes

N/A

Refs: #357



---
---

## Review Threads (14)

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:10 PM_

_File: [`assets/workspace/.github/workflows/release-publish.yml (line 154 RIGHT)`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953717965)_

```diff
@@ -113,7 +133,14 @@ jobs:
         run: |
           set -euo pipefail
           git tag -a "$PUBLISH_VERSION" -m "Release $PUBLISH_VERSION"
-          git push origin "$PUBLISH_VERSION"
+          if ! retry --retries 3 --backoff 5 --max-backoff 30 -- git push origin "$PUBLISH_VERSION"; then
+            if git ls-remote --tags --refs origin "$PUBLISH_VERSION" | grep -q "refs/tags/$PUBLISH_VERSION$"; then
+              echo "Tag already present on origin after push attempt: $PUBLISH_VERSION"
+            else
+              echo "ERROR: Failed to push tag $PUBLISH_VERSION"
+              exit 1
+            fi
```

Same issue as the upstream release workflow: if the tag push fails but the tag exists on origin, this continues without validating that the remote tag SHA matches the local tag target. Consider comparing SHAs and failing on mismatch to avoid silently publishing against the wrong tag.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 02:27 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953834708)

  Implemented: after failed tag push, workflow now compares local vs remote tag target SHA and fails on mismatch (workspace release-publish template updated).

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:10 PM_

_File: [`.github/workflows/sync-issues.yml`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953718044)_

```diff
@@ -83,10 +83,12 @@ jobs:
           sleep 2
           # Try to delete cache using GitHub API
           CACHE_KEY="sync-issues-state-${{ github.repository }}"
-          CACHE_ID=$(gh api repos/${{ github.repository }}/actions/caches --jq ".actions_caches[] | select(.key == \"$CACHE_KEY\") | .id" | head -1)
+          CACHE_ID=$(uv run retry --retries 3 --backoff 3 --max-backoff 15 -- \
+            gh api repos/${{ github.repository }}/actions/caches --jq ".actions_caches[] | select(.key == \"$CACHE_KEY\") | .id" | head -1)
           if [ -n "$CACHE_ID" ]; then
             echo "Found cache ID: $CACHE_ID, attempting deletion..."
-            gh api repos/${{ github.repository }}/actions/caches/$CACHE_ID -X DELETE && echo "Cache deleted successfully" || echo "Cache deletion failed (may be locked or already deleted)"
+            uv run retry --retries 3 --backoff 3 --max-backoff 15 -- \
+              gh api repos/${{ github.repository }}/actions/caches/$CACHE_ID -X DELETE && echo "Cache deleted successfully" || echo "Cache deletion failed (may be locked or already deleted)"
```

`uv run retry ...` is used here, but this workflow never installs `uv` (or the `vig-utils` scripts that provide `retry`). On a fresh GitHub runner this can fail with `uv: command not found` or `retry: not found`. Add a setup step (e.g., call the repo's `./.github/actions/setup-env` with `sync-dependencies: 'true'`, or otherwise install `uv` + `vig-utils`) before the first `uv run retry` invocation.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 02:27 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953834844)

  Implemented: added explicit setup step in this workflow (`.github/actions/setup-env` with `sync-dependencies: true`) before first `uv run retry` use.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:10 PM_

_File: [`.github/workflows/sync-main-to-dev.yml`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953718076)_

```diff
@@ -53,7 +53,7 @@ jobs:
         id: check
         run: |
           set -euo pipefail
-          git fetch origin main dev
+          uv run retry --retries 3 --backoff 3 --max-backoff 20 -- git fetch origin main dev
```

This workflow now depends on `uv run retry`, but it doesn't install `uv` or the `vig-utils` CLI entrypoints anywhere in the job. Unless the runner already has `uv` and `retry` available, the workflow will fail. Add an environment setup step before the first `uv run retry` call (e.g., `./.github/actions/setup-env` with dependency sync) or switch to a retry mechanism that doesn't require `uv`.


Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 02:28 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953835007)

  Implemented: added explicit setup step in this workflow before any `uv run retry` call so retry tooling is available on fresh runners.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:10 PM_

_File: [`assets/workspace/.github/workflows/sync-main-to-dev.yml`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953718103)_

```diff
@@ -53,7 +53,7 @@ jobs:
         id: check
         run: |
           set -euo pipefail
-          git fetch origin main dev
+          uv run retry --retries 3 --backoff 3 --max-backoff 20 -- git fetch origin main dev
```

The workspace template project does not include `vig-utils` as a dependency (and this workflow does not install it), so `uv run retry` is not guaranteed to exist in generated repos. Either install `uv` + `vig-utils` before using `uv run retry`, or run this job in the devcontainer image and call `retry` directly (consistent with the other containerized template workflows).


Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 02:28 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953835226)

  Implemented: removed `uv run retry` dependency from workspace template sync-main-to-dev workflow and replaced calls with a local bounded shell retry helper.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:10 PM_

_File: [`assets/workspace/.github/workflows/sync-issues.yml`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953718144)_

```diff
@@ -83,10 +83,12 @@ jobs:
           sleep 2
           # Try to delete cache using GitHub API
           CACHE_KEY="sync-issues-state-${{ github.repository }}"
-          CACHE_ID=$(gh api repos/${{ github.repository }}/actions/caches --jq ".actions_caches[] | select(.key == \"$CACHE_KEY\") | .id" | head -1)
+          CACHE_ID=$(uv run retry --retries 3 --backoff 3 --max-backoff 15 -- \
+            gh api repos/${{ github.repository }}/actions/caches --jq ".actions_caches[] | select(.key == \"$CACHE_KEY\") | .id" | head -1)
           if [ -n "$CACHE_ID" ]; then
             echo "Found cache ID: $CACHE_ID, attempting deletion..."
-            gh api repos/${{ github.repository }}/actions/caches/$CACHE_ID -X DELETE && echo "Cache deleted successfully" || echo "Cache deletion failed (may be locked or already deleted)"
+            uv run retry --retries 3 --backoff 3 --max-backoff 15 -- \
+              gh api repos/${{ github.repository }}/actions/caches/$CACHE_ID -X DELETE && echo "Cache deleted successfully" || echo "Cache deletion failed (may be locked or already deleted)"
```

This template workflow uses `uv run retry` but does not install `uv` or the `retry` CLI. In generated workspace repos (which don't vendor `vig-utils`), these steps will fail. Consider adding an explicit `setup-uv`/dependency install step, or run the workflow in the devcontainer image and use `retry` from PATH.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 02:28 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953835416)

  Implemented: removed `uv run retry` dependency from workspace template sync-issues workflow and replaced it with a local bounded shell retry helper.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:10 PM_

_File: [`.github/workflows/release.yml (line 825 RIGHT)`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953718188)_

```diff
@@ -801,7 +804,14 @@ jobs:
             fi
           fi
 
-          git push origin "$PUBLISH_VERSION"
+          if ! uv run retry --retries 3 --backoff 5 --max-backoff 30 -- git push origin "$PUBLISH_VERSION"; then
+            if git ls-remote --tags --refs origin "$PUBLISH_VERSION" | grep -q "refs/tags/$PUBLISH_VERSION$"; then
+              echo "Tag already present on origin after push attempt: $PUBLISH_VERSION"
+            else
+              echo "ERROR: Failed to push tag $PUBLISH_VERSION"
+              exit 1
+            fi
```

If `git push` fails and the tag already exists on origin, this logic continues without verifying that the remote tag points at the commit SHA you just tagged locally. That can mask a corrupted/partial rerun where the remote tag exists but points elsewhere. After a failed push, compare the remote tag SHA (`git ls-remote --tags --refs origin ...`) with the local tag target SHA and fail if they differ.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 02:28 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953835590)

  Implemented: after failed tag push in release workflow, we now compare remote and local tag target SHA and hard-fail if they differ.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:10 PM_

_File: [`.github/workflows/release.yml (line 1101 RIGHT)`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953718226)_

```diff
@@ -1111,15 +1073,21 @@ jobs:
           GH_TOKEN: ${{ steps.app-token.outputs.token }}
         run: |
           set -euo pipefail
-          if gh release view "$PUBLISH_VERSION" >/dev/null 2>&1; then
+          if uv run retry --retries 2 --backoff 5 --max-backoff 20 -- gh release view "$PUBLISH_VERSION" >/dev/null 2>&1; then
             echo "ERROR: GitHub Release already exists for tag $PUBLISH_VERSION"
             exit 1
           fi
 
-          gh release create "$PUBLISH_VERSION" \
+          uv run retry --retries 3 --backoff 5 --max-backoff 30 -- gh release create "$PUBLISH_VERSION" \
             --title "$PUBLISH_VERSION" \
             --notes-file /tmp/github-release-notes.md \
-            --verify-tag
+            --verify-tag || {
+              if gh release view "$PUBLISH_VERSION" >/dev/null 2>&1; then
+                echo "GitHub Release already present after create attempt: $PUBLISH_VERSION"
+              else
+                exit 1
+              fi
```

The fallback check inside the `gh release create ... || { ... }` block calls `gh release view` without retry. If `gh release create` succeeds but the subsequent `gh release view` fails transiently (rate limiting / network), this step can still exit 1. Wrap the fallback `gh release view` in the same `uv run retry ...` used elsewhere, or avoid the extra call by capturing and inspecting `gh release create` output/status.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 02:28 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953835792)

  Implemented: wrapped fallback `gh release view` check in retry to avoid transient false failures after `gh release create`.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:48 PM_

_File: [`.github/workflows/sync-main-to-dev.yml (line 56 RIGHT)`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953975565)_

```diff
@@ -36,9 +36,29 @@ permissions:
   contents: read
 
 jobs:
+  resolve-image:
+    name: Resolve image tag
+    runs-on: ubuntu-22.04
+    timeout-minutes: 2
+    outputs:
+      image-tag: ${{ steps.resolve.outputs.image-tag }}
+    steps:
+      - name: Checkout repository
+        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
+        with:
+          sparse-checkout: .vig-os
+          sparse-checkout-cone-mode: false
+
+      - name: Resolve container image
+        id: resolve
+        uses: ./.github/actions/resolve-image
+
   check:
```

`actions/checkout` is sparse-checking out `.vig-os`, but this repository does not currently have a root `.vig-os` file. As written, image resolution will fail (or resolve an empty tag) on real runs. Either commit a root `.vig-os` for this repo, or change the resolve logic to read the correct source of truth (e.g., a workflow input / a file that actually exists).


Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 05:08 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2954968341)

  Implemented in follow-up commits: added root `.vig-os` so image resolution now has a concrete source in this repo.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:48 PM_

_File: [`.github/workflows/sync-issues.yml (line 56 RIGHT)`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953975639)_

```diff
@@ -36,8 +36,30 @@ on:  # yamllint disable-line rule:truthy
 permissions: {}  # restrict default; job declares its own
 
 jobs:
+  resolve-image:
+    name: Resolve image tag
+    runs-on: ubuntu-22.04
+    timeout-minutes: 2
+    permissions:
+      contents: read
+    outputs:
+      image-tag: ${{ steps.resolve.outputs.image-tag }}
+    steps:
+      - name: Checkout repository
+        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
+        with:
+          sparse-checkout: .vig-os
+          sparse-checkout-cone-mode: false
+
+      - name: Resolve container image
+        id: resolve
+        uses: ./.github/actions/resolve-image
```

This workflow calls a local action at `./.github/actions/resolve-image`, but that directory is not present in the repository. The `resolve-image` job will fail when it reaches this step. Add/commit the composite action under `.github/actions/resolve-image/` or change the workflow to use an existing action/inline logic.


Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 05:08 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2954968542)

  Implemented: added the missing root composite action at `.github/actions/resolve-image/action.yml` and wired workflows to use it.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:48 PM_

_File: [`assets/smoke-test/.github/workflows/repository-dispatch.yml (line 148 RIGHT)`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953975691)_

```diff
@@ -119,11 +119,42 @@ jobs:
           echo "source_sha=${SOURCE_SHA}" >> "${GITHUB_OUTPUT}"
           echo "correlation_id=${CORRELATION_ID}" >> "${GITHUB_OUTPUT}"
 
+  resolve-image:
+    name: Resolve image tag
+    runs-on: ubuntu-22.04
+    timeout-minutes: 2
+    outputs:
+      image-tag: ${{ steps.resolve.outputs.image-tag }}
+    steps:
+      - name: Checkout repository
+        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
+        with:
+          sparse-checkout: .vig-os
+          sparse-checkout-cone-mode: false
+
+      - name: Resolve container image tag from .vig-os
+        id: resolve
+        shell: bash
+        run: |
+          set -euo pipefail
+          if [ ! -f .vig-os ]; then
+            echo "ERROR: .vig-os is required to resolve DEVCONTAINER_VERSION"
+            exit 1
+          fi
+          TAG="$(awk -F= '/^DEVCONTAINER_VERSION=/{gsub(/^[ \t"]+|[ \t"]+$/, "", $2); print $2; exit}' .vig-os || true)"
+          if [ -z "${TAG:-}" ]; then
+            echo "ERROR: DEVCONTAINER_VERSION not found in .vig-os"
+            exit 1
+          fi
```

This `resolve-image` job assumes a root `.vig-os` file exists, but the `assets/smoke-test/` template currently does not include one. That means the workflow will hard-fail on every dispatch. Either add a `.vig-os` file to the smoke-test template repo content, or allow a fallback/override (e.g., workflow input or hardcoded image tag) so the workflow can run without it.


Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 05:08 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2954968726)

  Implemented: added `assets/smoke-test/.vig-os` so smoke-test image resolution has the expected source file.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:48 PM_

_File: [`.github/workflows/release.yml`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953975742)_

```diff
@@ -801,7 +804,26 @@ jobs:
             fi
           fi
 
-          git push origin "$PUBLISH_VERSION"
+          if ! uv run retry --retries 3 --backoff 5 --max-backoff 30 -- git push origin "$PUBLISH_VERSION"; then
+            if git ls-remote --tags --refs origin "$PUBLISH_VERSION" | grep -q "refs/tags/$PUBLISH_VERSION$"; then
+              LOCAL_TAG_TARGET_SHA=$(git rev-parse "$PUBLISH_VERSION^{}")
+              REMOTE_TAG_TARGET_SHA=$(git ls-remote --tags origin "refs/tags/$PUBLISH_VERSION^{}" | awk '{print $1}')
+              if [ -z "$REMOTE_TAG_TARGET_SHA" ]; then
```

Inside the “push tag failed” fallback, the `git ls-remote ...` calls are still network operations but aren’t wrapped in the new retry helper. If origin is flaky/rate-limited, this can still fail even though the push itself is retried. Consider wrapping the `git ls-remote` checks with `uv run retry ...` as well (or otherwise making this fallback network-resilient).

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 05:08 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2954968878)

  Implemented: wrapped fallback `git ls-remote` network checks in retry in the root release workflow.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:48 PM_

_File: [`assets/workspace/.github/workflows/release-publish.yml`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953975786)_

```diff
@@ -113,7 +133,26 @@ jobs:
         run: |
           set -euo pipefail
           git tag -a "$PUBLISH_VERSION" -m "Release $PUBLISH_VERSION"
-          git push origin "$PUBLISH_VERSION"
+          if ! retry --retries 3 --backoff 5 --max-backoff 30 -- git push origin "$PUBLISH_VERSION"; then
+            if git ls-remote --tags --refs origin "$PUBLISH_VERSION" | grep -q "refs/tags/$PUBLISH_VERSION$"; then
+              LOCAL_TAG_TARGET_SHA=$(git rev-parse "$PUBLISH_VERSION^{}")
+              REMOTE_TAG_TARGET_SHA=$(git ls-remote --tags origin "refs/tags/$PUBLISH_VERSION^{}" | awk '{print $1}')
```

In the “push tag failed” fallback, the `git ls-remote` calls are additional network operations but aren’t wrapped in `retry`. A transient Git/GitHub hiccup can still break the idempotency check even though the push is retried. Wrap the `git ls-remote`/remote-SHA resolution calls in `retry` as well to fully harden this path.


Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 05:08 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2954969024)

  Implemented: wrapped the same `git ls-remote` calls in retry in the workspace release-publish workflow as well.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 02:48 PM_

_File: [`.github/workflows/sync-main-to-dev.yml (line 55 RIGHT)`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2953975830)_

```diff
@@ -36,9 +36,29 @@ permissions:
   contents: read
 
 jobs:
+  resolve-image:
+    name: Resolve image tag
+    runs-on: ubuntu-22.04
+    timeout-minutes: 2
+    outputs:
+      image-tag: ${{ steps.resolve.outputs.image-tag }}
+    steps:
+      - name: Checkout repository
+        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
+        with:
+          sparse-checkout: .vig-os
+          sparse-checkout-cone-mode: false
+
+      - name: Resolve container image
+        id: resolve
+        uses: ./.github/actions/resolve-image
+
```

This workflow calls a local action at `./.github/actions/resolve-image`, but there is no `.github/actions/resolve-image/` directory in the repo, so this step will fail at runtime. Either add/commit the composite action to that path (mirroring the workspace template implementation) or replace this with inline `.vig-os` parsing / a reusable action that actually exists.


Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 05:08 PM — [link](https://github.com/vig-os/devcontainer/pull/359#discussion_r2954969168)

  Implemented: root `./.github/actions/resolve-image` now exists and is committed, so this job path resolves correctly.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 05:19 PM_

_File: [`packages/vig-utils/src/vig_utils/retry.py`](https://github.com/vig-os/devcontainer/pull/359#discussion_r2955033375)_

```diff
@@ -0,0 +1,118 @@
+#!/usr/bin/env python3
+"""Retry CLI for transient command failures with bounded exponential backoff.
+
+Use `uv run retry -- ...` on bare runners that execute within the repository's
+Python environment, or `retry -- ...` in devcontainer jobs where the command is
+already available on PATH.
+"""
+
+from __future__ import annotations
+
+import subprocess
+import sys
+import time
+from typing import TYPE_CHECKING
+
+if TYPE_CHECKING:
+    from collections.abc import Sequence
+
+
+def _parse_positive_int(value: str, option_name: str) -> int:
+    if not value.isdigit() or int(value) <= 0:
+        raise ValueError(f"retry: {option_name} must be a positive integer")
+    return int(value)
+
+
+def parse_cli(argv: Sequence[str]) -> tuple[int, int, int, list[str]]:
+    """Parse retry options and command from argv (without program name)."""
+    retries = 3
+    backoff = 5
+    max_backoff = 60
+    command_start = None
+    i = 0
+
+    while i < len(argv):
+        arg = argv[i]
+        if arg == "--":
+            command_start = i + 1
+            break
+        if arg == "--retries":
+            if i + 1 >= len(argv):
+                raise ValueError("retry: missing value for '--retries'")
+            retries = _parse_positive_int(argv[i + 1], "--retries")
+            i += 2
+            continue
+        if arg == "--backoff":
+            if i + 1 >= len(argv):
+                raise ValueError("retry: missing value for '--backoff'")
+            backoff = _parse_positive_int(argv[i + 1], "--backoff")
+            i += 2
+            continue
+        if arg == "--max-backoff":
+            if i + 1 >= len(argv):
+                raise ValueError("retry: missing value for '--max-backoff'")
+            max_backoff = _parse_positive_int(argv[i + 1], "--max-backoff")
+            i += 2
+            continue
+        raise ValueError(f"retry: unknown option '{arg}'")
+
+    if command_start is None or command_start >= len(argv):
+        raise ValueError("retry: missing command after '--'")
+
+    return retries, backoff, max_backoff, list(argv[command_start:])
+
+
+def retry_command(
+    command: list[str],
+    *,
+    retries: int,
+    backoff: int,
+    max_backoff: int,
+) -> int:
+    """Run command with bounded exponential retry."""
+    exit_code = 0
+    for attempt in range(1, retries + 1):
+        result = subprocess.run(
+            command,
+            stdin=sys.stdin,
+            stdout=sys.stdout,
+            stderr=sys.stderr,
+            check=False,
+        )
```

`retry_command` does not handle `subprocess.run(...)` raising `OSError` (e.g., command not found / permission denied). In that case the CLI will crash with a Python traceback rather than returning a clean non-zero exit code and an actionable message. Consider catching `OSError` around the `subprocess.run` call, printing a concise error (including the command), and exiting immediately (no retry) or treating it as a failed attempt depending on desired semantics.



---
---

## Commits

### Commit 1: [466523b](https://github.com/vig-os/devcontainer/commit/466523b1e20ea95b398c092e5c0ac69dec64054e) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 01:50 PM
test(vigutils): add red tests for retry cli, 134 files modified (packages/vig-utils/tests/test_retry.py)

### Commit 2: [13a3550](https://github.com/vig-os/devcontainer/commit/13a3550e1227065ea19344a47c887c4aa61eee57) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 01:52 PM
feat(vigutils): add bounded retry cli command, 119 files modified (packages/vig-utils/pyproject.toml, packages/vig-utils/src/vig_utils/retry.py)

### Commit 3: [9b484ab](https://github.com/vig-os/devcontainer/commit/9b484ab08eaf5b042220ccbb2982673f1302f33e) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 01:52 PM
fix(ci): use retry cli for setup and image pull steps, 37 files modified (.github/actions/setup-env/action.yml, .github/actions/test-image/action.yml, .github/workflows/ci.yml)

### Commit 4: [2ff89ec](https://github.com/vig-os/devcontainer/commit/2ff89ec47256540d6c888a356ef82adbc06d032b) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 01:53 PM
fix(ci): harden release and sync workflows with retry, 275 files modified

### Commit 5: [06e4aab](https://github.com/vig-os/devcontainer/commit/06e4aab7dd7320c552b4165b9caaa5ff5d2ead81) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 01:54 PM
fix(ci): harden workspace release templates with retry, 151 files modified

### Commit 6: [b8b5304](https://github.com/vig-os/devcontainer/commit/b8b53043847562505e97d42a2b51b767c7c477e5) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 01:54 PM
fix(ci): harden smoke-test dispatch retries and image resolution, 111 files modified (assets/smoke-test/.github/workflows/repository-dispatch.yml)

### Commit 7: [1d2dce4](https://github.com/vig-os/devcontainer/commit/1d2dce4532d6f054e77ff5fd96ebfbaba95adf95) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 01:54 PM
docs(ci): record retry cli rollout in workflows, 14 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 8: [b5e2c7c](https://github.com/vig-os/devcontainer/commit/b5e2c7c087f4501c6383018cb78dc2c191e283c3) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 02:29 PM
fix(ci): validate tag target and release fallback retry, 30 files modified (.github/workflows/release.yml, assets/workspace/.github/workflows/release-publish.yml)

### Commit 9: [7b6e155](https://github.com/vig-os/devcontainer/commit/7b6e155ff95c196621bbf9aa787cc7c5c94dd36f) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 02:30 PM
fix(ci): harden sync workflow retry setup and portability, 30 files modified (.github/workflows/sync-issues.yml, .github/workflows/sync-main-to-dev.yml, assets/workspace/.github/workflows/sync-issues.yml, assets/workspace/.github/workflows/sync-main-to-dev.yml)

### Commit 10: [1f5851d](https://github.com/vig-os/devcontainer/commit/1f5851d2f9548f3748cea4ea9205bf02a7d5b17d) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 02:36 PM
fix(ci): run sync workflows in container image, 182 files modified (.github/workflows/sync-issues.yml, .github/workflows/sync-main-to-dev.yml, assets/workspace/.github/workflows/sync-issues.yml, assets/workspace/.github/workflows/sync-main-to-dev.yml)

### Commit 11: [21764df](https://github.com/vig-os/devcontainer/commit/21764df5f822a15115c5be6a43fbee2181ad2d40) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 05:06 PM
fix(ci): add resolve-image action and vig-os config, 78 files modified (.github/actions/resolve-image/action.yml, .vig-os, assets/smoke-test/.vig-os)

### Commit 12: [e93f9b2](https://github.com/vig-os/devcontainer/commit/e93f9b256e2bb4ee47a850b4422fcd02f91f53a2) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 05:06 PM
fix(ci): retry remote tag checks in push fallback, 10 files modified (.github/workflows/release.yml, assets/workspace/.github/workflows/release-publish.yml)

### Commit 13: [992ed2f](https://github.com/vig-os/devcontainer/commit/992ed2fa8739ac9579611424a6b82929e795fce3) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 05:32 PM
fix(vigutils): handle command execution errors in retry CLI, 51 files modified (packages/vig-utils/src/vig_utils/retry.py, packages/vig-utils/tests/test_retry.py)
