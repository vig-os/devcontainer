---
type: pull_request
state: closed (merged)
branch: bugfix/266-prepare-release-dev-ref → dev
created: 2026-03-12T09:48:21Z
updated: 2026-03-12T10:26:47Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devcontainer/pull/267
comments: 12
labels: none
assignees: c-vigo
milestone: none
projects: none
merged: 2026-03-12T10:26:44Z
synced: 2026-03-12T12:05:24.434Z
---

# [PR 267](https://github.com/vig-os/devcontainer/pull/267) fix(ci): pin release workflow refs and harden prepare-release rollback

## Description

Pins release workflow dispatches to explicit branch refs and hardens `prepare-release` failure rollback so partial side effects are reverted safely without clobbering concurrent `dev` updates.

## Type of Change

- [ ] `feat` -- New feature
- [x] `fix` -- Bug fix
- [x] `docs` -- Documentation only
- [ ] `chore` -- Maintenance task (deps, config, etc.)
- [ ] `refactor` -- Code restructuring (no behavior change)
- [x] `test` -- Adding or updating tests
- [x] `ci` -- CI/CD pipeline changes
- [ ] `build` -- Build system or dependency changes
- [ ] `revert` -- Reverts a previous commit
- [ ] `style` -- Code style (formatting, whitespace)

### Modifiers

- [ ] Breaking change (`!`) -- This change breaks backward compatibility

## Changes Made

- `justfile`
  - `prepare-release`, `publish-candidate`, and `finalize-release` accept optional `ref`
  - workflow dispatches use `gh workflow run ... --ref "$REF"`
  - default refs: `dev` (prepare) and `release/{{ version }}` (candidate/finalize)
- `tests/bats/just.bats`
  - regression tests for dispatch ref defaults
  - assertions for rollback step presence in `prepare-release.yml`
- `.github/workflows/prepare-release.yml`
  - captures pre-prepare `dev` SHA
  - adds rollback routine on failure
  - adds rollback safety guards for missing `GH_TOKEN` / `PREPARE_START_SHA`
  - skips CHANGELOG rollback when `dev` has advanced past freeze SHA
- `docs/RELEASE_CYCLE.md`
  - documents explicit ref usage and rollback behavior
- `CHANGELOG.md`
  - adds/updates unreleased fixed entry for #266 with consistent `release/X.Y.Z` notation
- generated docs updated by hooks
  - `README.md`
  - `CONTRIBUTE.md`

## Changelog Entry

### Fixed

- **Release workflow dispatch refs and prepare rollback safeguards** ([#266](https://github.com/vig-os/devcontainer/issues/266))
  - `just prepare-release` dispatches `prepare-release.yml` from `dev` via `--ref dev`
  - `just publish-candidate` and `just finalize-release` dispatch `release.yml` from `release/X.Y.Z` via `--ref release/X.Y.Z`
  - `prepare-release.yml` now rolls back partial side effects on failure by deleting a created `release/X.Y.Z` branch and restoring `CHANGELOG.md` on `dev`

## Testing

- [x] Tests pass locally (`just test`)
- [ ] Manual testing performed (describe below)

### Manual Testing Details

N/A

## Checklist

- [x] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [ ] I have commented my code, particularly in hard-to-understand areas
- [x] I have updated the documentation accordingly (edit `docs/templates/`, then run `just docs`)
- [x] I have updated `CHANGELOG.md` in the `[Unreleased]` section (and pasted the entry above)
- [x] My changes generate no new warnings or errors
- [x] I have added tests that prove my fix is effective or that my feature works
- [x] New and existing unit tests pass locally with my changes
- [ ] Any dependent changes have been merged and published

## Additional Notes

- Addressed Co*ilot review feedback: accepted rollback guard improvements and changelog notation consistency; rejected false-positive escaping comments on BATS tests after runtime verification.

Refs: #266


---
---

## Review Threads (6)

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 12, 2026 at 09:55 AM_

_File: [`tests/bats/just.bats (line 29 RIGHT)`](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923486379)_

```diff
@@ -14,3 +14,28 @@ setup() {
     assert_success
     assert_output --partial "Available recipes"
 }
+
+@test "prepare-release dispatches workflow from dev ref" {
+    run bash -lc "awk '/^prepare-release version ref=\"\" \\*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF=\"dev\"'"
+    assert_success
+}
+
+@test "finalize-release dispatches workflow from release branch ref" {
+    run bash -lc "awk '/^finalize-release version ref=\"\" \\*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF=\"release/{{ version }}\"'"
+    assert_success
+}
+
+@test "publish-candidate dispatches workflow from release branch ref" {
+    run bash -lc "awk '/^publish-candidate version ref=\"\" \\*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF=\"release/{{ version }}\"'"
```

The grep assertion is searching for the literal text `REF=\"dev\"` (because the pattern is single-quoted), which won’t match the actual justfile line `REF="dev"`. Also, the AWK regex fragment `\\*flags:` does not match the literal `*flags:` in the recipe header (it effectively matches zero or more backslashes). Update the grep/awk patterns so they match the justfile content as written (e.g., search for `REF="dev"` and escape `*` correctly).
```suggestion
    run bash -lc "awk '/^prepare-release version ref="" \*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF="dev"'"
    assert_success
}

@test "finalize-release dispatches workflow from release branch ref" {
    run bash -lc "awk '/^finalize-release version ref="" \*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF="release/{{ version }}"'"
    assert_success
}

@test "publish-candidate dispatches workflow from release branch ref" {
    run bash -lc "awk '/^publish-candidate version ref="" \*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF="release/{{ version }}"'"
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 12, 2026 at 10:03 AM — [link](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923527245)

  Thanks for the close read. This escaping is intentional because the full command is wrapped in outer double quotes for bash -lc. In that context, backslash escaping is reduced before awk and grep run, and the runtime match is against ref="" *flags: and REF="dev" as intended. I also validated by running the exact command directly and it passes.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 12, 2026 at 09:55 AM_

_File: [`tests/bats/just.bats (line 29 RIGHT)`](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923486440)_

```diff
@@ -14,3 +14,28 @@ setup() {
     assert_success
     assert_output --partial "Available recipes"
 }
+
+@test "prepare-release dispatches workflow from dev ref" {
+    run bash -lc "awk '/^prepare-release version ref=\"\" \\*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF=\"dev\"'"
+    assert_success
+}
+
+@test "finalize-release dispatches workflow from release branch ref" {
+    run bash -lc "awk '/^finalize-release version ref=\"\" \\*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF=\"release/{{ version }}\"'"
+    assert_success
+}
+
+@test "publish-candidate dispatches workflow from release branch ref" {
+    run bash -lc "awk '/^publish-candidate version ref=\"\" \\*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF=\"release/{{ version }}\"'"
```

Same issue as the previous test: the grep pattern includes backslashes inside single quotes (so it won’t match `REF="release/{{ version }}"`), and the AWK header match uses `\\*flags:` which doesn’t match the literal `*flags:` token. Adjust the awk/grep patterns so the test reliably matches the recipe block and the `REF="release/{{ version }}"` assignment.
```suggestion
    run bash -lc "awk '/^prepare-release version ref=\"\" [*]flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF="dev"'"
    assert_success
}

@test "finalize-release dispatches workflow from release branch ref" {
    run bash -lc "awk '/^finalize-release version ref=\"\" [*]flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF="release/{{ version }}"'"
    assert_success
}

@test "publish-candidate dispatches workflow from release branch ref" {
    run bash -lc "awk '/^publish-candidate version ref=\"\" [*]flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF="release/{{ version }}"'"
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 12, 2026 at 10:03 AM — [link](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923527364)

  Same reasoning as above: the current escaping is required by the outer bash -lc quoting layer, and the command evaluates correctly at runtime. Verified by executing the exact test command directly; it passes.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 12, 2026 at 09:55 AM_

_File: [`tests/bats/just.bats (line 29 RIGHT)`](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923486463)_

```diff
@@ -14,3 +14,28 @@ setup() {
     assert_success
     assert_output --partial "Available recipes"
 }
+
+@test "prepare-release dispatches workflow from dev ref" {
+    run bash -lc "awk '/^prepare-release version ref=\"\" \\*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF=\"dev\"'"
+    assert_success
+}
+
+@test "finalize-release dispatches workflow from release branch ref" {
+    run bash -lc "awk '/^finalize-release version ref=\"\" \\*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF=\"release/{{ version }}\"'"
+    assert_success
+}
+
+@test "publish-candidate dispatches workflow from release branch ref" {
+    run bash -lc "awk '/^publish-candidate version ref=\"\" \\*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF=\"release/{{ version }}\"'"
```

Same pattern bug here as well: the grep fixed-string contains backslashes inside single quotes (so it searches for `\"` literally), and the AWK regex `\\*flags:` won’t match the recipe header’s `*flags:`. Fix the quoting/escaping so this test matches the justfile content.
```suggestion
    run bash -lc "awk '/^prepare-release version ref="" \*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF="dev"'"
    assert_success
}

@test "finalize-release dispatches workflow from release branch ref" {
    run bash -lc "awk '/^finalize-release version ref="" \*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF="release/{{ version }}"'"
    assert_success
}

@test "publish-candidate dispatches workflow from release branch ref" {
    run bash -lc "awk '/^publish-candidate version ref="" \*flags:/{flag=1; next} /^$/{if(flag){exit}} flag' justfile | grep -Fq -- 'REF="release/{{ version }}"'"
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 12, 2026 at 10:03 AM — [link](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923527524)

  Confirmed this is a false positive from static escaping interpretation. The existing quoting is correct for the nested bash -lc invocation, and the command passes when executed as written.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 12, 2026 at 09:55 AM_

_File: [`.github/workflows/prepare-release.yml (line 359 RIGHT)`](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923486494)_

```diff
@@ -296,7 +306,69 @@ jobs:
           echo "pr_url=$PR_URL" >> $GITHUB_OUTPUT
           echo "✓ Draft PR created: $PR_URL (#$PR_NUMBER)"
 
+      - name: Roll back prepare-release side effects on failure
+        if: ${{ failure() }}
+        id: rollback-prepare
+        env:
+          GH_TOKEN: ${{ steps.app-token.outputs.token }}
+          VERSION: ${{ needs.validate.outputs.version }}
+          RELEASE_BRANCH: ${{ needs.validate.outputs.release_branch }}
+          PREPARE_START_SHA: ${{ steps.pre-state.outputs.prepare_start_sha }}
+        run: |
+          set -euo pipefail
+
+          BRANCH_DELETED=false
+          CHANGELOG_ROLLBACK_NEEDED=false
+
+          if gh api "repos/${{ github.repository }}/git/ref/heads/$RELEASE_BRANCH" >/dev/null 2>&1; then
+            gh api -X DELETE "repos/${{ github.repository }}/git/refs/heads/$RELEASE_BRANCH"
+            BRANCH_DELETED=true
+            echo "✓ Deleted partially created release branch: $RELEASE_BRANCH"
+          else
+            echo "i Release branch not present; nothing to delete"
+          fi
+
+          gh api "repos/${{ github.repository }}/contents/CHANGELOG.md?ref=$PREPARE_START_SHA" --jq '.content' | tr -d '\n' | base64 -d > /tmp/changelog.pre-prepare.md
+          gh api "repos/${{ github.repository }}/contents/CHANGELOG.md?ref=dev" --jq '.content' | tr -d '\n' | base64 -d > /tmp/changelog.current-dev.md
+
+          if cmp -s /tmp/changelog.pre-prepare.md /tmp/changelog.current-dev.md; then
+            echo "i dev CHANGELOG already matches pre-prepare state"
+          else
+            cp /tmp/changelog.pre-prepare.md CHANGELOG.md
+            CHANGELOG_ROLLBACK_NEEDED=true
+            echo "✓ Prepared CHANGELOG rollback content for dev"
```

The rollback routine restores `CHANGELOG.md` on `dev` based solely on the SHA captured at workflow start. If `dev` advances during the run (e.g., unrelated commits that also touch `CHANGELOG.md`), this will overwrite those newer changes. Consider gating the CHANGELOG restore on `dev` still pointing to the expected head created by this workflow (e.g., capture the post-freeze `dev` SHA and only roll back when current `dev` matches it), or otherwise skip rollback and emit a clear message to avoid clobbering concurrent updates.
```suggestion
          CURRENT_DEV_SHA="$(gh api "repos/${{ github.repository }}/git/ref/heads/dev" --jq '.object.sha')"

          if [ "$CURRENT_DEV_SHA" != "$PREPARE_START_SHA" ]; then
            echo "i dev has advanced since prepare-release started (current: $CURRENT_DEV_SHA, expected: $PREPARE_START_SHA); skipping CHANGELOG rollback to avoid clobbering concurrent updates"
          else
            gh api "repos/${{ github.repository }}/contents/CHANGELOG.md?ref=$PREPARE_START_SHA" --jq '.content' | tr -d '\n' | base64 -d > /tmp/changelog.pre-prepare.md
            gh api "repos/${{ github.repository }}/contents/CHANGELOG.md?ref=dev" --jq '.content' | tr -d '\n' | base64 -d > /tmp/changelog.current-dev.md

            if cmp -s /tmp/changelog.pre-prepare.md /tmp/changelog.current-dev.md; then
              echo "i dev CHANGELOG already matches pre-prepare state"
            else
              cp /tmp/changelog.pre-prepare.md CHANGELOG.md
              CHANGELOG_ROLLBACK_NEEDED=true
              echo "✓ Prepared CHANGELOG rollback content for dev"
            fi
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 12, 2026 at 10:03 AM — [link](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923528438)

  Great catch. I will gate CHANGELOG rollback using the post-freeze dev SHA already captured from branch creation (`steps.create-branch.outputs.dev_sha`). During rollback, if current dev head differs from that SHA, rollback will skip CHANGELOG restore and log a warning so concurrent updates are not clobbered.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 12, 2026 at 09:55 AM_

_File: [`.github/workflows/prepare-release.yml (line 320 RIGHT)`](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923486516)_

```diff
@@ -296,7 +306,69 @@ jobs:
           echo "pr_url=$PR_URL" >> $GITHUB_OUTPUT
           echo "✓ Draft PR created: $PR_URL (#$PR_NUMBER)"
 
+      - name: Roll back prepare-release side effects on failure
+        if: ${{ failure() }}
+        id: rollback-prepare
+        env:
+          GH_TOKEN: ${{ steps.app-token.outputs.token }}
+          VERSION: ${{ needs.validate.outputs.version }}
+          RELEASE_BRANCH: ${{ needs.validate.outputs.release_branch }}
+          PREPARE_START_SHA: ${{ steps.pre-state.outputs.prepare_start_sha }}
+        run: |
+          set -euo pipefail
+
```

This rollback step assumes `steps.pre-state.outputs.prepare_start_sha` and `steps.app-token.outputs.token` are always available. If the job fails before those steps run (or if token generation fails), `PREPARE_START_SHA`/`GH_TOKEN` will be empty and the rollback commands may either fail or restore from the default branch unintentionally. Add guards (e.g., validate these env vars are non-empty before attempting rollback) and fall back to a safe no-op with a clear log message when prerequisites for rollback aren’t present.
```suggestion

          # Ensure we have the necessary context to perform a safe rollback.
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "w Skipping rollback: GH_TOKEN is not set; cannot safely call GitHub API."
            exit 0
          fi

          if [ -z "${PREPARE_START_SHA:-}" ]; then
            echo "w Skipping rollback: PREPARE_START_SHA is not set; cannot restore CHANGELOG to pre-prepare state."
            exit 0
          fi
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 12, 2026 at 10:03 AM — [link](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923528591)

  Agreed. I am adding explicit guards at the top of rollback to no-op safely when `GH_TOKEN` or `PREPARE_START_SHA` is missing, with clear log messages.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 12, 2026 at 09:55 AM_

_File: [`CHANGELOG.md`](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923486547)_

```diff
@@ -559,6 +559,10 @@ and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0
   - Label taxonomy guidance clarifies that `setup-labels` is provided by devcontainer bootstrap tooling
 - **CodeQL PR trigger no longer blocks merge protection on non-Python changes** ([#247](https://github.com/vig-os/devcontainer/issues/247))
   - Removed `pull_request` `paths` filter from `codeql.yml` in both root and workspace template so CodeQL always runs for PRs targeting protected branches and uploads SARIF results
+- **Release workflow dispatch refs and prepare rollback safeguards** ([#266](https://github.com/vig-os/devcontainer/issues/266))
+  - `just prepare-release` dispatches `prepare-release.yml` from `dev` via `--ref dev`
+  - `just publish-candidate` and `just finalize-release` dispatch `release.yml` from `release/X.Y.Z` via `--ref release/{{ version }}`
```

The changelog entry mixes an abstract branch example (`release/X.Y.Z`) with a templated ref example (`release/{{ version }}`). Since `CHANGELOG.md` is user-facing, consider using a single consistent notation (e.g., `release/X.Y.Z` everywhere, or `release/<version>` everywhere) to avoid confusion about whether `{{ version }}` is something users should type literally.
```suggestion
  - `just publish-candidate` and `just finalize-release` dispatch `release.yml` from `release/X.Y.Z` via `--ref release/X.Y.Z`
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 12, 2026 at 10:03 AM — [link](https://github.com/vig-os/devcontainer/pull/267#discussion_r2923528754)

  Good catch. I will make the changelog wording consistent and user-facing by using `release/X.Y.Z` notation.


---
---

## Commits

### Commit 1: [06541a0](https://github.com/vig-os/devcontainer/commit/06541a00926e86fee26d58fcea5d31ec2b01a541) by [c-vigo](https://github.com/c-vigo) on March 12, 2026 at 09:43 AM
test(ci): add dispatch ref regression checks, 15 files modified (tests/bats/just.bats)

### Commit 2: [a438270](https://github.com/vig-os/devcontainer/commit/a4382700601a43a1824901ad06215ab48603fb3f) by [c-vigo](https://github.com/c-vigo) on March 12, 2026 at 09:44 AM
fix(ci): add branch ref defaults for release dispatch, 36 files modified (CONTRIBUTE.md, README.md, justfile)

### Commit 3: [d82e238](https://github.com/vig-os/devcontainer/commit/d82e2389967e452b2b1494fadd85b8d755bd09ab) by [c-vigo](https://github.com/c-vigo) on March 12, 2026 at 09:44 AM
test(ci): add prepare-release rollback assertions, 10 files modified (tests/bats/just.bats)

### Commit 4: [c528736](https://github.com/vig-os/devcontainer/commit/c528736bb02e51cefe01d752cdecae568f722e30) by [c-vigo](https://github.com/c-vigo) on March 12, 2026 at 09:44 AM
fix(ci): rollback failed prepare-release side effects, 72 files modified (.github/workflows/prepare-release.yml)

### Commit 5: [1e609b8](https://github.com/vig-os/devcontainer/commit/1e609b8b2887fb9c964365b8161efa45eff1a78c) by [c-vigo](https://github.com/c-vigo) on March 12, 2026 at 09:45 AM
docs(ci): document branch refs and prepare rollback, 15 files modified (CHANGELOG.md, docs/RELEASE_CYCLE.md)

### Commit 6: [3e0e714](https://github.com/vig-os/devcontainer/commit/3e0e71449ad4b8da08ab5fc99cedbba704479701) by [c-vigo](https://github.com/c-vigo) on March 12, 2026 at 10:10 AM
fix(ci): harden prepare-release rollback safeguards, 22 files modified (.github/workflows/prepare-release.yml, CHANGELOG.md)
