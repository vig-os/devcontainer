---
type: pull_request
state: closed (merged)
branch: bugfix/953-init-workspace-copy-and-org-fixes → dev
created: 2026-07-09T11:55:50Z
updated: 2026-07-09T12:10:46Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devkit/pull/956
comments: 1
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-07-09T12:10:45Z
synced: 2026-07-11T13:35:08.593Z
---

# [PR 956](https://github.com/vig-os/devkit/pull/956) fix(workspace): ship nested scaffold docs + fix imageless DEVKIT_ORG default (#953, #954)

Two related `assets/init-workspace.sh` bug fixes, TDD, one PR into `dev`.

## #953 — scaffold copy drops nested README.md/CHANGELOG.md
**Problem:** `PRESERVE_FILES` lists bare names (`README.md`, `CHANGELOG.md`) to protect the consumer's ROOT docs. The copy built `--exclude=$preserved` (no leading slash), so rsync matched by **basename at every depth** and silently dropped devkit-authored NESTED docs (`.devcontainer/README.md`, `.devcontainer/CHANGELOG.md`, `.claude/skills/*/README.md`) on `--force` upgrades — files the `--preview` report (exact-path `is_preserved_file`) still promised under ADDED.

**Fix:** root-anchor each exclude (`--exclude=/$preserved`), matching `is_preserved_file`'s exact-path semantics. Root docs stay preserved; nested docs now ship.

**RED→GREEN:** new bats test seeds a consumer root `README.md`/`CHANGELOG.md`, runs a `--force` upgrade, then asserts the root docs are preserved AND `.devcontainer/README.md`, `.devcontainer/CHANGELOG.md`, `.claude/skills/inception_explore/README.md` were copied. Failed on the nested-doc assertion before the fix; green after.

## #954 — imageless `--no-prompts` defaults org to literal `vigOS/devc`
**Problem:** under `--no-prompts` with no `ORG_NAME` env and no manifest `DEVKIT_ORG`, the org defaulted to the hardcoded `vigOS/devc` — a bogus `/`-bearing org that sed-substitutes into `{{ORG_NAME}}` in generated files (e.g. the LICENSE copyright line). `GITHUB_REPOSITORY` (owner/repo) is already available on this path (`DEVKIT_REPO` uses it) but ORG ignored it.

**Fix:** default the org to the `GITHUB_REPOSITORY` owner segment (`${GITHUB_REPOSITORY%%/*}`), falling back to the sane literal `vigOS` only when no usable owner/repo is present (empty or `OWNER/REPO`).

**RED→GREEN:** new bats test runs `--no-prompts` with `GITHUB_REPOSITORY=some-org/repo` and no `ORG_NAME`/manifest org, then asserts `.vig-os` persists `DEVKIT_ORG=some-org` (not `vigOS/devc`) and the LICENSE org substitution is slash-free. Persisted `vigOS/devc` before the fix; green after. The existing #885 write-back test's assertion was updated from `DEVKIT_ORG=vigOS/devc` to `DEVKIT_ORG=test` to reflect the corrected owner-derived default.

## Verification
- Full bats suite (`tests/bats/init-workspace.bats`): 123 passed, 0 failed (existing #949/#951 and all prior tests still green).
- Full `prek run --all-files`: green (sync-manifest mirrored the changelog into `assets/workspace/.devcontainer/CHANGELOG.md`, folded into the fix commit).

Closes #953
Closes #954



---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/devkit/pull/956#issuecomment-4924872393) by [@c-vigo](https://github.com/c-vigo)

_Posted on July 9, 2026 at 12:07 PM_

Reviewed: both fixes match the filed root causes and are minimal — #953 root-anchors the preserve excludes (`--exclude=/$preserved`) so devkit nested docs ship and the preview matches the apply; #954 derives the imageless `--no-prompts` org from `GITHUB_REPOSITORY`'s owner (fallback `vigOS`), dropping the bogus `vigOS/devc` literal. Two new TDD tests reproduce red→green (123 passed); the updated existing assertions are legitimate (the old `DEVKIT_ORG=vigOS/devc` assertion was codifying the bug). Changelog under Unreleased. LGTM — auto-merge into dev stands.

---
---

## Commits

### Commit 1: [7b2ba2f](https://github.com/vig-os/devkit/commit/7b2ba2f7f82e726b7d88e8e058d7b742c3900f1b) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 11:46 AM
test(workspace): nested scaffold docs must survive a preserve-anchored upgrade, 32 files modified (tests/bats/init-workspace.bats)

### Commit 2: [1477dd4](https://github.com/vig-os/devkit/commit/1477dd4b9cf4c066a178bd96af80889c186d42f2) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 11:49 AM
fix(workspace): root-anchor preserve excludes so nested scaffold docs ship, 11 files modified (assets/init-workspace.sh, tests/bats/init-workspace.bats)

### Commit 3: [1067a7f](https://github.com/vig-os/devkit/commit/1067a7f7dc5f43a205765c894ba1118213db0af3) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 11:50 AM
test(workspace): imageless --no-prompts must derive org from repo owner, 35 files modified (tests/bats/init-workspace.bats)

### Commit 4: [daee722](https://github.com/vig-os/devkit/commit/daee7228239eeb4bbdaf6f6c4409336cba011d13) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 11:51 AM
fix(workspace): derive imageless --no-prompts org from the repo owner, 38 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, tests/bats/init-workspace.bats)
