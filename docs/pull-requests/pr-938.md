---
type: pull_request
state: closed (merged)
branch: release/0.5.0 → main
created: 2026-07-08T15:01:26Z
updated: 2026-07-09T13:04:57Z
author: vig-os-release-app[bot]
author_url: https://github.com/vig-os-release-app[bot]
url: https://github.com/vig-os/devkit/pull/938
comments: 4
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-07-09T13:04:54Z
synced: 2026-07-11T13:35:07.479Z
---

# [PR 938](https://github.com/vig-os/devkit/pull/938) chore: release 0.5.0

# [Release 0.5.0](https://github.com/vig-os/devcontainer/releases/tag/0.5.0) - 2026-07-09

This PR prepares release 0.5.0 for merge to main.

## [0.5.0](https://github.com/vig-os/devcontainer/releases/tag/0.5.0) - 2026-07-09

### Added

- **Opt-in Python starter flake template** ([#930](https://github.com/vig-os/devcontainer/issues/930))
  - `nix flake init -t github:vig-os/devcontainer#python` restores a Python package layout (`pyproject.toml`, `src/`, `tests/`) onto the now language-neutral scaffold ([#929](https://github.com/vig-os/devcontainer/issues/929)). The template uses a concrete `example_pkg` name the user renames — `nix flake init -t` does no placeholder substitution — and ships a minimal pytest dev group (no `science`/`jupyter` extras).

- **Authenticated GHCR pulls for the shipped container CI** ([#920](https://github.com/vig-os/devcontainer/issues/920))
  - The `resolve-image` action now runs an **authenticated** manifest probe: it logs in to `ghcr.io` before the probe when given a token (new optional `registry-token`/`registry-username` inputs) and no longer swallows the probe's stderr, so failures are classified into actionable `::error::` annotations that distinguish an auth/denied failure ("set the `GHCR_PULL_TOKEN` secret / grant `packages: read`") from a genuinely missing tag. The anonymous path is kept for public images.
  - Every shipped container job (`ci.yml`, `prepare-release.yml`, `promote-release.yml`, `release.yml`, `release-core.yml`, `release-publish.yml`, `sync-issues.yml`, `sync-main-to-dev.yml`, `renovate-changelog-build.yml`) gains an opt-in `credentials:` block (`username: github.actor`, `password: ${{ secrets.GHCR_PULL_TOKEN || github.token }}`) and `packages: read`, so a **private** or rate-limited image pulls without per-repo edits.
  - **Public consumers are unaffected:** with `GHCR_PULL_TOKEN` unset the expression falls back to the automatic `github.token` (never an empty password), which performs an authenticated pull of a public image successfully.
  - `docs/container-ci-quirks.md` re-framed from a "public-image limitation" note into first-class authenticated-pull documentation (secret contract, `github.token` fallback, `packages: read` requirement). Split out of the workflow audit ([#854](https://github.com/vig-os/devcontainer/issues/854)) and rides the devkit rename cycle ([#781](https://github.com/vig-os/devcontainer/issues/781)).

- **Nix-direct CI lane for direnv-mode consumers** ([#854](https://github.com/vig-os/devcontainer/issues/854))
  - `direnv` mode now scaffolds a host-native `ci.yml` overlay (`assets/workspace-direnv/`, applied like the bare overlay and keyed off the persisted `DEVKIT_MODE`): no `resolve-image`, no in-container jobs — the runner installs Nix (with the vig-os Cachix substituter, SHA-pinned `install-nix`/`cachix` actions reused from this repo's own lane) and drives the same `just sync|precommit|test` contract inside the flake dev-shell via `nix develop -c`, dropping the container-only `PREK_HOME`/`UV_PROJECT_ENVIRONMENT` env.
  - Documented boundary in `docs/MIGRATION.md` ("direnv-mode CI"): only `ci.yml` is converted for direnv mode; the other shipped workflows (`prepare-release`, `promote-release`, `release*`, `sync-issues`, `renovate-changelog-build`, `sync-main-to-dev`) stay container-based and devcontainer-mode-only until the full workflow audit rides the devkit rename ([#781](https://github.com/vig-os/devcontainer/issues/781)); the container-independent ones (`codeql`, `scorecard`, `renovate-changelog-commit`, `release-extension`) keep working in every mode.

- **Opt-in capability modules for `mkProjectShell`** ([#884](https://github.com/vig-os/devcontainer/issues/884))
  - `modules = [ "native" ]` composes curated capability modules (packages + env vars + shellHook fragments) onto the project dev-shell; contract recorded in `docs/rfcs/ADR-capability-modules.md`.
  - Zero-module shells are byte-identical to the previous builder and the published image stays base-only; `extraPackages` remains the per-repo escape hatch and wins PATH lookup.
  - `native` module ships first (`stdenv.cc`, `cmake`, `gnumake`, `pkg-config`, generic `CC`/`CXX`) — the long-term [#879](https://github.com/vig-os/devcontainer/issues/879) answer; `geant4`/`rust`/`fortran`/`root` stay ask-gated candidates.
  - Per-module flake checks (`checks.<system>.module-<name>`) plus a uv C-extension sdist smoke test (`tests/test_flake_modules.py`).

- **`.vig-os` project manifest and new `bare` delivery mode** ([#885](https://github.com/vig-os/devcontainer/issues/885))
  - `.vig-os` now persists the delivery mode and identity (`DEVKIT_MODE`, `DEVKIT_PROJECT`, `DEVKIT_ORG`, `DEVKIT_REPO`, reserved `DEVKIT_MODULES`) alongside the version pin — flat `KEY=VALUE` format unchanged, existing parsers byte-for-byte unaffected. Precedence flag/env > `.vig-os` > prompt/default, with resolved values written back, so manifest-bearing repos upgrade with `--force` and no mode/identity flags while keeping shape and names.
  - Legacy (version-only) consumers get their mode inferred conservatively from the tree shape on upgrade (wider mode on ambiguity, inference printed and persisted, never reshaping); an explicit `--mode` contradicting the persisted `DEVKIT_MODE` refuses, pointing at `--preview` and the preflight-guard flow.
  - New `bare` mode ships the standards layer only (justfiles, hooks config, `.github/` CI, `.vig-os`), prunes `.devcontainer/`/`flake.nix`/`.envrc` behind the #738/#859 pre-existence guards, and scaffolds a host-native `ci.yml` (no image resolution, no container jobs — `setup-uv` on the runner drives the same `just sync|precommit|test` contract) with `rust-just`/`prek` pinned to the toolchain versions.
  - The template `.vig-os` ships `DEVKIT_MODE` empty and the resolved mode/identity are persisted immediately after the template copy, so an upgrade aborted mid-scaffold can never persist a delivery mode the repo did not choose.

- **Preflight guard and diff preview for scaffold upgrades** ([#886](https://github.com/vig-os/devcontainer/issues/886))
  - `install.sh --force` upgrades now refuse on `main`/`dev`/`release/*`/detached HEAD and on a dirty tree, so every upgrade lands as a reviewable, revertible diff on a dedicated branch; on a protected branch with a clean tree the installer offers to create and switch to `chore/devkit-upgrade-<version>`, and non-git directories get a warn-and-confirm path. A single `--skip-preflight` flag bypasses the guard; `--smoke-test` runs and fresh installs are exempt.
  - New `--preview` mode prints the add/overwrite/preserve/delete file report (including mode-prune deletions such as the retired `.devcontainer/justfile.base`) and exits without changing any files — unlike `--dry-run`, which only prints the container command.

- **Flake-generated pre-commit hooks: one definition, consumer-extensible** ([#883](https://github.com/vig-os/devcontainer/issues/883))
  - `nix/hooks.nix` now defines the pre-commit hook set once; it renders the sandbox-pure `checks.pre-commit` gate, the committed `.pre-commit-config.yaml` + scaffold copy (drift CI-gated by `tests/test_flake_hooks.py` against `nix eval .#lib.hooksPortable` — the hand-synced triangle and the manifest transform chain are retired), and the consumer surface.
  - `mkProjectShell` gains opt-in `hooks` (toggle base hooks, per-hook `excludes`/overrides, fully custom hooks) and `hooksExcludes` (global excludes): entering the shell installs the rendered config as a repo-root symlink without ever touching `core.hooksPath` — the scaffold's `.githooks` entry point stays in charge; a preserved hand-edited YAML ([#878](https://github.com/vig-os/devcontainer/issues/878)) is never overwritten, and the zero-hooks dev-shell stays byte-identical.
  - Consumer contract and migration steps in `docs/MIGRATION.md`; the `.vig-os` manifest raw-YAML opt-out flag is tracked in [#885](https://github.com/vig-os/devcontainer/issues/885).

### Changed

- **Release-candidate dispatch gates on CI only** ([#902](https://github.com/vig-os/devcontainer/issues/902))
  - `release.yml` no longer requires the release PR to be marked ready-for-review and approved before publishing a candidate — candidate dispatch now gates on CI status only, so RCs are freely dispatchable during verification while the PR stays a draft.
  - The draft + approval gate now applies only to the **final** release (the step that burns the immutable `X.Y.Z` tag); `promote-release.yml`'s merge job still re-enforces approval before merging to `main`, so `main` and `:latest` remain fully protected.
  - Release docs, `justfile`, and `CONTRIBUTE.md` reordered accordingly: publish candidates to verify first, then mark ready and get approval before `finalize-release`.

- **Copied scaffold is language-neutral** ([#929](https://github.com/vig-os/devcontainer/issues/929))
  - The scaffold no longer assumes a Python package: `just lint/format/test/test-cov` are guarded on `pyproject.toml` and no-op (exit 0) when it is absent, mirroring the existing `sync` guard, so a non-Python repo's `just sync|precommit|test` CI contract stays green out of the box.
  - `init-workspace.sh` drops the `src/template_project` → `src/<name>` rename and test-import rewrite; `pyproject.toml` stays in the preserved set, so a consumer that brings its own is never clobbered.

### Removed

- **`pre-commit` compat shim removed from the image** ([#897](https://github.com/vig-os/devcontainer/issues/897))
  - The one-release-cycle `pre-commit → prek` shim shipped in 0.4.x ([#881](https://github.com/vig-os/devcontainer/issues/881)) is gone; `pre-commit` invocations now fail with exit 127.
  - Consumers had the 0.4.x cycle to rename invocations; the upgrade-time scan still warns with `file:line` on preserved surfaces — see `docs/MIGRATION.md` for the rename checklist (justfile recipes, repo-owned `.githooks/`, CI configs → `prek`).

- **Python package starter dropped from the scaffold** ([#929](https://github.com/vig-os/devcontainer/issues/929))
  - `pyproject.toml`, `src/template_project/`, and `tests/` are no longer shipped in `assets/workspace/`; a fresh scaffold is language-neutral. Restore a Python layout on demand with the opt-in `nix flake init -t github:vig-os/devcontainer#python` template ([#930](https://github.com/vig-os/devcontainer/issues/930)).

### Fixed

- **Upgrade preview report follows template symlinks** ([#949](https://github.com/vig-os/devcontainer/issues/949))
  - On the Nix image the baked template is a tree of symlinks into the nix store, so the `--preview`/`--force` classifier's `find … -type f` matched zero files and the OVERWRITTEN/ADDED report was always empty even though the real copy (`rsync -avL`) still overwrote them. The classifier now uses `find -L` to follow symlinks and match the copy semantics.

- **Upgrade preview no longer over-reports the baked `.venv` tree** ([#951](https://github.com/vig-os/devcontainer/issues/951))
  - Follow-up to the [#949](https://github.com/vig-os/devcontainer/issues/949) preview fix: `find -L` also descended the baked `.venv` symlink tree, so the ADDED section listed phantom `.venv/…/site-packages/*` files the real upgrade never writes. The report `find` now mirrors the static excludes the rsync copy applies (`.venv`, `docs/issues/`, `docs/pull-requests/`), so ADDED matches what the upgrade actually does.

- **Autochangelog now records grouped Renovate PRs** ([#936](https://github.com/vig-os/devcontainer/issues/936))
  - `renovate-changelog-pr` parsed the update table only when the change cell used an ASCII `->` arrow, but Renovate renders it with the Unicode arrow `→` (U+2192). Every real Renovate PR body therefore parsed to nothing; a changelog entry only appeared when the PR *title* happened to match (digest bumps, single `update X to Y`), so grouped dependency PRs were silently skipped. The change-cell parser now accepts both arrows.

- **Imageless upgrades stamp the real built tag** ([#921](https://github.com/vig-os/devcontainer/issues/921))
  - The image now bakes an authoritative built-tag record (`/root/assets/VERSION`) distinct from the template `.vig-os` pin: the release `build-image` action runs `nix build --impure` with `VIG_OS_VERSION` set to the true publish tag (RCs included), which the flake reads via `builtins.getEnv`. A plain, pure `nix build` reads `""` and falls back to the checked-in repo pin, so ordinary builds stay bit-reproducible.
  - `init-workspace.sh` reads that record when no `VIG_OS_VERSION` is forwarded, so a bare `podman run … init-workspace.sh` upgrade (no `install.sh`) now pins `.vig-os` to the image's real tag instead of the stale baked template pin ([#916](https://github.com/vig-os/devcontainer/issues/916)); an explicit `VIG_OS_VERSION` still wins, and an absent record leaves the pin untouched.

- **Template `sync` recipe no longer forces `--all-extras`** ([#892](https://github.com/vig-os/devcontainer/issues/892))
  - The shipped `justfile.project` `sync` recipe used `uv sync --all-extras --all-groups`, which forced every optional extra to install. Repos that quarantine platform-limited dependencies (e.g. cp312/cp313-only wheels) in extras got a hard `just sync` failure on the cp314 image.
  - The recipe is now parameterized (`sync *args="--all-groups"`) and defaults to `--all-groups`: dev groups stay synced while extras become opt-in via `just sync --all-extras`. The `update` recipe (which calls `just sync`) uses the new default unchanged.

- **Version-skew hardening for the shipped CI glue** ([#854](https://github.com/vig-os/devcontainer/issues/854))
  - **CI-wired skew guard:** the shipped container `ci.yml` and the new direnv `ci.yml` lint jobs now fail fast with an actionable `::error::` if the toolchain does not provide `prek`, turning an opaque `just precommit` exit 127 (old scaffold vs new image, or an image too old to ship the prek hook runner) into a one-line diagnosis.
  - **`prepare-release.yml` resolver unified:** the scaffold's forked inline-awk image resolver with a silent `latest` fallback is replaced by the shared `resolve-image` action, which hard-fails on a missing/unreadable `DEVCONTAINER_VERSION` pin.
  - **`devc-upgrade` honors the pin:** the recipe read `install.sh` from `main` regardless of the consumer's pin; it now reads `DEVCONTAINER_VERSION` from `.vig-os` and upgrades to that generation (script ref + `--version`), keeping `main`/`latest` only for unpinned repos.
  - **pipefail in every mode:** `set shell := ["bash", "-euo", "pipefail", "-c"]` moved from the devc-only `justfile.devc` to the root `justfile` (the SSoT), so direnv/bare recipes get pipefail too.
  - **`init-precommit.sh` derives its root** from the script location instead of a hard-coded `/workspace/{{SHORT_NAME}}`.
  - **Stale doc fixed:** `docs/container-ci-quirks.md` no longer describes a removed `uv run bandit` `pre-commit` hook; the private-image (unauthenticated `resolve-image` probe + missing `credentials:`) limitation is documented, with the first-class fix tracked in [#920](https://github.com/vig-os/devcontainer/issues/920).



---
---

# Comments (4)

## [Comment #1](https://github.com/vig-os/devkit/pull/938#issuecomment-4922037927) by [@c-vigo](https://github.com/c-vigo)

_Posted on July 9, 2026 at 06:02 AM_

#### 🧪 Local validation — `0.5.0-rc2` deployed to consumer repos + feature probes

Deployed `ghcr.io/vig-os/devcontainer:0.5.0-rc2` (pulled from GHCR) into isolated git worktrees of 4 real consumers and ran 4 feature probes against the `0.5.0-rc2` tag. No pushes, no issues — worktrees only.

**Consumers:** `mat` (0.4.0, devcontainer-mode, Python) · `talys` (0.4.0, both-mode, Rust) · `scitadel` (0.3.3, both-mode, Rust — biggest jump) · `vault` (0.3.5, devcontainer-mode, no-code)

##### Results

| Area | Feature | Verdict |
|---|---|---|
| #885 | `.vig-os` manifest + mode inference on upgrade | ✅ PASS — `devcontainer` inferred for mat/vault, `both` for talys/scitadel; printed, persisted, tree never reshaped |
| #885 | New `bare` delivery mode | ✅ PASS — prunes `.devcontainer/`+`flake.nix`+`.envrc`, ships host-native `ci.yml` (`setup-uv`, no `resolve-image`), pins `rust-just`/`prek` |
| #886 | Preflight guard | ✅ PASS — refuses on `main`/`dev`/`release/*`/detached/dirty (each with `--skip-preflight` hint); offers + really creates/switches `chore/devkit-upgrade-<v>`; `--skip-preflight` bypasses; fresh installs exempt |
| #886 | `--preview` no-mutation | ✅ PASS — leaves tree clean; ⚠️ **but the file report is broken — see below** |
| #921 | Imageless `init-workspace.sh` tag stamp | ✅ PASS — bare `podman run` pins the real `0.5.0-rc2` from baked `/root/assets/VERSION`; explicit `VIG_OS_VERSION` still wins |
| #929 | Language-neutral scaffold | ✅ PASS — on no-code `vault`, `just lint/format/test/test-cov/sync` all no-op `rc=0`; consumer `pyproject.toml` preserved on `mat` |
| #930 | Opt-in `#python` template | ✅ PASS — `nix flake init -t …#python` restores `pyproject`+`src/example_pkg`+`tests`, no placeholders, `dev=[pytest,pytest-cov]` only (no science/jupyter), builds+tests green in-container |
| #883 | Flake-generated hooks | ✅ PASS — single `nix/hooks.nix` fans out to `checks.pre-commit`+committed YAML+consumer surface; `test_flake_hooks.py` **13 passed** (no drift); installs as symlink, never touches `core.hooksPath`; #878 hand-edit preserved |
| #884 | Capability modules | ✅ PASS — `module-native` check builds; `test_flake_modules.py` **3 passed** (C-ext sdist compile); zero-module `drvPath` byte-identical to default; `extraPackages` wins PATH |
| #892 | `sync` no longer forces `--all-extras` | ✅ PASS — shipped `justfile.project` is `sync *args="--all-groups"` |

##### 🐛 Confirmed bug — `--preview`/`--force` file report is blank on the Nix image

The pre-upgrade **"which files will be OVERWRITTEN / ADDED / PRESERVED"** report is **always empty** on the `0.5.0` Nix image, so `--preview` (and the interactive `--force` confirmation) reports *"No existing files would be overwritten. No new files would be added."* immediately before an upgrade that in fact rewrites **~21 managed files** (verified on `talys`).

**Root cause:** the classifier at `assets/init-workspace.sh:534` uses `find "$TEMPLATE_DIR" -type f`, but on the Nix image `/root/assets/workspace` is **998 store symlinks, 0 regular files** — so `-type f` matches nothing. The actual copy uses `rsync -avL` (follows symlinks) and works fine; only the *report* is blind. The `DELETED` section stays accurate (it scans the workspace, not the template).

```
### in the rc2 image:
find    /root/assets/workspace -type f  →    0
find -L /root/assets/workspace -type f  → 1872
```

**Suggested fix:** add `-L` (follow symlinks) to that `find`, e.g. `find -L "$TEMPLATE_DIR" -type f …`. This defeats the purpose of the #886 reviewable-upgrade preview, so worth fixing before `0.5.0` final. Reproduced 3 independent ways (in-flight, clean-`talys` repro, controlled tree-divergence). Tracked in the follow-up issue below.

##### ✅ Clarified as working-as-designed (not a bug)

- **#854 both-mode CI:** `both`-mode consumers (`talys`, `scitadel`) keep the **container** `ci.yml`, not the nix-direct one. This is correct — the nix-direct lane is deliberately `direnv`-only (a `both` repo *has* a `.devcontainer/`, so container CI works). Confirmed against `init-workspace.sh` (`both) : # keep everything`) and `docs/MIGRATION.md` ("still work unchanged in `both`/`devcontainer` mode").

##### ⚠️ Expected migration debt (#897 shim removal — correctly warned)

The `pre-commit → prek` shim is gone, so preserved consumer files still calling `pre-commit` now exit 127. The installer **warns with `file:line`**: `mat` (`ci-container.yml:119`), `vault` (`justfile.project:31`). Minor gap: `scitadel`'s consumer `flake.nix` lists `pre-commit` as a dev-shell *package* (not an invocation), which the scan doesn't flag.

##### 🔧 Test-harness caveats (not rc2 defects)

Testing via git worktrees means each `.git` is a *file* pointing outside the container mount, so in-container `git` couldn't resolve the repo. This (only) degraded three sub-checks, all of which work on a normal full clone: in-container `just precommit`, the installer's template-vs-preserved *diff display*, and git-remote identity resolution (manifests got `DEVKIT_REPO=OWNER/REPO`). The imageless probe with a full mount + `GITHUB_REPOSITORY` resolved identity correctly (`vig-os/probe3`), confirming the mechanism.

---

**Bottom line:** rc2 is healthy across mode inference, preflight, bare mode, imageless stamping, language-neutral scaffold, the python template, flake hooks, and capability modules. **One fix recommended before final:** the `find -type f` symlink blindness that blanks the #886 upgrade preview report.


---

## [Comment #2](https://github.com/vig-os/devkit/pull/938#issuecomment-4923265997) by [@c-vigo](https://github.com/c-vigo)

_Posted on July 9, 2026 at 08:45 AM_

### 🧪 Local validation — `0.5.0-rc3` (re-run of the full battery after the #949 fix)

Same procedure as the rc2 round: pulled `ghcr.io/vig-os/devcontainer:0.5.0-rc3`, upgraded 4 real consumers in isolated worktrees, and re-ran the 4 feature probes against the `0.5.0-rc3` tag. No pushes/issues.

**Consumers:** `mat` (0.4.0, devcontainer) · `talys` (0.4.0, both, Rust) · `scitadel` (0.3.3, both, Rust — biggest jump) · `vault` (0.3.5, devcontainer, no-code).

#### ✅ Headline: the #949 preview-report fix works

On rc2 the upgrade file report was blank on the Nix image. On rc3 the **"will be OVERWRITTEN"** section is populated on every target, and preview still mutates nothing:

| Target | OVERWRITTEN listed | `git status` after preview |
|---|---|---|
| mat | 48 files | clean |
| talys | 93 files | clean |
| scitadel | 48 files (+1 DELETED: `justfile.base`) | clean |
| vault | 44 files | clean |
| B4 seeded repo | 3 files (incl. the stale `justfile`) + 1 ADDED (`.typos.toml`) | clean |

Root cause of the rc2 blindness re-confirmed in the image: `/root/assets/workspace` is a store-symlink tree, so `find -type f` → 0, `find -L -type f` → 1872. The `find -L` fix is baked into the rc3 image. The safety-critical "what will I overwrite" report is now accurate.

#### ✅ Regression: everything else still green

- **#885 mode inference** — `devcontainer` for mat/vault, `both` for talys/scitadel; persisted, no reshape.
- **#885 bare mode** — prunes container/flake, host-native `ci.yml` (`setup-uv`, no `resolve-image`), `DEVKIT_MODE=bare`.
- **#921 imageless stamp** — pins the real `0.5.0-rc3` from baked `/root/assets/VERSION`.
- **#929 language-neutral** — no-code `vault`: `just lint/test/sync` all `rc=0`; `mat` `pyproject.toml` preserved.
- **#930 python template** — layout restored, `dev=[pytest,pytest-cov]` only, builds+tests green in-container.
- **#883 flake hooks** — `test_flake_hooks.py` **13 passed** (no drift); `checks.pre-commit` builds; symlink install, no `core.hooksPath`.
- **#884 capability modules** — `module-native` builds; sdist C-ext smoke **3 passed**; zero-module `drvPath` == default.
- **#892** `sync *args="--all-groups"` (no forced `--all-extras`). **#897** prek-shim removal warns with `file:line` on preserved `pre-commit` invocations. **#854** `both`-mode keeps container `ci.yml` (nix-direct is `direnv`-only — by design).
- **#886 preflight** — full refusal matrix holds (protected branches offer + create/switch `chore/devkit-upgrade-0.5.0-rc3`; dirty/detached refuse; `--skip-preflight` bypasses; fresh installs exempt); `--preview` vs `--dry-run` distinct.

#### 🐛 New follow-up — the `find -L` fix over-reports the ADDED section (report-only, no data risk)

The same fix now inflates the **ADDED** list: the report walk `find -L "$TEMPLATE_DIR" -type f ! -path "*/.git/*"` at `assets/init-workspace.sh:534` **lacks the `--exclude=.venv`** that the real copy (`rsync -avL --exclude='.venv'`, lines 669/717) has, and the image template carries a `.venv`. Result across all targets: **~1763 of ~1867 ADDED entries are phantom `.venv/.../site-packages/*`** files the upgrade never writes.

- Impact: **cosmetic / report-only.** The real `--force` upgrade excludes `.venv` (verified: no `.venv`/site-packages land in any consumer tree), and the safety-critical OVERWRITTEN section is correct. But the ADDED list is unusable as-is.
- Fix: mirror the rsync excludes in the report `find` — add `! -path "*/.venv/*"` (and, for full parity, `! -path "*/docs/issues/*" ! -path "*/docs/pull-requests/*"`). One-liner.
- **Not rc3-blocking** (no data risk), but worth closing before `0.5.0` final since the preview report is a headline feature.

#### Minor notes (not blockers)
- `init-workspace.sh` raw-invoke path (no `install.sh`) derived `DEVKIT_ORG=vigOS/devc` from `GITHUB_REPOSITORY` with no `ORG_NAME`; the `install.sh` path resolves org correctly. Pre-existing.
- `scitadel` carries a stale `.cursor/worktrees.json` calling `pre-commit install` (exit-127 under prek-only); the `.cursor/`→`.claude/` move doesn't remove the old tree. Pre-existing consumer content.
- `DEVKIT_REPO=OWNER/REPO` on the isolated worktrees is a test-harness artifact (no `origin`); resolves on a full clone.

**Bottom line:** rc3 fixes the #949 preview blindness and regresses nothing. One low-severity, report-only follow-up remains (ADDED `.venv` over-count from the same `find -L`), fixable with a one-line exclude.


---

## [Comment #3](https://github.com/vig-os/devkit/pull/938#issuecomment-4924107499) by [@c-vigo](https://github.com/c-vigo)

_Posted on July 9, 2026 at 10:31 AM_

### 🧪 Local validation — `0.5.0-rc4` (verifies the #951 fix) + smoke test green

Third full battery (same procedure): pulled `ghcr.io/vig-os/devcontainer:0.5.0-rc4`, upgraded the 4 consumers in isolated worktrees, re-ran the 4 feature probes against the `0.5.0-rc4` tag. rc4 = rc3 + the #951 fix. No pushes/issues.

#### ✅ Headline: the #951 ADDED over-report is fixed (and #949 still holds)

The rc3 side-effect (preview ADDED bloated with ~1763 phantom `.venv/site-packages` entries) is gone; OVERWRITTEN stays populated:

| Probe | ADDED | `.venv`/site-packages | OVERWRITTEN (#949) |
|---|---|---|---|
| mat | 51 | **0** | 48 |
| talys | 4 | **0** | 93 |
| scitadel | 52 | **0** | 48 |
| vault | 56 | **0** | 44 |
| B4 (seeded) | 108 | **0** | 1 |

B4 pinned the mechanism: the report walk collapses from **1872 → 109** real files (matching the image), exactly the `.venv` exclusion. Preview still mutates nothing.

#### ✅ Regression + smoke — all green

- **B1** python template 6/6 · **B2** capability modules 5/5 · **B3** flake hooks `13 passed` (no drift) — all stable across rc2/rc3/rc4.
- Mode inference (`devcontainer`/`both`), bare mode, imageless stamp (`0.5.0-rc4`), `#929`/`#892`/`#897`/`#854`, and the full **#886 preflight matrix** all as expected.
- **Smoke test green** (`vig-os/devcontainer-smoke-test`): rc4 deployed → PR-to-`dev` CI passed & merged → prepare-release → release workflow → required checks, every stage `success`.

#### Minor, non-blocking follow-ups (distinct from #951)

- **Preview↔apply nested-doc gap** — preview lists a few nested `README.md`/`CHANGELOG.md` scaffold docs as ADDED (mat 3, talys 2) that the apply step skips by basename (the rule protecting the consumer's *root* README/CHANGELOG over-matches nested devkit docs). Consistent rc3→rc4; possibly those nested docs never ship. Worth a separate issue.
- **`DEVKIT_ORG=vigOS/devc`** on the imageless raw `init-workspace.sh` path (the `install.sh` path resolves `vigOS` correctly). Recurring, minor.

**Bottom line:** both cycle fixes (#949 preview blindness, #951 `.venv` over-report) are confirmed on real consumers and through the smoke pipeline; nothing regressed. rc4 looks ready to finalize.


---

## [Comment #4](https://github.com/vig-os/devkit/pull/938#issuecomment-4924994639) by [@c-vigo](https://github.com/c-vigo)

_Posted on July 9, 2026 at 12:19 PM_

### Smoke test + promotion — corrected account

**Correction to my earlier note:** `promote-release` does **not** treat the smoke test as advisory — it hard-gates on it. Validate step `Verify downstream published final release` (promote-release.yml) requires `devcontainer-smoke-test` to have a **published (non-draft, non-prerelease)** release for the tag, and fails closed otherwise.

What happened:
- Finalize published `0.5.0` (immutable tag + draft release + GHCR image) ✅.
- The smoke pipeline verified deploy → PR → prepare-release → **release** (produced `0.5.0` draft + tag + finalized changelog in the smoke repo) ✅.
- But the smoke listener's `Trigger and wait for promote-release` step was **skipped** — its upstream `Trigger and wait for release workflow` first lost a race to the GitHub Actions runner starvation, and a re-run then hit the `Verify CHANGELOG has TBD entry` guard (the prior successful release had already finalized `## [0.5.0] - TBD` → the dated heading). So the smoke `0.5.0` stayed a **draft**, and the first promote attempt failed exactly at the downstream-published gate.

**Unblock:** published the existing smoke draft release (`gh release edit 0.5.0 --draft=false` — the same operation the smoke's promote performs), satisfying the gate, then re-ran `promote-release` for `0.5.0`.

Follow-up worth filing on `devcontainer-smoke-test`: make `Trigger and wait for release workflow` auto-retry an infra-cancelled release run instead of hard-failing (that's the whole chain-reaction here).


---
---

## Commits

### Commit 1: [5024144](https://github.com/vig-os/devkit/commit/50241440f3b988bc6a8438397a2c5c18a0d311b1) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:04 PM
test: assert pre-commit is absent from the image again, 27 files modified (tests/test_image.py)

### Commit 2: [88d3894](https://github.com/vig-os/devkit/commit/88d3894ed38cfe35b12b7c6f4b6e8cbb0bf6dc03) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:04 PM
docs(rfcs): record the capability-module contract design note, 116 files modified (docs/rfcs/ADR-capability-modules.md)

### Commit 3: [c8640e1](https://github.com/vig-os/devkit/commit/c8640e1dce7c0f7d3ea2cd3b388dee5463ac4d68) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:05 PM
chore(image): remove the pre-commit compat shim, 13 files modified (flake.nix)

### Commit 4: [06863ca](https://github.com/vig-os/devkit/commit/06863cab2982d6eb308393339fbe4eb021691b7a) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:06 PM
test(flake): capability-module devshell contract + native sdist smoke, 214 files modified (tests/fixtures/native_ext/native_ext.c, tests/fixtures/native_ext/pyproject.toml, tests/fixtures/native_ext/setup.py, tests/test_flake_modules.py)

### Commit 5: [3829382](https://github.com/vig-os/devkit/commit/3829382e8ffa87bb8ad779c9298d5421f4d0d315) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:06 PM
docs: record the pre-commit shim removal in migration guide and changelog, 16 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md)

### Commit 6: [68540e7](https://github.com/vig-os/devkit/commit/68540e7c64ec2968c56717262d7d3a026a00e96f) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:11 PM
feat(flake): compose opt-in capability modules in mkProjectShell, 187 files modified (flake.nix, nix/modules/default.nix, nix/modules/native.nix, tests/test_flake_modules.py)

### Commit 7: [e5d6af4](https://github.com/vig-os/devkit/commit/e5d6af41b78d2ce5b47da0f70d420c1f3324de14) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:12 PM
docs: capability modules in MIGRATION/NIX + changelog entry, 90 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md, docs/NIX.md)

### Commit 8: [3a98b12](https://github.com/vig-os/devkit/commit/3a98b1202e302624803571ca1345468d24989401) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:13 PM
style: apply ruff-format/taplo to module test and fixture, 8 files modified (tests/fixtures/native_ext/pyproject.toml, tests/test_flake_modules.py)

### Commit 9: [7299007](https://github.com/vig-os/devkit/commit/729900798daa10d3294f294a0e769529421e149b) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:14 PM
docs(release): document that the empty Unreleased stays on release branches, 16 files modified (docs/RELEASE_CYCLE.md)

### Commit 10: [38cfec1](https://github.com/vig-os/devkit/commit/38cfec1d6569288a5a3cbc1b32bc0c30703bff3e) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:19 PM
chore(image): remove the pre-commit compat shim (one-cycle deprecation ends) (#899), 56 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md, flake.nix, tests/test_image.py)

### Commit 11: [483ca39](https://github.com/vig-os/devkit/commit/483ca39740d7683150f8487a0e021a32241ee3d6) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:11 PM
test: add upgrade preflight guard and --preview specs, 354 files modified (tests/bats/init-workspace.bats, tests/bats/install.bats)

### Commit 12: [bae89bb](https://github.com/vig-os/devkit/commit/bae89bb6bec2067bbe9026d2d56e8f8e18bf871a) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:13 PM
feat(setup): gate --force upgrades behind a host-side preflight guard, 148 files modified (install.sh)

### Commit 13: [8e36f5f](https://github.com/vig-os/devkit/commit/8e36f5fceab07131b8cd6a46b71559d5ca3921eb) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:16 PM
feat(setup): add --preview report-only mode to init-workspace, 142 files modified (assets/init-workspace.sh)

### Commit 14: [da824ee](https://github.com/vig-os/devkit/commit/da824ee07162869691b5c1db2d7fde903a3a3eb0) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:18 PM
docs(setup): document upgrade preflight guard and --preview, 47 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/.devcontainer/justfile.devc, docs/MIGRATION.md)

### Commit 15: [c1d9991](https://github.com/vig-os/devkit/commit/c1d99914ff8908d62a7990462fb9b8f2fb0a4e07) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:25 PM
ci: run flake module tests in the nix lane, 9 files modified (.github/workflows/ci.yml)

### Commit 16: [fdbe3d7](https://github.com/vig-os/devkit/commit/fdbe3d7c172f085e43b2e13ec3083715aeb907e9) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:20 PM
test: run the --force dry-run pytest against a git fixture branch, 35 files modified (tests/test_utils.py)

### Commit 17: [d4554c2](https://github.com/vig-os/devkit/commit/d4554c2e843fae30b369c831d1d75bf25a1bc925) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:26 PM
docs(release): document that the empty Unreleased stays on release branches (#900), 16 files modified (docs/RELEASE_CYCLE.md)

### Commit 18: [6ea7d42](https://github.com/vig-os/devkit/commit/6ea7d42d9f577e68b271b6a33c047524bbd0bdb6) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:31 PM
ci(release): gate RC dispatch on CI only, defer draft/approval to final, 152 files modified (.github/workflows/release.yml, CHANGELOG.md, CONTRIBUTE.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/RELEASE_CYCLE.md, docs/templates/CONTRIBUTE.md.j2, justfile)

### Commit 19: [90eb682](https://github.com/vig-os/devkit/commit/90eb682cc642bc411891c5881617eb33d003c82f) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:38 PM
feat(flake): opt-in capability modules on mkProjectShell (native first) (#901), 608 files modified

### Commit 20: [9c0fa92](https://github.com/vig-os/devkit/commit/9c0fa925081db2f8f1513b22e4e7750f1372b26f) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:42 PM
Merge remote-tracking branch 'origin/dev' into feature/886-upgrade-preflight-guard, 680 files modified

### Commit 21: [251d047](https://github.com/vig-os/devkit/commit/251d047892029fe837dede683ddc80f998007093) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:51 PM
test: add fidelity gate for the flake-defined pre-commit hook set, 293 files modified (tests/test_flake_hooks.py)

### Commit 22: [f78299e](https://github.com/vig-os/devkit/commit/f78299eaafc0baee3e53d9a8ed2604f70a24f290) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:55 PM
feat(setup): preflight guard for scaffold upgrades and --preview diff report (#903), 726 files modified

### Commit 23: [3a71f2d](https://github.com/vig-os/devkit/commit/3a71f2d9b22ed735b5ecd14139842ce0e42c8416) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 01:59 PM
feat(flake): one hook-set definition rendering gate, runner YAML and consumer surface, 985 files modified (flake.nix, nix/hooks.nix, tests/test_flake_hooks.py)

### Commit 24: [d618807](https://github.com/vig-os/devkit/commit/d6188076b7f897e5f4b04e2b595ed00abb25176e) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:01 PM
ci(release): gate RC dispatch on CI only, defer draft/approval to final (#907), 152 files modified (.github/workflows/release.yml, CHANGELOG.md, CONTRIBUTE.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/RELEASE_CYCLE.md, docs/templates/CONTRIBUTE.md.j2, justfile)

### Commit 25: [3b9e070](https://github.com/vig-os/devkit/commit/3b9e0702798f0dd9fa00a4e39783efe870fbb27d) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:05 PM
docs: record the one-definition hook model, consumer contract and scaffold example, 188 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/flake.nix, docs/MIGRATION.md, docs/NIX.md, scripts/manifest.toml)

### Commit 26: [4c52d65](https://github.com/vig-os/devkit/commit/4c52d65ec9aaaa50c59da653ac2fd5dbcdc18839) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:09 PM
test(workspace): cover .vig-os manifest read, write-back, and mode refusal, 249 files modified (tests/bats/init-workspace.bats, tests/bats/install.bats)

### Commit 27: [b4dc98f](https://github.com/vig-os/devkit/commit/b4dc98f8035c446d2d493cd352aba78a6db06949) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:14 PM
feat(workspace): persist delivery mode and identity in the .vig-os manifest, 268 files modified (assets/init-workspace.sh, assets/workspace/.vig-os, install.sh)

### Commit 28: [2072ea4](https://github.com/vig-os/devkit/commit/2072ea40e632aab6b688de0395a174e5af172960) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:15 PM
test(workspace): cover conservative legacy delivery-mode inference, 104 files modified (tests/bats/init-workspace.bats)

### Commit 29: [57faa6c](https://github.com/vig-os/devkit/commit/57faa6cba5139cae6893a03b4307644f4374914f) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:16 PM
feat(workspace): infer delivery mode for legacy consumers on upgrade, 55 files modified (assets/init-workspace.sh)

### Commit 30: [b58701b](https://github.com/vig-os/devkit/commit/b58701b20300e9083da6c15a6c2e3749adc82159) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:18 PM
test(workspace): cover the bare delivery mode and host-native CI scaffold, 135 files modified (tests/bats/init-workspace.bats, tests/bats/install.bats)

### Commit 31: [54d135e](https://github.com/vig-os/devkit/commit/54d135eca3d90de740dab0f25ba8b895fc921d3c) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:22 PM
feat(workspace): add bare delivery mode with host-native CI workflow, 259 files modified (assets/init-workspace.sh, assets/workspace-bare/.github/workflows/ci.yml, install.sh)

### Commit 32: [1087882](https://github.com/vig-os/devkit/commit/108788294c9fcdcfc1b6a7d0799021795fed19ff) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:22 PM
ci(security): collapse Trivy runs and reuse warm cache in PR scan, 33 files modified (.github/workflows/ci.yml)

### Commit 33: [e0cbdc6](https://github.com/vig-os/devkit/commit/e0cbdc6f99b5032260472d83e6ce53c4f0028e7d) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:23 PM
Merge remote-tracking branch 'origin/dev' into feature/883-flake-generated-hooks, 882 files modified

### Commit 34: [7bcaba3](https://github.com/vig-os/devkit/commit/7bcaba39f62127631b6401d6cc41d2e08b5d1bad) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:24 PM
test(ci): prove .vig-os manifest keys are invisible to existing parsers, 192 files modified (tests/bats/manifest-parsers.bats, tests/test_resolve_image_manifest.py)

### Commit 35: [f6249bc](https://github.com/vig-os/devkit/commit/f6249bc86d301757233d2c42884e705069b61b83) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:25 PM
test: assert opted-in shellHook never rewires core.hooksPath; guard stray top-level YAML keys, 87 files modified (tests/test_flake_hooks.py)

### Commit 36: [ddeea11](https://github.com/vig-os/devkit/commit/ddeea11201d5b54a21f05c6122f7ad54eb227d59) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:25 PM
docs(workspace): document the .vig-os manifest, bare mode, and changelog entry, 67 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md)

### Commit 37: [0ceb483](https://github.com/vig-os/devkit/commit/0ceb483cb6a6e87bb8d1189c2f95cd8553d36746) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:27 PM
test(workspace): silence SC2016 on the bare user-conf grep assertion, 1 file modified (tests/bats/install.bats)

### Commit 38: [d6edbd1](https://github.com/vig-os/devkit/commit/d6edbd127cc6ecf3c73a694b0343fdd10c206ca9) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:27 PM
fix(nix): install generated hook config without rewiring core.hooksPath, 57 files modified (flake.nix)

### Commit 39: [a696989](https://github.com/vig-os/devkit/commit/a6969896f6083cf5392603d45b051da8d68e6b23) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:28 PM
docs: record that opt-in hook generation never rewires core.hooksPath, 15 files modified (docs/MIGRATION.md)

### Commit 40: [ab66eab](https://github.com/vig-os/devkit/commit/ab66eab36cb56359505b4ce2bd03d1bd25c36f7b) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:28 PM
style: ruff-format test_flake_hooks, 4 files modified (tests/test_flake_hooks.py)

### Commit 41: [45c55fe](https://github.com/vig-os/devkit/commit/45c55fe9c82576621f2988f809db23f075ba7bfb) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:38 PM
ci(security): speed up PR Trivy scan (collapse runs + reuse warm cache) (#910), 33 files modified (.github/workflows/ci.yml)

### Commit 42: [9432c91](https://github.com/vig-os/devkit/commit/9432c911d1a8382a0e77c11da634be759305af2c) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:46 PM
style(nix): merge repeated config keys into one attrset for statix, 14 files modified (flake.nix)

### Commit 43: [785f4ee](https://github.com/vig-os/devkit/commit/785f4eeb1a8a439f0e938d3c59b3cd429c5ce2a4) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:48 PM
test(workspace): expose the torn-manifest window that poisons DEVKIT_MODE, 57 files modified (tests/bats/init-workspace.bats)

### Commit 44: [11ecfa5](https://github.com/vig-os/devkit/commit/11ecfa5dc238bb55b793ab5e689362ab2f102431) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:52 PM
fix(workspace): close the torn-manifest window on aborted upgrades, 19 files modified (assets/init-workspace.sh, assets/workspace/.vig-os)

### Commit 45: [c02e038](https://github.com/vig-os/devkit/commit/c02e038a574222c5660ccf3d02645586a7b057b4) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:53 PM
ci(workspace): pin rust-just and prek in the bare-mode ci.yml, 10 files modified (assets/workspace-bare/.github/workflows/ci.yml)

### Commit 46: [0670e09](https://github.com/vig-os/devkit/commit/0670e0946d822a4c064129986490a635d4d972cf) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:53 PM
docs(workspace): flag env-var prompt suppression and record review fixes, 11 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md)

### Commit 47: [2355daf](https://github.com/vig-os/devkit/commit/2355daffd2ba97650c78a8b4fa67907b5f3c2d0d) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 02:56 PM
feat(flake): generate .pre-commit-config.yaml from one flake hook definition, consumer-extensible (#908), 1491 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/flake.nix, docs/MIGRATION.md, docs/NIX.md, flake.nix, nix/hooks.nix, scripts/manifest.toml, tests/test_flake_hooks.py)

### Commit 48: [841c742](https://github.com/vig-os/devkit/commit/841c7423e94aa6c58b7c327bcacd3f7d1a4961f6) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 03:03 PM
test(workspace): isolate torn-state case from the CI GITHUB_REPOSITORY env, 4 files modified (tests/bats/init-workspace.bats)

### Commit 49: [55bbac6](https://github.com/vig-os/devkit/commit/55bbac601401c852a03d8015d94576997b985b40) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 03:13 PM
feat(workspace): .vig-os declarative manifest with persisted mode/identity and bare mode (#911), 1399 files modified

### Commit 50: [e832943](https://github.com/vig-os/devkit/commit/e832943b2a383a808898868864f62a3ad332ad7d) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 04:02 PM
feat(workspace): nix-direct direnv CI lane + version-skew hardening, 550 files modified

### Commit 51: [0560b3c](https://github.com/vig-os/devkit/commit/0560b3c01b180229c651a16e104b8d45bf928563) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 04:04 PM
fix(workspace): run prek skew guard via bash -c in direnv ci.yml, 2 files modified (assets/workspace-direnv/.github/workflows/ci.yml)

### Commit 52: [dd0b1f1](https://github.com/vig-os/devkit/commit/dd0b1f1fc7d35703b079b35100752bfc630ba00c) by [c-vigo](https://github.com/c-vigo) on July 7, 2026 at 04:17 PM
feat(workspace): nix-direct direnv CI lane + version-skew hardening (#923), 550 files modified

### Commit 53: [312b15c](https://github.com/vig-os/devkit/commit/312b15c84ed46671249bbf408aa5372a58f99610) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 07:58 AM
fix(workspace): parameterize sync recipe so extras are opt-in, 16 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/justfile.project, tests/test_integration.py)

### Commit 54: [49a51fd](https://github.com/vig-os/devkit/commit/49a51fde9e1e10ea1f9432745f50da287ed1c443) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 08:08 AM
feat(ci): authenticated GHCR probe and opt-in private-image credentials, 412 files modified

### Commit 55: [e6ea169](https://github.com/vig-os/devkit/commit/e6ea1693a9dab22e8d6ae6609b9d9b85b42482cf) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 08:19 AM
test(workspace): update recipe assertions for parameterized sync header, 8 files modified (tests/bats/init-workspace.bats, tests/test_integration.py)

### Commit 56: [8a46aea](https://github.com/vig-os/devkit/commit/8a46aea96908895597598ce676269e6405768301) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 11:08 AM
style(ci): apply ruff formatting to resolve-image probe test, 8 files modified (tests/test_resolve_image_probe.py)

### Commit 57: [81b9764](https://github.com/vig-os/devkit/commit/81b976415dd8a806fa9c61d35ff3caea10eaec4d) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 11:14 AM
fix(image): bake authoritative built-tag record for imageless upgrades, 140 files modified (.github/actions/build-image/action.yml, CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/bats/init-workspace.bats)

### Commit 58: [f179dcb](https://github.com/vig-os/devkit/commit/f179dcb6bbc35571b174fea5dcb37b5186fdb515) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 11:26 AM
fix(workspace): parameterize sync recipe so extras are opt-in (#924), 24 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, assets/workspace/justfile.project, tests/bats/init-workspace.bats, tests/test_integration.py)

### Commit 59: [b0649e7](https://github.com/vig-os/devkit/commit/b0649e722d539af3ab2ef6a53c46a0672b3c1cdc) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 11:27 AM
feat(ci): authenticated GHCR probe and opt-in private-image credentials (#925), 408 files modified

### Commit 60: [34c2cfa](https://github.com/vig-os/devkit/commit/34c2cfa03c976657df568060b66c9fa7d1fc0a07) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 11:30 AM
Merge branch 'dev' into bugfix/921-image-version-stamp, 432 files modified

### Commit 61: [115eeeb](https://github.com/vig-os/devkit/commit/115eeeb2bb7568d08a75ea71a2862f4dce1b8e61) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 11:38 AM
fix(image): bake authoritative built-tag record for imageless upgrades (#926), 140 files modified (.github/actions/build-image/action.yml, CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, flake.nix, tests/bats/init-workspace.bats)

### Commit 62: [1bceecd](https://github.com/vig-os/devkit/commit/1bceecdbd12a2ca1c8ee38d38e5b7b93e0914200) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 12:34 PM
test(workspace): expect language-neutral scaffold, no Python starter, 34 files modified (tests/bats/init-workspace.bats)

### Commit 63: [a026454](https://github.com/vig-os/devkit/commit/a026454e245eef517a42b78ee8be0b8183628582) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 12:37 PM
feat(workspace): make the copied scaffold language-neutral, 84 files modified

### Commit 64: [4cd90ee](https://github.com/vig-os/devkit/commit/4cd90ee62f56fffbe9a050c744aad10bc9f5e722) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 12:38 PM
test(workspace): update image tests for language-neutral scaffold, 49 files modified (tests/test_image.py, tests/test_install_script.py, tests/test_integration.py)

### Commit 65: [3f32086](https://github.com/vig-os/devkit/commit/3f320865002c0fe6921258900983c241500e818c) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 12:42 PM
ci(security): scope packages:write to the job in nix-image workflow, 7 files modified (.github/workflows/nix-image.yml)

### Commit 66: [12927e8](https://github.com/vig-os/devkit/commit/12927e8cc4f410ec26c81f4a19cfa85187f0fefd) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 12:45 PM
docs(workspace): document the language-neutral scaffold, 42 files modified (CHANGELOG.md, README.md, assets/workspace/.devcontainer/CHANGELOG.md, docs/MIGRATION.md, docs/templates/README.md.j2, scripts/manifest.toml)

### Commit 67: [b9eab46](https://github.com/vig-os/devkit/commit/b9eab46717e8e90a5c5bdf753a7bebf285875bcc) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 12:48 PM
feat(flake): opt-in Python starter template (nix flake init -t ...#python), 83 files modified

### Commit 68: [5c72b37](https://github.com/vig-os/devkit/commit/5c72b3754c265553065131600ae23a9fe4a20138) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 01:24 PM
Merge remote-tracking branch 'origin/dev' into chore/sync-main-to-dev-13-1, 5575 files modified

### Commit 69: [c8fba0d](https://github.com/vig-os/devkit/commit/c8fba0d04d4b3b645e4fd8d7676eeed09b4b041d) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 01:35 PM
chore: sync dev with main (conflicts) (#928), 466 files modified

### Commit 70: [8328f27](https://github.com/vig-os/devkit/commit/8328f27f247ee234b7bd185ae5e525d668ad0cfe) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 01:37 PM
Merge remote-tracking branch 'origin/dev' into feature/929-language-neutral-scaffold, 466 files modified

### Commit 71: [f621fb0](https://github.com/vig-os/devkit/commit/f621fb042d70321d251bda4a7482241f8d988d65) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 01:42 PM
test(workspace): just test no-ops on a language-neutral base scaffold, 17 files modified (tests/test_integration.py)

### Commit 72: [e2ca769](https://github.com/vig-os/devkit/commit/e2ca76996960eb58af13a3b2b8dfde213af05046) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 01:43 PM
ci(renovate): exempt requires-python from the pep621 pin rule, 6 files modified (assets/workspace/.github/renovate-default.json)

### Commit 73: [6097c9c](https://github.com/vig-os/devkit/commit/6097c9c787f4297379946056295339a5bdc67302) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 01:51 PM
test: cover Unicode arrow in Renovate changelog parser, 31 files modified (packages/vig-utils/tests/test_renovate_changelog_pr.py)

### Commit 74: [daf2e54](https://github.com/vig-os/devkit/commit/daf2e54eee88e2e411692e78fa2e795bae612807) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 01:52 PM
fix(changelog): parse Renovate change cells with the Unicode arrow, 13 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, packages/vig-utils/src/vig_utils/renovate_changelog_pr.py)

### Commit 75: [7710e00](https://github.com/vig-os/devkit/commit/7710e00cbe14bedf5fbf65eb57bb6b593eb8b44f) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 01:59 PM
ci(renovate): exempt requires-python from the pep621 pin rule (#935), 6 files modified (assets/workspace/.github/renovate-default.json)

### Commit 76: [f5e9f5f](https://github.com/vig-os/devkit/commit/f5e9f5ff171ea3a90f00b1c4802c2bf933beb241) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 02:03 PM
test(workspace): scaffold ships no src/ or tests/ directory, 28 files modified (tests/test_install_script.py)

### Commit 77: [b87a781](https://github.com/vig-os/devkit/commit/b87a78168e6ed88b81e04f3330d0d8c974e2b8e5) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 02:03 PM
fix(changelog): parse Renovate change cells with the Unicode arrow (#937), 44 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, packages/vig-utils/src/vig_utils/renovate_changelog_pr.py, packages/vig-utils/tests/test_renovate_changelog_pr.py)

### Commit 78: [bcb0863](https://github.com/vig-os/devkit/commit/bcb08633d922a100a07e5e4b9e70693c2af084b4) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 02:13 PM
feat(workspace): language-neutral scaffold + opt-in Python flake template (#933), 337 files modified

### Commit 79: [214eca4](https://github.com/vig-os/devkit/commit/214eca4726581504883c4aac209ad2a351457418) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 02:57 PM
ci(security): scope packages:write to the job in nix-image workflow (#932), 7 files modified (.github/workflows/nix-image.yml)

### Commit 80: [d7157c8](https://github.com/vig-os/devkit/commit/d7157c890c14f64fceb3bb767ec262db68959eec) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on July 8, 2026 at 03:01 PM
chore: freeze changelog for release 0.5.0, 36 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 81: [7ef3b97](https://github.com/vig-os/devkit/commit/7ef3b971e34c3191a64e1b9ae99740fdf670c2c9) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 03:58 PM
fix(security): except unfixed curl 8.20.0 CVEs pending upstream fix, 40 files modified (.vulnixignore)

### Commit 82: [72dae06](https://github.com/vig-os/devkit/commit/72dae066fa313f8dbcac8df2a4dca19883d25380) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 04:10 PM
fix(security): except unfixed curl 8.20.0 CVEs blocking 0.5.0 RC (#942), 40 files modified (.vulnixignore)

### Commit 83: [2c66162](https://github.com/vig-os/devkit/commit/2c661625044c298ba014674fbee889d710c681b9) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 05:20 PM
fix: install template pytest via dependency-group, 6 files modified (templates/python/pyproject.toml)

### Commit 84: [6becb11](https://github.com/vig-os/devkit/commit/6becb11a9320187aadcfa50d04c68a7e8751bf3f) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 05:31 PM
fix: install #python template pytest via dependency-group (#945), 6 files modified (templates/python/pyproject.toml)

### Commit 85: [e8dfbc1](https://github.com/vig-os/devkit/commit/e8dfbc1d8af9fefef2fa21ff300db6a6b1d4319e) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 08:47 PM
fix: grant packages: read on reusable-calling release jobs, 2 files modified (assets/workspace/.github/workflows/release.yml)

### Commit 86: [7208512](https://github.com/vig-os/devkit/commit/72085123e4d528082d50def96debcb591deb3c81) by [c-vigo](https://github.com/c-vigo) on July 8, 2026 at 08:57 PM
fix: grant packages: read on reusable-calling release jobs (#948), 2 files modified (assets/workspace/.github/workflows/release.yml)

### Commit 87: [09f6cf1](https://github.com/vig-os/devkit/commit/09f6cf17e370dc59adfb236229cacacd09dc5d50) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 06:08 AM
test: cover upgrade preview report over symlinked templates, 55 files modified (tests/bats/init-workspace.bats)

### Commit 88: [f5040d5](https://github.com/vig-os/devkit/commit/f5040d5c99b6e635ee13c08d4d8cc161fca65e16) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 06:14 AM
fix(workspace): upgrade preview report follows template symlinks, 8 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 89: [1eacba6](https://github.com/vig-os/devkit/commit/1eacba660872183b67a40a66791878cc84a86893) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 06:25 AM
fix(workspace): upgrade preview report follows template symlinks (#949) (#950), 63 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, tests/bats/init-workspace.bats)

### Commit 90: [90f56b8](https://github.com/vig-os/devkit/commit/90f56b8a5eeb34814f83c5a23d2ac44db5bbb5ae) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 08:50 AM
test: cover upgrade preview over-reporting the baked .venv tree, 48 files modified (tests/bats/init-workspace.bats)

### Commit 91: [1e144b0](https://github.com/vig-os/devkit/commit/1e144b0411fbfcc58c8de352878d62ee1caa097c) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 08:52 AM
fix(workspace): upgrade report excludes .venv/docs from the ADDED walk, 10 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 92: [286e419](https://github.com/vig-os/devkit/commit/286e419f35e79c221fb86565b9fef09355362887) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 09:14 AM
fix(workspace): upgrade report excludes .venv/docs from the ADDED walk (#951) (#952), 58 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md, tests/bats/init-workspace.bats)

### Commit 93: [22c9941](https://github.com/vig-os/devkit/commit/22c99413f89486648e15e7109f90f54a9b5a9e89) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on July 9, 2026 at 10:34 AM
chore: finalize release 0.5.0, 8 files modified (.vig-os, CHANGELOG.md, README.md, assets/workspace/.devcontainer/CHANGELOG.md)
