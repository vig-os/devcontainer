---
type: pull_request
state: closed (merged)
branch: bugfix/355-release-lookup-false-positive → release/0.3.1
created: 2026-03-18T09:14:08Z
updated: 2026-03-18T09:49:15Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devcontainer/pull/356
comments: 6
labels: none
assignees: c-vigo
milestone: none
projects: none
merged: 2026-03-18T09:49:13Z
synced: 2026-03-19T04:27:52.358Z
---

# [PR 356](https://github.com/vig-os/devcontainer/pull/356) fix(ci): use status-aware smoke-test release lookup

## Description

Fixes a false-positive release lookup in the smoke-test `repository_dispatch` workflow where a missing tag release (`404`) was treated as an existing release because the script relied on non-empty stdout instead of command status.

The workflow now branches on `gh api` exit status so missing tags correctly follow the release-create path, and existing releases still validate and update release notes.

## Type of Change

- [x] `fix` -- Bug fix
- [ ] `feat` -- New feature
- [ ] `docs` -- Documentation only
- [ ] `chore` -- Maintenance task (deps, config, etc.)
- [ ] `refactor` -- Code restructuring (no behavior change)
- [ ] `test` -- Adding or updating tests
- [ ] `ci` -- CI/CD pipeline changes
- [ ] `build` -- Build system or dependency changes
- [ ] `revert` -- Reverts a previous commit
- [ ] `style` -- Code style (formatting, whitespace)

### Modifiers

- [ ] Breaking change (`!`) -- This change breaks backward compatibility

## Changes Made

- `assets/smoke-test/.github/workflows/repository-dispatch.yml`
  - Replace `|| true` + stdout-emptiness release detection with status-aware `if EXISTING_RELEASE=$(gh api ...)`
  - Preserve existing release validation (`.prerelease` check) and `gh release edit` path
  - Ensure 404/missing tag falls into `gh release create` path instead of failing with `prerelease=null`
- `CHANGELOG.md`
  - Add `### Fixed` entry for issue `#355`
- `assets/workspace/.devcontainer/CHANGELOG.md`
  - Synced by pre-commit `sync-manifest` hook from root changelog update

## Changelog Entry

### Fixed
- **Smoke-test release lookup no longer treats missing tags as existing releases** ([#355](https://github.com/vig-os/devcontainer/issues/355))
  - Change `assets/smoke-test/.github/workflows/repository-dispatch.yml` to branch on `gh api` exit status when querying `releases/tags/<tag>`
  - Ensure missing release tags follow the create path instead of failing with `prerelease=null` mismatch

## Testing

- [ ] Tests pass locally (`just test`)
- [x] Manual testing performed (describe below)

### Manual Testing Details

- Ran `npx bats tests/bats/just.bats` (13/13 passing)

## Checklist

- [x] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [ ] I have commented my code, particularly in hard-to-understand areas
- [ ] I have updated the documentation accordingly (edit `docs/templates/`, then run `just docs`)
- [x] I have updated `CHANGELOG.md` in the `[Unreleased]` section (and pasted the entry above)
- [x] My changes generate no new warnings or errors
- [ ] I have added tests that prove my fix is effective or that my feature works
- [ ] New and existing unit tests pass locally with my changes
- [ ] Any dependent changes have been merged and published

## Additional Notes

N/A

Refs: #355



---
---

## Review Threads (3)

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 09:29 AM_

_File: [`assets/smoke-test/.github/workflows/repository-dispatch.yml`](https://github.com/vig-os/devcontainer/pull/356#discussion_r2952070045)_

```diff
@@ -328,19 +327,23 @@ jobs:
               --title "$TAG" \
               --notes-file /tmp/smoke-test-release-notes.md
             echo "Release '$TAG' already exists with expected prerelease value."
+          else
+            if [ "$EXPECTED_PRERELEASE" = "true" ]; then
+              gh release create "$TAG" \
+                --title "$TAG" \
```

`gh api` failures other than a missing tag (e.g., auth/rate-limit/network errors) will also go down the `else` path and attempt `gh release create`, which can mask the real failure mode and potentially create inconsistent behavior. Consider capturing the HTTP status (or error output) and only treating a 404 as "missing release"; for other failures, print the error and `exit 1`.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 09:32 AM — [link](https://github.com/vig-os/devcontainer/pull/356#discussion_r2952090903)

  Addressed in 3e5b769. Release lookup now inspects lookup failure output: only 404/not found enters the create path, while other lookup failures now fail fast with the original error output. This avoids masking auth/rate-limit/network issues.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 09:29 AM_

_File: [`assets/smoke-test/.github/workflows/repository-dispatch.yml`](https://github.com/vig-os/devcontainer/pull/356#discussion_r2952070076)_

```diff
@@ -328,19 +327,23 @@ jobs:
               --title "$TAG" \
               --notes-file /tmp/smoke-test-release-notes.md
             echo "Release '$TAG' already exists with expected prerelease value."
+          else
+            if [ "$EXPECTED_PRERELEASE" = "true" ]; then
+              gh release create "$TAG" \
+                --title "$TAG" \
+                --notes-file /tmp/smoke-test-release-notes.md \
+                --prerelease
+            else
+              gh release create "$TAG" \
+                --title "$TAG" \
+                --notes-file /tmp/smoke-test-release-notes.md
+            fi
+            echo "Created release '$TAG' with prerelease=$EXPECTED_PRERELEASE."
             exit 0
           fi
 
-          if [ "$EXPECTED_PRERELEASE" = "true" ]; then
-            gh release create "$TAG" \
-              --title "$TAG" \
-              --notes-file /tmp/smoke-test-release-notes.md \
-              --prerelease
-          else
-            gh release create "$TAG" \
-              --title "$TAG" \
-              --notes-file /tmp/smoke-test-release-notes.md
-          fi
+          echo "Updated existing release '$TAG' with expected prerelease value."
+          exit 0
```

This log line is a bit misleading/redundant: the step already logged "Release '$TAG' already exists..." earlier, and the actual update action is `gh release edit` above. Consider consolidating to a single message (and placing it next to the `gh release edit`) so logs clearly reflect what happened.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 09:33 AM — [link](https://github.com/vig-os/devcontainer/pull/356#discussion_r2952091047)

  Addressed in 3e5b769. I removed the redundant success messaging and now emit a single branch-specific message: update message after `gh release edit`, create message after `gh release create`.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 18, 2026 at 09:36 AM_

_File: [`assets/smoke-test/.github/workflows/repository-dispatch.yml`](https://github.com/vig-os/devcontainer/pull/356#discussion_r2952112552)_

```diff
@@ -327,19 +327,25 @@ jobs:
             gh release edit "$TAG" \
               --title "$TAG" \
               --notes-file /tmp/smoke-test-release-notes.md
-            echo "Release '$TAG' already exists with expected prerelease value."
-            exit 0
-          fi
-
-          if [ "$EXPECTED_PRERELEASE" = "true" ]; then
-            gh release create "$TAG" \
-              --title "$TAG" \
-              --notes-file /tmp/smoke-test-release-notes.md \
-              --prerelease
+            echo "Updated existing release '$TAG' with expected prerelease=$EXPECTED_PRERELEASE."
           else
-            gh release create "$TAG" \
-              --title "$TAG" \
-              --notes-file /tmp/smoke-test-release-notes.md
+            if printf '%s' "$RELEASE_LOOKUP_OUTPUT" | grep -Eqi "404|not found"; then
+              if [ "$EXPECTED_PRERELEASE" = "true" ]; then
+                gh release create "$TAG" \
+                  --title "$TAG" \
+                  --notes-file /tmp/smoke-test-release-notes.md \
```

Missing-release detection is currently based on grepping the human-readable `gh api` error text for `404|not found`. This is brittle and can misclassify other failures (or future CLI message changes), potentially taking the create-release path when the lookup actually failed for another reason. Consider capturing the HTTP status explicitly (e.g., `gh api -i` and parse the status line, or otherwise branching on exit code while separately checking for an actual 404) so only a true 404 triggers creation.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 18, 2026 at 09:41 AM — [link](https://github.com/vig-os/devcontainer/pull/356#discussion_r2952137815)

  Addressed in 998f6f7. Missing-release detection now uses `gh api -i` and parses the HTTP status line explicitly. We only create on status 404, use update path on 200, and fail for all other statuses with the lookup diagnostics.


---
---

## Commits

### Commit 1: [2683cb0](https://github.com/vig-os/devcontainer/commit/2683cb0aa1b7e4e08cf6d7fc84984b9ef8d1ca8b) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 09:07 AM
fix(ci): use status-aware smoke-test release lookup, 27 files modified (assets/smoke-test/.github/workflows/repository-dispatch.yml)

### Commit 2: [8ff2205](https://github.com/vig-os/devcontainer/commit/8ff22052e6334272c0cc19f661503c32c68ed409) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 09:08 AM
docs(ci): add changelog entry for release lookup fix, 6 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 3: [3e5b769](https://github.com/vig-os/devcontainer/commit/3e5b769f154fce16e6a1bad14a75e06a902462a5) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 09:32 AM
fix(ci): fail fast on non-404 release lookup errors, 35 files modified (assets/smoke-test/.github/workflows/repository-dispatch.yml)

### Commit 4: [998f6f7](https://github.com/vig-os/devcontainer/commit/998f6f77ededbc09542426ef0133ed4515c02ecf) by [c-vigo](https://github.com/c-vigo) on March 18, 2026 at 09:41 AM
fix(ci): detect missing release via HTTP status, 23 files modified (assets/smoke-test/.github/workflows/repository-dispatch.yml)
