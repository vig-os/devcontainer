---
type: pull_request
state: closed (merged)
branch: bugfix/949-preview-report-follows-symlinks → release/0.5.0
created: 2026-07-09T06:16:27Z
updated: 2026-07-09T06:25:36Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devkit/pull/950
comments: 1
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-07-09T06:25:35Z
synced: 2026-07-11T13:35:12.032Z
---

# [PR 950](https://github.com/vig-os/devkit/pull/950) fix(workspace): upgrade preview report follows template symlinks (#949)

## Problem
On the Nix image the baked template `/root/assets/workspace` is a tree of **symlinks into the nix store**. The `--preview`/`--force` upgrade classifier at `assets/init-workspace.sh:534` walked it with `find "$TEMPLATE_DIR" -type f …`, which matches **zero** files against a symlink tree (verified: `find -type f` → 0, `find -L -type f` → 1872). So the OVERWRITTEN / ADDED / PRESERVED report was always empty ("No existing files would be overwritten").

## Root cause
The real copy uses `rsync -avL`, which **follows symlinks**, so files were still overwritten — only the *report* was blind. The `DELETED` section is computed separately and was unaffected. This is the sole `find` over `$TEMPLATE_DIR`.

## Fix
Make the classifier follow symlinks to match the copy semantics:

```diff
-    done < <(find "$TEMPLATE_DIR" -type f ! -path "*/.git/*" -print0)
+    done < <(find -L "$TEMPLATE_DIR" -type f ! -path "*/.git/*" -print0)
```

## Tests (TDD, red → green)
Added two bats tests in `tests/bats/init-workspace.bats` that seed a **symlinked** `TEMPLATE_DIR` (mirroring the baked image) and drive `--preview --force --no-prompts` against a host workspace:
- a symlinked template file whose path already exists in the workspace must be listed under **OVERWRITTEN**;
- a symlinked, workspace-absent template file must be listed under **ADDED**.

Both **failed** on the old code (report said "No existing files would be overwritten" / "No new files would be added") and **pass** with the fix. Full `init-workspace.bats` suite: 123 passed, 0 failed. Full `prek run --all-files` hook suite: green (sync-manifest propagated the CHANGELOG entry to the scaffold copy).

Refs: #949
Closes #949



---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/devkit/pull/950#issuecomment-4922133138) by [@c-vigo](https://github.com/c-vigo)

_Posted on July 9, 2026 at 06:18 AM_

Reviewed the diff: the fix is the single-site `find` → `find -L` at `init-workspace.sh:534` (matches the `rsync -avL` copy semantics); the added bats tests build store-style `ln -s` symlinks in `TEMPLATE_DIR` and assert OVERWRITTEN/ADDED are populated, genuinely reproducing #949 (red→green); changelog entry correctly under `## [0.5.0] - TBD` → Fixed. Minimal, in-scope, TDD-clean. LGTM — auto-merge stands.

---
---

## Commits

### Commit 1: [09f6cf1](https://github.com/vig-os/devkit/commit/09f6cf17e370dc59adfb236229cacacd09dc5d50) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 06:08 AM
test: cover upgrade preview report over symlinked templates, 55 files modified (tests/bats/init-workspace.bats)

### Commit 2: [f5040d5](https://github.com/vig-os/devkit/commit/f5040d5c99b6e635ee13c08d4d8cc161fca65e16) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 06:14 AM
fix(workspace): upgrade preview report follows template symlinks, 8 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md)
