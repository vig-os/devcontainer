---
type: pull_request
state: closed
branch: feature/545-image-toolkit → main
created: 2026-05-15T14:01:27Z
updated: 2026-07-09T14:38:55Z
author: gerchowl
author_url: https://github.com/gerchowl
url: https://github.com/vig-os/devkit/pull/547
comments: 1
labels: none
assignees: none
milestone: none
projects: none
synced: 2026-07-11T13:34:54.338Z
---

# [PR 547](https://github.com/vig-os/devkit/pull/547) feat(image): bundle agent-CLI toolkit + TUI debug stack + Claude Code (#545)

## Summary

Bundles the agent-CLI + TUI-debug + Claude Code toolkit into the devcontainer image so Claude / cursor-agent sessions land in a fully-equipped environment without per-project tool installs.

Closes #545

## What's added

**Containerfile additions** (single file change):
- apt: `expect neovim ripgrep fd-find bat fzf` (+ symlinks `/usr/local/bin/{fd,bat}` → Debian's `fdfind`/`batcat` so agents can use the canonical names)
- binary release downloads: `eza delta lazygit zoxide starship freeze` (none in apt for bookworm)
- Claude Code baked via official installer with 3-attempt retry; symlinked to `/usr/local/bin/claude`
- `ENV IS_SANDBOX=1` so `claude --dangerously-skip-permissions` bypasses the uid-0 refusal cleanly (container is the trust boundary; documented escape hatch)
- Aliases `cc=claude` and `cld='claude --dangerously-skip-permissions'` in `/root/.bashrc`

**Tests** (`tests/test_image.py`):
- New `TestAgentToolkit` class with 36 cases covering presence + version-line for every added tool, plus `IS_SANDBOX=1` env assertion and alias-content checks against `/root/.bashrc`
- Bumps two pre-existing stale version pins (`just 1.50→1.51`, `cargo-binstall 1.18→1.19`) so CI is green on this PR — both were already failing on main

## Validation

- ✅ `just build` (linux/arm64) succeeds
- ✅ `just test-image` — **all 95 tests pass**, including all 36 new `TestAgentToolkit` cases
- Image size delta: ~+200MB
- Tested manually that `cc`/`cld` aliases work in interactive shells

## Test plan

- [ ] CI green (build + tests)
- [ ] `just test-image` against amd64 build (validated locally on arm64; user will retest on x86_64 host)
- [ ] Smoke-test in real workspace: `claude --version` → 2.x; `rg`/`fd`/`bat`/`tmux capture-pane`/`freeze` all reachable

## Related work

Part of the scope-split rework of #166 — see also #85 (Tailscale-in-container) and #546 (slim Claude Code OAuth-token forwarding) for the other slices.

Refs: #545


---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/devkit/pull/547#issuecomment-4875990656) by [@c-vigo](https://github.com/c-vigo)

_Posted on July 3, 2026 at 12:09 PM_

Closing as superseded: the Nix migration (#625) rebuilt the image pipeline from scratch — the agent-CLI toolkit + Claude Code requirements this PR implemented were absorbed into the flake `devTools` SSoT and the Nix-built image (#631/#634; #545 was closed as absorbed on 2026-07-02). The branch targets `main` from before the migration (300+ commits behind `dev`) and is no longer mergeable in any meaningful sense.

---
---

## Commits

### Commit 1: [c7e6ea5](https://github.com/vig-os/devkit/commit/c7e6ea50f361f90c8cad5541d508eb70c2026973) by [gerchowl](https://github.com/gerchowl) on May 15, 2026 at 11:14 AM
feat(image): bundle agent-CLI toolkit + TUI debug stack + Claude Code, 154 files modified (Containerfile)

### Commit 2: [c6d95ae](https://github.com/vig-os/devkit/commit/c6d95ae63427d0a4f87b7bd48aed3628791728ae) by [gerchowl](https://github.com/gerchowl) on May 15, 2026 at 02:00 PM
chore(tests): TestAgentToolkit + version bumps, 213 files modified (tests/test_image.py)
