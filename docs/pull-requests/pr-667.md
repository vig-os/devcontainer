---
type: pull_request
state: closed (merged)
branch: feature/666-nix-toolchain-parity → feature/625-nix-claude-migration
created: 2026-06-24T07:15:23Z
updated: 2026-06-24T07:23:00Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devcontainer/pull/667
comments: 0
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-06-24T07:22:57Z
synced: 2026-06-26T06:18:32.821Z
---

# [PR 667](https://github.com/vig-os/devcontainer/pull/667) feat(nix): Nix image toolchain parity — pass the full testinfra suite (#666)

## Summary

Closes the deferred discovery gap so the **Nix image passes the full `tests/test_image.py` suite (63/63)** — a prerequisite for making Nix the only build (#642).

- **Relaxed `requires-python` `==3.14.6` → `>=3.14,<3.15`** (root, `vig-utils`, workspace template) + regenerated `uv.lock`. `flake.lock` pins the exact interpreter now, so the `==` pin was redundant and unsatisfiable against nixpkgs (3.14.4) — `uv sync` was failing.
- **Baked the project Python toolchain (Nix-native):** packaged `vig-utils` (hatchling, dep `rich`) and `pip-licenses` (from its PyPI wheel — not in nixpkgs) into a `python314.withPackages` env so `import vig_utils` + the console scripts work without a network-populated venv; added `ruff`, `bandit`, `cargo-binstall`, `just-lsp`, `typstyle` from nixpkgs. Replaces the Debian image's build-time `uv pip install`.
- **Adapted `test_image.py`:** nixpkgs-pinned tools are checked presence/run-only (versions live in `flake.lock`, not the test; avoids breakage on every nixpkgs bump); the pre-commit cache dir is asserted present (a hermetic build can't pre-fetch hook repos). Made `nix-image.yml`'s `build-and-test` job **gate** on testinfra (discovery phase closed; the push step stays tolerant of registry hiccups).

## Verified locally
- `nix build .#devcontainerImage` succeeds; `podman load` + `TEST_CONTAINER_TAG=… uv run pytest tests/test_image.py` → **63 passed** (was 31).
- Dev-shell still evaluates; `devShellTools` parity list unchanged (23).

## Dependencies
- Part of #625; unblocks #642 (Debian decommission). Refs #634, #635.

Refs: #666


---
---

## Commits

### Commit 1: [c783960](https://github.com/vig-os/devcontainer/commit/c7839602c45b673481d3e43dc45e35a90c538ee0) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 07:12 AM
build(nix): relax requires-python to >=3.14,<3.15, 11 files modified (assets/workspace/pyproject.toml, packages/vig-utils/pyproject.toml, pyproject.toml, uv.lock)

### Commit 2: [d953a8c](https://github.com/vig-os/devcontainer/commit/d953a8c0493c5ca7fc5306aff5a3061bc213e41d) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 07:13 AM
feat(nix): bake the project Python toolchain into the image, 67 files modified (flake.nix)

### Commit 3: [b4b411a](https://github.com/vig-os/devcontainer/commit/b4b411acc6ec72872e6b096eb9289d21dc83b996) by [c-vigo](https://github.com/c-vigo) on June 24, 2026 at 07:14 AM
test(image): adapt version assertions to the Nix toolchain; gate nix-image testinfra, 115 files modified (.github/workflows/nix-image.yml, CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md, tests/test_image.py)
