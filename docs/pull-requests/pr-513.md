---
type: pull_request
state: closed (merged)
branch: chore/509-switch-dependabot-to-renovate → dev
created: 2026-04-10T09:00:18Z
updated: 2026-04-10T12:06:38Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devcontainer/pull/513
comments: 16
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-04-10T12:06:37Z
synced: 2026-04-10T13:54:33.205Z
---

# [PR 513](https://github.com/vig-os/devcontainer/pull/513) chore: switch from Dependabot to Renovate and automate dependency changelog

## Description

Implements [#509](https://github.com/vig-os/devcontainer/issues/509) (switch from Dependabot to Renovate for dependency updates, including template paths Dependabot could not scan) and [#506](https://github.com/vig-os/devcontainer/issues/506) (automatic `CHANGELOG.md` updates on Renovate PRs via `pull_request_target` and `renovate-changelog-pr`). Adds `.vig-os` + `resolve-image` so `sync-issues` and `sync-main-to-dev` run in the pinned devcontainer image; `install.sh` / `init-workspace.sh` resolve `GITHUB_REPOSITORY` for templated `renovate.json`.

## Type of Change

- [x] `feat` -- New feature
- [ ] `fix` -- Bug fix
- [x] `docs` -- Documentation only
- [x] `chore` -- Maintenance task (deps, config, etc.)
- [ ] `refactor` -- Code restructuring (no behavior change)
- [x] `test` -- Adding or updating tests
- [x] `ci` -- CI/CD pipeline changes
- [ ] `build` -- Build system or dependency changes
- [ ] `revert` -- Reverts a previous commit
- [ ] `style` -- Code style (formatting, whitespace)

### Modifiers

- [ ] Breaking change (`!`) -- This change breaks backward compatibility

## Changes Made

- **27 files**, ~1164 insertions / ~247 deletions vs `dev` (includes merge commit from `origin/dev`).
- **Renovate / Dependabot:** Remove root and workspace `dependabot.yml`; add `renovate.json`, `assets/workspace/renovate.json`, `renovate-default.json`, smoke-test `renovate.json`; update `CODEOWNERS`.
- **CI image pinning:** Add `.vig-os`, `.github/actions/resolve-image/`, `release.yml` step to bump `DEVCONTAINER_VERSION` on final release; `sync-issues.yml` / `sync-main-to-dev.yml` use resolved GHCR tag, `safe.directory`, drop `setup-env` / inlined retry; sync branch creation uses `git push`.
- **Workspace init:** `parse-github-remote-lib.sh`, `Containerfile` placeholder manifest, `install.sh --repo`, `init-workspace.sh` + BATS/install/conftest updates.
- **Changelog automation:** `renovate_changelog_pr.py`, tests, console script in `pyproject.toml`, root + workspace `renovate-changelog.yml`; `scripts/manifest.toml` syncs new paths.
- **Docs:** `CHANGELOG.md` and synced `assets/workspace/.devcontainer/CHANGELOG.md`.

## Changelog Entry

### Added

- **Renovate changelog automation** ([#506](https://github.com/vig-os/devcontainer/issues/506))
  - `renovate-changelog-pr` CLI tool parses Renovate PR metadata and inserts Keep-a-Changelog entries under `## Unreleased`
  - `renovate-changelog` workflow runs on `pull_request_target` for `renovate[bot]` PRs in both upstream and workspace template
- **Devcontainer image version pinning** ([#509](https://github.com/vig-os/devcontainer/issues/509))
  - `.vig-os` file at repo root declares `DEVCONTAINER_VERSION` as the single source of truth for CI container image tags
  - `resolve-image` composite action resolves the image tag and validates it exists in GHCR
- **`GITHUB_REPOSITORY` resolution for workspace init** ([#509](https://github.com/vig-os/devcontainer/issues/509))
  - `parse-github-remote-lib.sh` extracts `owner/repo` from HTTPS, SSH, and `git@` GitHub URLs
  - `install.sh` gains `--repo` flag; `init-workspace.sh` replaces `{{GITHUB_REPOSITORY}}` in workspace template files

### Changed

- **Switch from Dependabot to Renovate** ([#509](https://github.com/vig-os/devcontainer/issues/509))
  - Replace `.github/dependabot.yml` with `renovate.json` and shared `renovate-default.json` preset
  - Renovate covers all ecosystems previously tracked (github-actions, pip, npm, docker) plus template directories not reachable by Dependabot
- **Sync workflows run in devcontainer image** ([#509](https://github.com/vig-os/devcontainer/issues/509))
  - `sync-issues` and `sync-main-to-dev` use `resolve-image` and run inside the pinned devcontainer, removing the `setup-env` composite action dependency and the inlined retry helper
  - `sync-main-to-dev` creates sync branches via `git push` instead of the GitHub refs API

### Removed

- **Dependabot configuration** ([#509](https://github.com/vig-os/devcontainer/issues/509))
  - Delete `.github/dependabot.yml` and `assets/workspace/.github/dependabot.yml`

## Testing

- [x] Tests pass locally (`just test`)
- [ ] Manual testing performed (describe below)

### Manual Testing Details

N/A (CI and scripted tests cover changes; Renovate/changelog workflow validates in GitHub after merge).

## Checklist

- [x] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [ ] I have commented my code, particularly in hard-to-understand areas
- [ ] I have updated the documentation accordingly (edit `docs/templates/`, then run `just docs`)
- [x] I have updated `CHANGELOG.md` in the `[Unreleased]` section (and pasted the entry above)
- [x] My changes generate no new warnings or errors
- [x] I have added tests that prove my fix is effective or that my feature works
- [x] New and existing unit tests pass locally with my changes
- [x] Any dependent changes have been merged and published

## Additional Notes

- Branch merged latest `origin/dev` before PR (includes unrelated doc sync under `docs/pull-requests/`).
- Commit 6 subject is `docs:` (not `docs(changelog):`) due to commit-msg scope allowlist.

Refs: #509, #506



---
---

## Review Threads (8)

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on April 10, 2026 at 09:12 AM_

_File: [`renovate.json (line 6 RIGHT)`](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063279375)_

```diff
@@ -0,0 +1,15 @@
+{
+  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
+  "extends": [
+    "github>vig-os/devcontainer//assets/workspace/.github/renovate-default"
+  ],
+  "enabledManagers": ["github-actions", "pep621", "uv", "npm", "dockerfile"],
```

`extends` points to `assets/workspace/.github/renovate-default` but the preset file in this repo is `assets/workspace/.github/renovate-default.json`. If Renovate treats this as a file path (not a preset name), this will 404 and disable updates. Consider referencing the exact filename (or whatever format Renovate expects for repo file presets) to avoid misresolution.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on April 10, 2026 at 11:43 AM — [link](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063978670)

  Renovate preset resolution automatically appends `.json` when no extension is given (`github>org/repo//path/name` resolves to `path/name.json`). No change needed; see https://github.com/renovatebot/renovate/blob/master/docs/usage/config-presets.md

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on April 10, 2026 at 09:12 AM_

_File: [`assets/workspace/renovate.json (line 6 RIGHT)`](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063279423)_

```diff
@@ -0,0 +1,7 @@
+{
+  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
+  "extends": [
+    "github>{{GITHUB_REPOSITORY}}//.github/renovate-default"
+  ],
+  "enabledManagers": ["github-actions", "pep621", "uv", "npm"]
```

The `extends` value uses `//.github/renovate-default`, but the preset in this template is `assets/workspace/.github/renovate-default.json`. If Renovate requires the explicit `.json` path, downstream workspaces will fail to load the preset. Consider matching the actual filename to avoid configuration resolution errors.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on April 10, 2026 at 11:43 AM — [link](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063978734)

  Same as the root `renovate.json` thread: Renovate resolves `//path/name` to `path/name.json` for repo presets. No change needed.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on April 10, 2026 at 09:12 AM_

_File: [`install.sh (line 368 RIGHT)`](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063279458)_

```diff
@@ -315,6 +342,21 @@ PROJECT_NAME=$(sanitize_name "$PROJECT_NAME")
 # Sanitize ORG_NAME for security (remove shell metacharacters) but preserve capitalization
 ORG_NAME=$(sanitize_for_security "$ORG_NAME")
 
+# GITHUB_REPOSITORY for init-workspace --no-prompts (Renovate extends in renovate.json)
+GITHUB_REPOSITORY="$GITHUB_REPO_OVERRIDE"
+GITHUB_REPOSITORY=$(sanitize_for_security "$GITHUB_REPOSITORY")
+if [ -z "$GITHUB_REPOSITORY" ] && [ -d "$PROJECT_PATH/.git" ]; then
+    url=$(git -C "$PROJECT_PATH" remote get-url origin 2>/dev/null || true)
+    if [ -n "$url" ]; then
+        if repo=$(parse_github_remote "$url"); then
+            GITHUB_REPOSITORY="$repo"
+        fi
+    fi
+fi
+if [ -z "$GITHUB_REPOSITORY" ]; then
+    GITHUB_REPOSITORY="OWNER/REPO"
+fi
```

`parse_github_remote` can return owner/repo segments containing characters that are not valid in GitHub repo slugs (the regex allows anything except '/' and '.'). Since the parsed value is later passed through to init-workspace and used for template substitution, it should be validated/sanitized after parsing (e.g., enforce `^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$` or run `sanitize_for_security` on the parsed result as well).

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on April 10, 2026 at 11:43 AM — [link](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063978841)

  Addressed: `parse_github_remote` now validates `owner/repo` against `^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$`, and `install.sh` runs `sanitize_for_security` on the parsed value before passing it through (`eb9bb4a`, `0f3a4c0`).

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on April 10, 2026 at 09:12 AM_

_File: [`assets/parse-github-remote-lib.sh (line 32 RIGHT)`](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063279483)_

```diff
@@ -0,0 +1,53 @@
+#!/usr/bin/env bash
+# Parse github.com remote URL to owner/repo (stdout), or return 1 if unsupported
+# Used by init-workspace.sh for {{GITHUB_REPOSITORY}} in renovate.json (Refs: #509).
+parse_github_remote() {
+    local url="$1"
+    [[ -z "$url" ]] && return 1
+    # https://github.com/org/repo.git or trailing /
+    if [[ "$url" =~ https?://github\.com/([^/]+)/([^/.]+)(\.git)?/?$ ]]; then
+        echo "${BASH_REMATCH[1]}/${BASH_REMATCH[2]}"
+        return 0
+    fi
+    # git@github.com:org/repo.git
+    if [[ "$url" =~ ^git@github\.com:([^/]+)/([^/.]+)(\.git)?$ ]]; then
+        echo "${BASH_REMATCH[1]}/${BASH_REMATCH[2]}"
+        return 0
+    fi
+    # ssh://git@github.com/org/repo.git
+    if [[ "$url" =~ ^ssh://git@github\.com/([^/]+)/([^/.]+)(\.git)?$ ]]; then
+        echo "${BASH_REMATCH[1]}/${BASH_REMATCH[2]}"
+        return 0
+    fi
+    return 1
```

`parse_github_remote` matches `([^/.]+)` for owner/repo, which allows many characters GitHub does not permit in repository slugs (e.g., spaces, `$`, `;`). Since `GITHUB_REPOSITORY` is later used for placeholder substitution into files, consider tightening validation to GitHub’s allowed charset (or validating the final `owner/repo` matches an allowlist regex) before accepting it.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on April 10, 2026 at 11:43 AM — [link](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063978930)

  Implemented in `parse_github_remote`: after URL extraction we reject slugs outside GitHub's allowed charset (`eb9bb4a`). Added BATS coverage for the rejection path.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on April 10, 2026 at 09:12 AM_

_File: [`packages/vig-utils/src/vig_utils/renovate_changelog_pr.py (line 188 RIGHT)`](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063279505)_

```diff
@@ -0,0 +1,238 @@
+"""Parse Renovate PR metadata and insert a Keep-a-Changelog entry (Refs: #506)."""
+
+from __future__ import annotations
+
+import argparse
+import os
+import re
+import sys
+from pathlib import Path
+
+
+def _strip_md_link(cell: str) -> str:
+    s = cell.strip()
+    m = re.match(r"\[([^\]]+)\]\([^)]+\)", s)
+    if m:
+        return m.group(1).strip()
+    return s
+
+
+def _parse_change_cell(cell: str) -> tuple[str | None, str | None]:
+    """Return (old, new) from a Renovate-style change cell."""
+    text = cell.strip()
+    m = re.search(r"`([^`]+)`\s*->\s*`([^`]+)`", text)
+    if m:
+        return m.group(1).strip(), m.group(2).strip()
+    # Digest / unquoted: abc -> def
+    m = re.search(r"(\S+)\s*->\s*(\S+)", text)
+    if m:
+        return m.group(1).strip(), m.group(2).strip()
+    return None, None
+
+
+def _parse_table_updates(body: str) -> list[tuple[str, str | None, str | None]]:
+    rows: list[tuple[str, str | None, str | None]] = []
+    for raw_line in body.splitlines():
+        line = raw_line.strip()
+        if not line.startswith("|"):
+            continue
+        parts = [p.strip() for p in line.split("|")]
+        # leading/trailing empty from split
+        cells = [c for c in parts if c != ""]
+        if len(cells) < 2:
+            continue
+        if re.match(r"^-+$", cells[0].replace(" ", "")):
+            continue
+        first = _strip_md_link(cells[0])
+        if first.lower() in ("package", "name", "dependency"):
+            continue
+        old_v, new_v = None, None
+        for cell in reversed(cells):
+            o, n = _parse_change_cell(cell)
+            if o is not None and n is not None:
+                old_v, new_v = o, n
+                break
+        if new_v is not None:
+            rows.append((first, old_v, new_v))
+    return rows
+
+
+def _parse_title_updates(title: str) -> list[tuple[str, str | None, str | None]]:
+    t = title.strip()
+    # digest: update actions/checkout digest to <sha>
+    m = re.search(
+        r"update\s+([^\s]+)\s+digest\s+to\s+(\S+)",
+        t,
+        re.IGNORECASE,
+    )
+    if m:
+        return [(m.group(1), None, m.group(2))]
+    # update dependency <pkg> to <ver>
+    m = re.search(
+        r"update\s+dependency\s+(\S+)\s+to\s+(\S+)",
+        t,
+        re.IGNORECASE,
+    )
+    if m:
+        return [(m.group(1), None, m.group(2))]
+    # update <pkg> to <ver> (no "dependency")
+    m = re.search(r"update\s+(\S+)\s+to\s+(\S+)", t, re.IGNORECASE)
+    if m:
+        return [(m.group(1), None, m.group(2))]
+    return []
+
+
+def parse_renovate_pr_updates(
+    title: str, body: str
+) -> list[tuple[str, str | None, str | None]]:
+    from_table = _parse_table_updates(body)
+    if from_table:
+        return from_table
+    return _parse_title_updates(title)
+
+
+def format_changelog_entry(
+    pr_number: int,
+    repo_html_url: str,
+    updates: list[tuple[str, str | None, str | None]],
+) -> str:
+    base = repo_html_url.rstrip("/")
+    pr_url = f"{base}/pull/{pr_number}"
+    pr_link = f"([#{pr_number}]({pr_url}))"
+    if len(updates) == 1:
+        pkg, old_v, new_v = updates[0]
+        if old_v:
+            title = f"Renovate: update `{pkg}` from `{old_v}` to `{new_v}`"
+        else:
+            title = f"Renovate: update `{pkg}` to `{new_v}`"
+        return f"- **{title}** {pr_link}\n"
+    lines = [f"- **Renovate dependency update** {pr_link}"]
+    for pkg, old_v, new_v in updates:
+        if old_v:
+            lines.append(f"  - Update `{pkg}` from `{old_v}` to `{new_v}`")
+        else:
+            lines.append(f"  - Update `{pkg}` to `{new_v}`")
+    return "\n".join(lines) + "\n"
+
+
+def _pr_marked_in_changed(unreleased: str, pr_number: int) -> bool:
+    needle = f"[#{pr_number}]("
+    changed_idx = unreleased.find("### Changed")
+    if changed_idx == -1:
+        return False
+    next_hdr = re.search(r"\n### (?!Changed)\w+", unreleased[changed_idx:])
+    if next_hdr:
+        changed_block = unreleased[changed_idx : changed_idx + next_hdr.start()]
+    else:
+        changed_block = unreleased[changed_idx:]
+    return needle in changed_block
+
+
+def insert_renovate_changelog_entry(
+    changelog: str,
+    pr_number: int,
+    entry: str,
+) -> tuple[str, bool]:
+    lines = changelog.splitlines(keepends=True)
+    unreleased_start: int | None = None
+    unreleased_end: int | None = None
+    for i, line in enumerate(lines):
+        if line.startswith("## Unreleased"):
+            unreleased_start = i
+            break
+    if unreleased_start is None:
+        return changelog, False
+    for j in range(unreleased_start + 1, len(lines)):
+        if lines[j].startswith("## [") and "[Unreleased]" not in lines[j]:
+            unreleased_end = j
+            break
+    if unreleased_end is None:
+        unreleased_end = len(lines)
+    block = "".join(lines[unreleased_start:unreleased_end])
+    if _pr_marked_in_changed(block, pr_number):
+        return changelog, False
+
+    changed_idx: int | None = None
+    next_sec: int | None = None
+    for i in range(unreleased_start, unreleased_end):
+        line = lines[i]
+        if line.startswith("### Changed"):
+            changed_idx = i
+            continue
+        if (
+            changed_idx is not None
+            and line.startswith("### ")
+            and not line.startswith("### Changed")
+        ):
+            next_sec = i
+            break
+    if changed_idx is None:
+        return changelog, False
+
+    # Insert before the next ### heading, or at end of ## Unreleased if Changed is last subsection
+    insert_at = next_sec if next_sec is not None else unreleased_end
+
+    if not entry.endswith("\n"):
+        entry = entry + "\n"
+    new_lines = lines[:insert_at] + [entry] + lines[insert_at:]
+    return "".join(new_lines), True
```

`insert_renovate_changelog_entry` inserts `entry` immediately before the next `###` heading (or `##` version), which can remove the blank line that currently separates list items from the following heading (Keep-a-Changelog formatting). Consider inserting before trailing blank line(s) that precede the next heading, or ensuring the inserted entry is followed by a blank line when the next line is a heading.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on April 10, 2026 at 11:43 AM — [link](https://github.com/vig-os/devcontainer/pull/513#discussion_r3063978997)

  Fixed by moving the insertion point backward across blank lines before the next heading so the Keep-a-Changelog separator stays intact; regression test in `acb4b4a`, fix in `294c1d4`.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on April 10, 2026 at 11:50 AM_

_File: [`install.sh (line 255 RIGHT)`](https://github.com/vig-os/devcontainer/pull/513#discussion_r3064025353)_

```diff
@@ -221,6 +224,26 @@ sanitize_for_security() {
     echo "$1" | sed 's/[^a-zA-Z0-9._\/-]/_/g'
 }
 
+# Parse github.com remote URL to owner/repo (stdout), or return 1 if unsupported
+# Same rules as assets/parse-github-remote-lib.sh (for GITHUB_REPOSITORY / renovate.json).
+parse_github_remote() {
+    local url="$1"
+    [[ -z "$url" ]] && return 1
+    if [[ "$url" =~ https?://github\.com/([^/]+)/([^/.]+)(\.git)?/?$ ]]; then
+        echo "${BASH_REMATCH[1]}/${BASH_REMATCH[2]}"
+        return 0
+    fi
+    if [[ "$url" =~ ^git@github\.com:([^/]+)/([^/.]+)(\.git)?$ ]]; then
+        echo "${BASH_REMATCH[1]}/${BASH_REMATCH[2]}"
+        return 0
+    fi
+    if [[ "$url" =~ ^ssh://git@github\.com/([^/]+)/([^/.]+)(\.git)?$ ]]; then
+        echo "${BASH_REMATCH[1]}/${BASH_REMATCH[2]}"
+        return 0
+    fi
+    return 1
+}
```

In install.sh, parse_github_remote claims to follow the same rules as assets/parse-github-remote-lib.sh, but it currently returns the extracted owner/repo without validating it against the allowed GitHub slug charset. This means values like 'or;g/re$po' could be accepted (and merely transformed by sanitize_for_security), producing a non-existent repo and potentially breaking Renovate preset resolution. Align this helper with the library by enforcing the same allowlist regex (^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$) and failing when it doesn't match (or reuse the shared library).

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on April 10, 2026 at 11:59 AM — [link](https://github.com/vig-os/devcontainer/pull/513#discussion_r3064093573)

  Addressed in 852d77a: `parse_github_remote` in `install.sh` now applies the same `owner/repo` allowlist as `assets/parse-github-remote-lib.sh` before echoing a parsed remote.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on April 10, 2026 at 11:50 AM_

_File: [`assets/parse-github-remote-lib.sh (line 71 RIGHT)`](https://github.com/vig-os/devcontainer/pull/513#discussion_r3064025402)_

```diff
@@ -0,0 +1,63 @@
+#!/usr/bin/env bash
+# Parse github.com remote URL to owner/repo (stdout), or return 1 if unsupported
+# Used by init-workspace.sh for {{GITHUB_REPOSITORY}} in renovate.json (Refs: #509).
+parse_github_remote() {
+    local url="$1"
+    local owner repo
+    [[ -z "$url" ]] && return 1
+    # https://github.com/org/repo.git or trailing /
+    if [[ "$url" =~ https?://github\.com/([^/]+)/([^/.]+)(\.git)?/?$ ]]; then
+        owner="${BASH_REMATCH[1]}"
+        repo="${BASH_REMATCH[2]}"
+        [[ "$owner/$repo" =~ ^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$ ]] || return 1
+        echo "$owner/$repo"
+        return 0
+    fi
+    # git@github.com:org/repo.git
+    if [[ "$url" =~ ^git@github\.com:([^/]+)/([^/.]+)(\.git)?$ ]]; then
+        owner="${BASH_REMATCH[1]}"
+        repo="${BASH_REMATCH[2]}"
+        [[ "$owner/$repo" =~ ^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$ ]] || return 1
+        echo "$owner/$repo"
+        return 0
+    fi
+    # ssh://git@github.com/org/repo.git
+    if [[ "$url" =~ ^ssh://git@github\.com/([^/]+)/([^/.]+)(\.git)?$ ]]; then
+        owner="${BASH_REMATCH[1]}"
+        repo="${BASH_REMATCH[2]}"
+        [[ "$owner/$repo" =~ ^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$ ]] || return 1
+        echo "$owner/$repo"
+        return 0
+    fi
+    return 1
+}
+
+# Resolve GITHUB_REPOSITORY for {{GITHUB_REPOSITORY}} in renovate.json (after template copy)
+resolve_github_repository() {
+    if [[ -n "${GITHUB_REPOSITORY:-}" ]]; then
+        echo "GitHub repository (Renovate): $GITHUB_REPOSITORY (from environment)"
+        return 0
+    fi
+    local url=""
+    if [[ -d "$WORKSPACE_DIR/.git" ]]; then
+        url=$(git -C "$WORKSPACE_DIR" remote get-url origin 2>/dev/null || true)
+    fi
+    if [[ -n "$url" ]]; then
+        local parsed=""
+        if parsed=$(parse_github_remote "$url"); then
+            GITHUB_REPOSITORY="$parsed"
+            echo "GitHub repository (Renovate): $GITHUB_REPOSITORY (from git remote origin)"
+            return 0
+        fi
+    fi
+    if [[ "$NO_PROMPTS" == "true" ]]; then
+        echo "Error: GITHUB_REPOSITORY is required with --no-prompts (e.g. export GITHUB_REPOSITORY=org/repo), or use a workspace with github.com origin." >&2
+        exit 1
+    fi
+    read -rp "Enter GitHub repository for Renovate (owner/repo, e.g. vig-os/myapp): " GITHUB_REPOSITORY
+    if [[ -z "$GITHUB_REPOSITORY" ]]; then
+        echo "Error: GitHub repository is required to fill renovate.json." >&2
+        exit 1
+    fi
+    echo "GitHub repository (Renovate): $GITHUB_REPOSITORY"
+}
```

resolve_github_repository accepts GITHUB_REPOSITORY from the environment or user prompt without validating it matches an owner/repo GitHub slug. Since this value is later substituted into renovate.json, an invalid value (whitespace, extra path segments, shell metacharacters, etc.) can silently produce a broken Renovate config. Consider validating the final GITHUB_REPOSITORY against the same allowlist used in parse_github_remote (^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$) and erroring/re-prompting if invalid.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on April 10, 2026 at 11:59 AM — [link](https://github.com/vig-os/devcontainer/pull/513#discussion_r3064093642)

  Addressed in 16c5c5c: values from `GITHUB_REPOSITORY` (environment or interactive prompt) must match `^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$` or we exit with a clear error; BATS covers the invalid-env path.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on April 10, 2026 at 11:50 AM_

_File: [`packages/vig-utils/src/vig_utils/renovate_changelog_pr.py (line 183 RIGHT)`](https://github.com/vig-os/devcontainer/pull/513#discussion_r3064025455)_

```diff
@@ -0,0 +1,242 @@
+"""Parse Renovate PR metadata and insert a Keep-a-Changelog entry (Refs: #506)."""
+
+from __future__ import annotations
+
+import argparse
+import os
+import re
+import sys
+from pathlib import Path
+
+
+def _strip_md_link(cell: str) -> str:
+    s = cell.strip()
+    m = re.match(r"\[([^\]]+)\]\([^)]+\)", s)
+    if m:
+        return m.group(1).strip()
+    return s
+
+
+def _parse_change_cell(cell: str) -> tuple[str | None, str | None]:
+    """Return (old, new) from a Renovate-style change cell."""
+    text = cell.strip()
+    m = re.search(r"`([^`]+)`\s*->\s*`([^`]+)`", text)
+    if m:
+        return m.group(1).strip(), m.group(2).strip()
+    # Digest / unquoted: abc -> def
+    m = re.search(r"(\S+)\s*->\s*(\S+)", text)
+    if m:
+        return m.group(1).strip(), m.group(2).strip()
+    return None, None
+
+
+def _parse_table_updates(body: str) -> list[tuple[str, str | None, str | None]]:
+    rows: list[tuple[str, str | None, str | None]] = []
+    for raw_line in body.splitlines():
+        line = raw_line.strip()
+        if not line.startswith("|"):
+            continue
+        parts = [p.strip() for p in line.split("|")]
+        # leading/trailing empty from split
+        cells = [c for c in parts if c != ""]
+        if len(cells) < 2:
+            continue
+        if re.match(r"^-+$", cells[0].replace(" ", "")):
+            continue
+        first = _strip_md_link(cells[0])
+        if first.lower() in ("package", "name", "dependency"):
+            continue
+        old_v, new_v = None, None
+        for cell in reversed(cells):
+            o, n = _parse_change_cell(cell)
+            if o is not None and n is not None:
+                old_v, new_v = o, n
+                break
+        if new_v is not None:
+            rows.append((first, old_v, new_v))
+    return rows
+
+
+def _parse_title_updates(title: str) -> list[tuple[str, str | None, str | None]]:
+    t = title.strip()
+    # digest: update actions/checkout digest to <sha>
+    m = re.search(
+        r"update\s+([^\s]+)\s+digest\s+to\s+(\S+)",
+        t,
+        re.IGNORECASE,
+    )
+    if m:
+        return [(m.group(1), None, m.group(2))]
+    # update dependency <pkg> to <ver>
+    m = re.search(
+        r"update\s+dependency\s+(\S+)\s+to\s+(\S+)",
+        t,
+        re.IGNORECASE,
+    )
+    if m:
+        return [(m.group(1), None, m.group(2))]
+    # update <pkg> to <ver> (no "dependency")
+    m = re.search(r"update\s+(\S+)\s+to\s+(\S+)", t, re.IGNORECASE)
+    if m:
+        return [(m.group(1), None, m.group(2))]
+    return []
+
+
+def parse_renovate_pr_updates(
+    title: str, body: str
+) -> list[tuple[str, str | None, str | None]]:
+    from_table = _parse_table_updates(body)
+    if from_table:
+        return from_table
+    return _parse_title_updates(title)
+
+
+def format_changelog_entry(
+    pr_number: int,
+    repo_html_url: str,
+    updates: list[tuple[str, str | None, str | None]],
+) -> str:
+    base = repo_html_url.rstrip("/")
+    pr_url = f"{base}/pull/{pr_number}"
+    pr_link = f"([#{pr_number}]({pr_url}))"
+    if len(updates) == 1:
+        pkg, old_v, new_v = updates[0]
+        if old_v:
+            title = f"Renovate: update `{pkg}` from `{old_v}` to `{new_v}`"
+        else:
+            title = f"Renovate: update `{pkg}` to `{new_v}`"
+        return f"- **{title}** {pr_link}\n"
+    lines = [f"- **Renovate dependency update** {pr_link}"]
+    for pkg, old_v, new_v in updates:
+        if old_v:
+            lines.append(f"  - Update `{pkg}` from `{old_v}` to `{new_v}`")
+        else:
+            lines.append(f"  - Update `{pkg}` to `{new_v}`")
+    return "\n".join(lines) + "\n"
+
+
+def _pr_marked_in_changed(unreleased: str, pr_number: int) -> bool:
+    needle = f"[#{pr_number}]("
+    changed_idx = unreleased.find("### Changed")
+    if changed_idx == -1:
+        return False
+    next_hdr = re.search(r"\n### (?!Changed)\w+", unreleased[changed_idx:])
+    if next_hdr:
+        changed_block = unreleased[changed_idx : changed_idx + next_hdr.start()]
+    else:
+        changed_block = unreleased[changed_idx:]
+    return needle in changed_block
+
+
+def insert_renovate_changelog_entry(
+    changelog: str,
+    pr_number: int,
+    entry: str,
+) -> tuple[str, bool]:
+    lines = changelog.splitlines(keepends=True)
+    unreleased_start: int | None = None
+    unreleased_end: int | None = None
+    for i, line in enumerate(lines):
+        if line.startswith("## Unreleased"):
+            unreleased_start = i
+            break
+    if unreleased_start is None:
+        return changelog, False
+    for j in range(unreleased_start + 1, len(lines)):
+        if lines[j].startswith("## [") and "[Unreleased]" not in lines[j]:
+            unreleased_end = j
+            break
+    if unreleased_end is None:
+        unreleased_end = len(lines)
+    block = "".join(lines[unreleased_start:unreleased_end])
+    if _pr_marked_in_changed(block, pr_number):
+        return changelog, False
+
+    changed_idx: int | None = None
+    next_sec: int | None = None
+    for i in range(unreleased_start, unreleased_end):
+        line = lines[i]
+        if line.startswith("### Changed"):
+            changed_idx = i
+            continue
+        if (
+            changed_idx is not None
+            and line.startswith("### ")
+            and not line.startswith("### Changed")
+        ):
+            next_sec = i
+            break
+    if changed_idx is None:
+        return changelog, False
+
+    # Insert before the next ### heading, or at end of ## Unreleased if Changed is last subsection
+    insert_at = next_sec if next_sec is not None else unreleased_end
+
+    # Keep blank line between list items and the following heading (Keep-a-Changelog)
+    while insert_at > changed_idx + 1 and lines[insert_at - 1].strip() == "":
+        insert_at -= 1
```

The insertion-point adjustment that moves insert_at backward across blank lines preserves the blank line before the next heading, but it also collapses the blank line between '### Changed' and the first list item when the Changed section is empty (i.e., only blank lines before the next heading). That produces '### Changed' immediately followed by a '-' bullet, which diverges from the Keep-a-Changelog formatting used elsewhere in the file. Consider stopping the backward scan from crossing the first blank line after '### Changed', and instead ensure there’s still a blank line separating the inserted bullet(s) from the following heading.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on April 10, 2026 at 11:59 AM — [link](https://github.com/vig-os/devcontainer/pull/513#discussion_r3064093719)

  Addressed in a283fc7: insertion uses a minimum `insert_at` when the line after `### Changed` is blank so the first bullet stays separated from the heading per Keep-a-Changelog; `test_insert_empty_changed_section` asserts `### Changed` + blank + bullet.


---
---

## Commits

### Commit 1: [e63e4b1](https://github.com/vig-os/devcontainer/commit/e63e4b11069ed8841230b3163db538723b6e58f9) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 08:42 AM
chore: replace Dependabot with Renovate configuration, 215 files modified

### Commit 2: [b7b01e3](https://github.com/vig-os/devcontainer/commit/b7b01e316364d2a470546722ab0f20050b060e80) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 08:42 AM
ci: add .vig-os version file and resolve-image action, 88 files modified (.github/actions/resolve-image/action.yml, .github/workflows/release.yml, .vig-os, scripts/manifest.toml)

### Commit 3: [1a794ab](https://github.com/vig-os/devcontainer/commit/1a794abcaa91171b1c7af23bc8a1fba22b38816f) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 08:42 AM
refactor(ci): run sync workflows in devcontainer image, 173 files modified (.github/workflows/sync-issues.yml, .github/workflows/sync-main-to-dev.yml)

### Commit 4: [e1431be](https://github.com/vig-os/devcontainer/commit/e1431beafb764ec67fc57136818fd22c301a02bb) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 08:44 AM
feat: resolve GITHUB_REPOSITORY for Renovate preset extends, 290 files modified (Containerfile, assets/init-workspace.sh, assets/parse-github-remote-lib.sh, install.sh, tests/bats/init-workspace.bats, tests/bats/install.bats, tests/conftest.py)

### Commit 5: [3efa03d](https://github.com/vig-os/devcontainer/commit/3efa03d8ba9a7191297ef7fa1280144b67e514b3) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 08:44 AM
feat: add renovate-changelog-pr tool and workflow, 609 files modified

### Commit 6: [4655b57](https://github.com/vig-os/devcontainer/commit/4655b57537dc605c34e5684c44c737390cf39b03) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 08:45 AM
docs: document Renovate migration and changelog automation, 40 files modified (CHANGELOG.md, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 7: [f97b2e9](https://github.com/vig-os/devcontainer/commit/f97b2e9c1fd605b897bded44abcf5665f29e78f3) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 08:45 AM
chore: merge origin/dev into chore/509-switch-dependabot-to-renovate, 215 files modified (docs/pull-requests/pr-510.md)

### Commit 8: [1c82b97](https://github.com/vig-os/devcontainer/commit/1c82b9716cb9dc7b57c014f8a929cfbd73bce955) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 11:33 AM
test: clear GITHUB_REPOSITORY in BATS so CI env does not skew tests, 6 files modified (tests/bats/init-workspace.bats)

### Commit 9: [54f4346](https://github.com/vig-os/devcontainer/commit/54f43466c776cd04d4198384cb48eca38865258e) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 11:39 AM
test: verify parse_github_remote rejects invalid charset, 5 files modified (tests/bats/init-workspace.bats)

### Commit 10: [eb9bb4a](https://github.com/vig-os/devcontainer/commit/eb9bb4acd7c28b4bbc98c0fcbf5fa626f88b81ff) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 11:40 AM
fix: validate GitHub owner/repo charset in parse_github_remote, 16 files modified (assets/parse-github-remote-lib.sh)

### Commit 11: [0f3a4c0](https://github.com/vig-os/devcontainer/commit/0f3a4c01b62faa64c165dda99e868593ae1cc8b9) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 11:40 AM
fix: sanitize parse_github_remote result in install.sh, 2 files modified (install.sh)

### Commit 12: [acb4b4a](https://github.com/vig-os/devcontainer/commit/acb4b4a5dcf8f79041e0a54e8e02ff88fb4c555b) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 11:41 AM
test: verify blank line before heading after changelog insertion, 19 files modified (packages/vig-utils/tests/test_renovate_changelog_pr.py)

### Commit 13: [294c1d4](https://github.com/vig-os/devcontainer/commit/294c1d45489db51803dd8334142844c1e49715f8) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 11:41 AM
fix: preserve blank line before heading in changelog inserter, 4 files modified (packages/vig-utils/src/vig_utils/renovate_changelog_pr.py)

### Commit 14: [a283fc7](https://github.com/vig-os/devcontainer/commit/a283fc74bdd43e80dca8c8d853ebca62ba7660ed) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 11:57 AM
fix(vigutils): preserve blank line after ### Changed for empty section, 10 files modified (packages/vig-utils/src/vig_utils/renovate_changelog_pr.py, packages/vig-utils/tests/test_renovate_changelog_pr.py)

### Commit 15: [16c5c5c](https://github.com/vig-os/devcontainer/commit/16c5c5cbe8e849c34f158ee9f94b77d5e2fb119e) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 11:58 AM
fix(setup): validate GITHUB_REPOSITORY slug in resolve_github_repository, 14 files modified (assets/parse-github-remote-lib.sh, tests/bats/init-workspace.bats)

### Commit 16: [852d77a](https://github.com/vig-os/devcontainer/commit/852d77aea0b3bfcd80ab6c155b2b07ebdd6144fc) by [c-vigo](https://github.com/c-vigo) on April 10, 2026 at 11:58 AM
fix(setup): validate parsed GitHub remote owner/repo in install.sh, 16 files modified (install.sh)
