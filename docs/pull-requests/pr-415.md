---
type: pull_request
state: closed (merged)
branch: bugfix/403-smoke-test-changelog-scaffold → release/0.3.1
created: 2026-03-23T11:26:20Z
updated: 2026-03-23T11:59:43Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devcontainer/pull/415
comments: 6
labels: none
assignees: c-vigo
milestone: none
projects: none
merged: 2026-03-23T11:59:42Z
synced: 2026-03-24T04:26:09.399Z
---

# [PR 415](https://github.com/vig-os/devcontainer/pull/415) fix(ci): smoke-test deploy uses workspace changelog scaffold

## Description

Smoke-test `repository-dispatch` deploy job no longer replaces root `CHANGELOG.md` with a minimal stub. It keeps the scaffold produced by `init-workspace`, injects the deploy bullet under the first `### Changed`, and remaps a leading `## [X.Y.Z] - …` (TBD or release date) to `## Unreleased` when needed so downstream `prepare-release` validation succeeds across RC and final cycles.

## Type of Change

- [ ] `feat` -- New feature
- [x] `fix` -- Bug fix
- [ ] `docs` -- Documentation only
- [ ] `chore` -- Maintenance task (deps, config, etc.)
- [ ] `refactor` -- Code restructuring (no behavior change)
- [ ] `test` -- Adding or updating tests
- [ ] `ci` -- CI/CD pipeline changes
- [ ] `build` -- Build system or dependency changes
- [ ] `revert` -- Reverts a previous commit
- [ ] `style` -- Code style (formatting, whitespace)

### Modifiers

- [ ] Breaking change (`!`) -- This change breaks backward compatibility

## Changes Made

- **`assets/smoke-test/.github/workflows/repository-dispatch.yml`**
  - Require existing readable `CHANGELOG.md` after install (workspace scaffold).
  - Detect first `##` section: if `## Unreleased`, leave as-is; if `## [version] - …`, rewrite first such line to `## Unreleased` via GNU `sed`.
  - Append `- Deploy devcontainer ${TAG}` after the first `### Changed` line.
- **`CHANGELOG.md`** and **`assets/workspace/.devcontainer/CHANGELOG.md`**
  - Document fix under `## [0.3.1] - TBD` → **Fixed** (issue #403).

## Changelog Entry

This branch uses the active release section `## [0.3.1] - TBD` (not `## Unreleased`). Entry added under **Fixed**:

### Fixed

- **Smoke-test deploy keeps workspace scaffold as root CHANGELOG** ([#403](https://github.com/vig-os/devcontainer/issues/403))
  - Stop overwriting `CHANGELOG.md` with a minimal stub in `assets/smoke-test/.github/workflows/repository-dispatch.yml`
  - Inject the deploy line into the `## Unreleased` scaffold from `init-workspace` so downstream `prepare-release` validation matches shipped workspace layout
  - When the first changelog section is `## [X.Y.Z] - …` (TBD or a release date), remap that top version header to `## Unreleased` before injecting the deploy entry so downstream `prepare-release` can run

## Testing

- [ ] Tests pass locally (`just test`)
- [ ] Manual testing performed (describe below)

### Manual Testing Details

N/A — workflow shell logic only; validated `sed` remap behavior locally for dated and TBD headers.

## Checklist

- [x] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [ ] I have commented my code, particularly in hard-to-understand areas
- [ ] I have updated the documentation accordingly (edit `docs/templates/`, then run `just docs`)
- [ ] I have updated `CHANGELOG.md` in the `[Unreleased]` section (and pasted the entry above)
- [ ] My changes generate no new warnings or errors
- [ ] I have added tests that prove my fix is effective or that my feature works
- [ ] New and existing unit tests pass locally with my changes
- [ ] Any dependent changes have been merged and published

## Additional Notes

- Downstream `prepare-release` expects `## Unreleased` with at least one `-` entry; remapping supports post-release `CHANGELOG` layouts where the top section is still a version header.
- **Base `release/0.3.1`:** Changelog entry is under `## [0.3.1] - TBD` → **Fixed**, matching the release branch.

Refs: #403



---
---

## Review Threads (3)

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 23, 2026 at 11:35 AM_

_File: [`assets/smoke-test/.github/workflows/repository-dispatch.yml (line 242 RIGHT)`](https://github.com/vig-os/devcontainer/pull/415#discussion_r2974483804)_

```diff
@@ -229,15 +229,25 @@ jobs:
             echo "ERROR: .devcontainer/CHANGELOG.md is not readable after ownership repair"
             exit 1
           fi
-          cat > "CHANGELOG.md" <<CHLOG
-          # Changelog
-
-          ## Unreleased
-
-          ### Changed
-
-          - Deploy devcontainer ${TAG}
-          CHLOG
+          if [ ! -f "CHANGELOG.md" ]; then
+            echo "ERROR: expected CHANGELOG.md after install (workspace scaffold)"
+            exit 1
+          fi
+          if [ ! -r "CHANGELOG.md" ]; then
+            echo "ERROR: CHANGELOG.md is not readable after ownership repair"
+            exit 1
+          fi
```

The new readability check for root CHANGELOG.md can fail even when an ownership repair would fix it. Earlier in this step, `NEEDS_CHOWN` is only triggered by `.devcontainer/CHANGELOG.md` being unreadable or root `CHANGELOG.md` being non-writable; it does not consider root `CHANGELOG.md` being non-readable. Consider including the root changelog readability probe in the `NEEDS_CHOWN` decision so the job attempts `chown -R` instead of immediately erroring when `CHANGELOG.md` exists but isn't readable due to permissions/ownership.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 23, 2026 at 11:49 AM — [link](https://github.com/vig-os/devcontainer/pull/415#discussion_r2974547572)

  Thanks — agreed. We only gated `NEEDS_CHOWN` on unreadable `.devcontainer/CHANGELOG.md` and non-writable root `CHANGELOG.md`, so a root changelog that existed but was not readable could still skip `chown` and fail the later `[ ! -r CHANGELOG.md ]` check. Added `[ -e CHANGELOG.md ] && [ ! -r CHANGELOG.md ]` to the same `NEEDS_CHOWN` block so ownership repair runs in that case too.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 23, 2026 at 11:35 AM_

_File: [`assets/smoke-test/.github/workflows/repository-dispatch.yml`](https://github.com/vig-os/devcontainer/pull/415#discussion_r2974483835)_

```diff
@@ -229,15 +229,25 @@ jobs:
             echo "ERROR: .devcontainer/CHANGELOG.md is not readable after ownership repair"
             exit 1
           fi
-          cat > "CHANGELOG.md" <<CHLOG
-          # Changelog
-
-          ## Unreleased
-
-          ### Changed
-
-          - Deploy devcontainer ${TAG}
-          CHLOG
+          if [ ! -f "CHANGELOG.md" ]; then
+            echo "ERROR: expected CHANGELOG.md after install (workspace scaffold)"
+            exit 1
+          fi
+          if [ ! -r "CHANGELOG.md" ]; then
+            echo "ERROR: CHANGELOG.md is not readable after ownership repair"
+            exit 1
+          fi
+          FIRST_CHANGELOG_SECTION="$(awk '/^## / { print; exit }' CHANGELOG.md)"
+          if [ "${FIRST_CHANGELOG_SECTION}" = "## Unreleased" ]; then
+            :
+          elif printf '%s\n' "${FIRST_CHANGELOG_SECTION}" | grep -Eq '^## \[[^]]+\] - '; then
+            echo "Remapping top ## [version] - <date> to ## Unreleased for prepare-release"
+            sed -i '0,/^## \[[^]]*\] - /s/^## \[[^]]*\] - .*/## Unreleased/' CHANGELOG.md
+          else
+            echo "ERROR: CHANGELOG.md first section is neither ## Unreleased nor ## [version] - <date>: ${FIRST_CHANGELOG_SECTION:-<empty>}"
+            exit 1
+          fi
+          sed -i '/^### Changed$/a\- Deploy devcontainer '"${TAG}" CHANGELOG.md
```

This `sed` append will run for *every* `### Changed` heading in CHANGELOG.md, which can duplicate the deploy bullet across historical release sections if the file contains more than one `### Changed`. The PR description says to inject under the first `### Changed`; consider limiting the insertion to the first match (or to the Unreleased section only) to avoid rewriting past release notes.
```suggestion
          sed -i '0,/^### Changed$/{s//&\
- Deploy devcontainer '"${TAG}"'/}' CHANGELOG.md
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 23, 2026 at 11:49 AM — [link](https://github.com/vig-os/devcontainer/pull/415#discussion_r2974547666)

  Good catch — the global `a\\` append would duplicate the deploy line under every `### Changed` in Keep a Changelog history. We dropped changelog deploy-line injection entirely: the job now only validates the workspace scaffold and remaps the top `##` header when needed for `prepare-release`, so past release sections are never edited for a deploy bullet.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 23, 2026 at 11:54 AM_

_File: [`tests/bats/just.bats (line 79 RIGHT)`](https://github.com/vig-os/devcontainer/pull/415#discussion_r2974570670)_

```diff
@@ -75,8 +75,8 @@ setup() {
     assert_success
 }
 
-@test "smoke-test dispatch generates minimal changelog for prepare-release freeze" {
-    run bash -lc 'grep -Fq -- "cat > \"CHANGELOG.md\" <<CHLOG" assets/smoke-test/.github/workflows/repository-dispatch.yml && grep -Fq -- "## Unreleased" assets/smoke-test/.github/workflows/repository-dispatch.yml && grep -Fq -- "- Deploy devcontainer \${TAG}" assets/smoke-test/.github/workflows/repository-dispatch.yml'
+@test "smoke-test dispatch normalizes workspace changelog for prepare-release freeze" {
+    run bash -lc 'grep -Fq -- "expected CHANGELOG.md after install (workspace scaffold)" assets/smoke-test/.github/workflows/repository-dispatch.yml && grep -Fq -- "FIRST_CHANGELOG_SECTION" assets/smoke-test/.github/workflows/repository-dispatch.yml && grep -Fq -- "## Unreleased" assets/smoke-test/.github/workflows/repository-dispatch.yml'
```

This Bats test no longer asserts that the smoke-test deploy workflow produces an Unreleased section with at least one `- ...` entry (required by downstream `prepare-release` validation). Once the workflow is updated to add the deploy bullet/placeholder entry, update this test to assert that behavior so regressions (like missing insertion) are caught.
```suggestion
    run bash -lc 'grep -Fq -- "expected CHANGELOG.md after install (workspace scaffold)" assets/smoke-test/.github/workflows/repository-dispatch.yml && grep -Fq -- "FIRST_CHANGELOG_SECTION" assets/smoke-test/.github/workflows/repository-dispatch.yml && grep -Fq -- "## Unreleased" assets/smoke-test/.github/workflows/repository-dispatch.yml && grep -Fq -- "- ..." assets/smoke-test/.github/workflows/repository-dispatch.yml'
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 23, 2026 at 11:58 AM — [link](https://github.com/vig-os/devcontainer/pull/415#discussion_r2974589897)

  Thanks for the nudge on downstream validation.

We are not going to grep the workflow YAML for a literal `- ...` string—that would not assert runtime `CHANGELOG.md` content and would not match anything meaningful in this file.

Separately, `prepare-release` does require at least one `-` bullet under `## Unreleased` (see the synced `prepare-release` workflow `awk` check and `prepare_changelog validate`). The workspace scaffold has headings only, so if we need that invariant again we should add a deliberate minimal bullet in the deploy job (or adjust validation/scaffold), not encode it via a bogus workflow-file grep. No change on this PR for now.


---
---

## Commits

### Commit 1: [fedb863](https://github.com/vig-os/devcontainer/commit/fedb8634f7ee7cfcece8540472eb14a8a11007ab) by [c-vigo](https://github.com/c-vigo) on March 23, 2026 at 11:22 AM
fix(ci): smoke-test deploy uses workspace changelog scaffold, 36 files modified (CHANGELOG.md, assets/smoke-test/.github/workflows/repository-dispatch.yml, assets/workspace/.devcontainer/CHANGELOG.md)

### Commit 2: [71ee226](https://github.com/vig-os/devcontainer/commit/71ee2260eee33cbb4f37a17fbb2c1d8ce611c96f) by [c-vigo](https://github.com/c-vigo) on March 23, 2026 at 11:45 AM
fix(ci): smoke-test deploy changelog chown and first Changed inject, 9 files modified (assets/smoke-test/.github/workflows/repository-dispatch.yml, tests/bats/just.bats)

### Commit 3: [42d1712](https://github.com/vig-os/devcontainer/commit/42d1712a58ba0a4face49f3152c819e3eb46431d) by [c-vigo](https://github.com/c-vigo) on March 23, 2026 at 11:48 AM
fix(ci): stop injecting deploy line into smoke-test CHANGELOG, 11 files modified (CHANGELOG.md, assets/smoke-test/.github/workflows/repository-dispatch.yml, assets/workspace/.devcontainer/CHANGELOG.md, tests/bats/just.bats)
