---
type: pull_request
state: closed (merged)
branch: bugfix/951-preview-report-exclude-venv → release/0.5.0
created: 2026-07-09T08:54:51Z
updated: 2026-07-09T09:14:08Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devkit/pull/952
comments: 1
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-07-09T09:14:06Z
synced: 2026-07-11T13:35:10.937Z
---

# [PR 952](https://github.com/vig-os/devkit/pull/952) fix(workspace): upgrade report excludes .venv/docs from the ADDED walk (#951)

## Problem

`--preview`/`--force` upgrade report over-reported ~1763 phantom `.venv/…/site-packages/*` files in the **ADDED** section that the real upgrade never writes, making the ADDED list unusable. Report-only bug — no data risk.

## Root cause

The #949 fix switched the report classifier to `find -L` (`assets/init-workspace.sh:534`) so it follows the Nix image's store-symlink template — correct for the OVERWRITTEN section. But `find -L` now also follows the baked template's `.venv` symlink tree. The report `find` excluded only `.git`, whereas the real rsync copy excludes more:

```
rsync -avL --delete --exclude='.git' --exclude='.venv' --exclude='docs/issues/' --exclude='docs/pull-requests/' …
```

So every symlinked `.venv` file leaked into ADDED.

## Fix

Mirror the copy's static excludes in the report `find` (single-site change, line 534):

```sh
done < <(find -L "$TEMPLATE_DIR" -type f \
    ! -path "*/.git/*" ! -path "*/.venv/*" \
    ! -path "*/docs/issues/*" ! -path "*/docs/pull-requests/*" -print0)
```

`.venv` is the one that leaks today; the two `docs/*` excludes are for parity so future template changes can't leak either.

## Test evidence (TDD)

New bats test `upgrade preview omits the baked .venv symlink tree from ADDED (#951)` places a symlinked `.venv/…/site-packages/phantom-venv-pkg.py` (mirroring the image's baked venv) plus a normal symlinked template file under `TEMPLATE_DIR`, and asserts the normal file is still in ADDED while the `.venv` file is not.

- **RED** (before fix): `refute_output --partial "phantom-venv-pkg.py"` failed — the `.venv` file showed in ADDED.
- **GREEN** (after fix): new test passes; the two existing #949 OVERWRITTEN/ADDED tests still pass; full `init-workspace.bats` suite 124/124 green.

Full `prek run --all-files` hook suite green (sync-manifest mirror of `CHANGELOG.md` re-staged).

Refs: #951
Closes #951


---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/devkit/pull/952#issuecomment-4923350552) by [@c-vigo](https://github.com/c-vigo)

_Posted on July 9, 2026 at 08:56 AM_

Reviewed: single-site change adding `! -path "*/.venv/*"` (+ docs/issues, docs/pull-requests) to the report `find` at init-workspace.sh:534, mirroring the rsync copy's static excludes. Test reproduces (.venv symlink shows in ADDED before, excluded after; #949 OVERWRITTEN/ADDED tests still green, 124/124). Changelog under [0.5.0]-TBD as a #949 follow-up. Minimal + clean. LGTM — auto-merge stands.

---
---

## Commits

### Commit 1: [90f56b8](https://github.com/vig-os/devkit/commit/90f56b8a5eeb34814f83c5a23d2ac44db5bbb5ae) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 08:50 AM
test: cover upgrade preview over-reporting the baked .venv tree, 48 files modified (tests/bats/init-workspace.bats)

### Commit 2: [1e144b0](https://github.com/vig-os/devkit/commit/1e144b0411fbfcc58c8de352878d62ee1caa097c) by [c-vigo](https://github.com/c-vigo) on July 9, 2026 at 08:52 AM
fix(workspace): upgrade report excludes .venv/docs from the ADDED walk, 10 files modified (CHANGELOG.md, assets/init-workspace.sh, assets/workspace/.devcontainer/CHANGELOG.md)
