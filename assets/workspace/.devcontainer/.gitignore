# User-specific configuration (personal, not shared with team)
.conf/
.env
.env.local
docker-compose.local.yaml

# VS Code workspace file (personal editor layout)
workspace.code-workspace

# Temporary files
*.tmp
*.log
